# archive/INDEX.md — ledger of archived docs

One entry per archived doc: status, what it was, and the case law worth
citing before re-attempting anything it rejected. Stamped 2026-07-17.
Archived docs are immutable history — the current-state truth lives in
`docs/architecture/`; open work is indexed in `docs/BACKLOG.md`.


## Meta-tools

- **`inspector-read-only-subjects-plan.md`** — COMPLETE. An inspector opened on a value that cannot service an EDIT (a number, a string, a Point) presents a read-only inspector instead of controls that throw. Naming became a service (`Utils.derivedColloquialName` / `colloquialNameOf`); the bottom edit row is built, laid out and reached-into as ONE conditional unit.
  - ⚖ `colloquialName` was NOT extractable-and-deletable: ~50 classes override it, several answering what only that object knows (a transform frame returns its sole content's name). The polymorphism is load-bearing — hence a derive-only base plus an ask-then-derive service, not one function.
  - ⚖ A `ClassInspectorWdgt`'s subject is a PROTOTYPE and is NOT always editable — `Point.prototype` does not inherit the edit verbs. Measured; reasoning from "classes carry the meta verbs" gets it backwards.
  - ⚖ Capability query (`injectProperty?`), never `instanceof Widget`: the question is whether the subject services the protocol.
  - ⚖ Display was generic ALL ALONG — the member list already populated for primitives, so the planned third phase was a no-op. Census before scoping.

## Layout & settle campaigns

- **`single-geometry-visual-wave-plan.md`** — EXECUTED AND CLOSED 2026-08-24/25 (Plan 3 of `docs/plans/frames-input-touch-program.md`; Fable-authored; six probe iteration rounds closed twenty owner findings BEFORE a single reference moved). ONE touch-capable geometry for mouse and finger: 44 targets / 24 glyphs / thin always-on indicators (the fade never shipped — visibility is a pure derivation from overflow), 44px menu rows with painted hairline separators, GRADED bar titles (full / >=70%-and->=5-chars ellipsis / absent — chrome never garbles), the piece roster degrades by fit to an empty bar, toolbars hug their cell grid and DERIVE their dock thickness from it, 1-row strips overflow into a chevron whose remainder menu is derived at pop, the resize handle is a 44 triangle (hit = ink) hidden while collapsed; a collapsed frame is not resizable (bar drag moves; uncollapse restores extent). Deleted: `_makePrettier` (the suite-vs-product fork — references now pin what users see), the input-mode apparatus (T11), `toolbarDockThickness` + six per-toolbar constants, `anyScrollBarShowing`, a 2018 UA-sniffing bold-title fork. Test world 960x440 -> 1920x880, Automator 0.2.0 -> 0.3.0, 1,898 references recaptured (184MB). Case law: a census's AXIS is its blind spot (F7: the world menu opens by raw right-click — no `openMenuOf` grep can see it); the recapture completeness gate catches what review cannot (zero-byte refs, nondeterminism-that-wasn't); an accidental crutch can mask a real cost for years (the failure-image budget hid SWCanvas's ~36MB-per-image pure-JS PNG encoder until the extent removed the mask); a fixed-fraction assertion is extent-rent (derive oracles from the fixture's own bounds); the gauntlet's deep legs caught three wave regressions presuite structurally cannot (a UA fork exposed by a preference fold, an in-pass destroy re-entering a gesture seam, a rig expectation encoding the pre-fold world). Wave commits: Fizzygum `5a45a135` / tests `530b49d4e`.
- **`command-panel-unification-plan.md`** — EXECUTED AND CLOSED 2026-08-25/26 (Plan 3.5 of `docs/plans/frames-input-touch-program.md`; Fable-authored; the T1 tail row as owner-expanded). ONE command MODEL over two projection surfaces: `CommandSpec` (né MenuItemSpec) = the command record — label + optional `icon` slot + target/action — carrying the dispatch-contract LAW in its header (a projection of a command invokes the SAME action path as the primary: the row's four-slot trigger / the cell's `thumbnailClickReceiver()`); `CommandPanelWdgt` (né MenuRowsPanelWdgt) the column panel, verbatim under the rename; the display axis LIVE — the chevron's remainder rows show each hidden tool's rasterized miniature beside its label (rasterize-once: `InertIconHolderWdgt` holds a CANVAS shot of the sized throwaway copy, never a live widget); the Draw palette a `ToolbarWdgt`-family strip (`RadioButtonsHolderWdgt` deleted; `dockThickness: 103` — the tree's last declared dock-thickness constant — dead by derivation, band 103→~70, cells 44); NEW core `User` model (`world.users`/`world.user`) — the armed drawing tool is the PERSON's fact (owner-directed at OD3), palettes are views+controllers via one dataflow announce edge each (the wallpaper-menu double-link one level up; `DataflowSource` the non-widget-source precedent), a new drawing is born painting the HELD tool (pencil on an empty hand). Case law: the ONE-CLASS panel merge was FALSIFIED by measurement — 17 friction members (8 contract denials of its own base + 3 `Widget::` parent-skips + 6 core behaviours copied into app-kit) proved the two arrangements share ARITHMETIC and a MODEL, not an is-a; the pre-authorized fallback taken owner-ruled. `catchesPointerAt` on a holder cannot shield its CHILD (children-first hit test; rows act by ESCALATION) — a picture is made a picture by construction, not by a role flag. A P0 rename REHEARSAL (spike suite run over both repos) measured zero pixel exposure and made the two-repo sweep routine; all 15 §1 facts held at P0 re-verification — a program first. Commits: P1 `7a6b56cd`, P2 `89e7098b`/`2276fd8ab`, P3 `8ac5d256`/`54f20ab34`, P4 `97a7a647`/`b6912bc54`, + the close commit.
- **`pointer-events-plan.md`** — EXECUTED AND CLOSED 2026-08-24 (Plan 2 of `docs/plans/frames-input-touch-program.md`; Fable-authored, executed same day). ONE `PointerInputEvent` family (down/move/up/cancel) replaces the six Mouse*/Touch* adapters + their two bases; one listener set (`touch-action: none`, guarded `setPointerCapture`, no preventDefault); the hand takes the event value (`processPointer*`), carries `pointerType`, and `processPointerCancel` is a state-table ABORT; the masked page-offset double-add and the Firefox resize `event` global died with it. Pixel-neutral (dpr1 + WebKit byte-identical); 2 witness tests.
  - ⚖ The plan's own premises fell in execution the same day it was authored: F12's "~8 test dirs" was 3; §2.6's literal `synthetic undefined, undefined, …` spelling was unbuildable against the HARD positional-hole stink (the family rule: a hole means REORDER — trailing optional pair); `fg smoke` is not a subcommand.
  - ⚖ A falsified oracle can be the better one: "knob returns to black" was wrong — the per-cycle hover re-sync paints it HOVER blue, which is the sharper proof (`mouseMove` early-returns while pressed).
  - ⚖ An assertion is a census axis screenshots never sweep; and a metadata edit after page generation is exactly what `check-visualisations --strict` exists to catch (it did, at the close gauntlet).

- **`frame-lifetime-and-docking-plan.md`** — EXECUTED AND CLOSED 2026-08-24 (Plan 1 of `docs/plans/frames-input-touch-program.md`). ONE chrome container: `FrameWdgt` carries `lifetime 'transient'|'persistent'`; menu/window/card are DERIVED manifestations; `MenuWdgt`/`PromptWdgt` are framed citizens; `PopUpWdgt` + `MenuHeader` deleted; docking = `EdgeDockLayoutSpec` (the same frame docks/floats/re-docks; `floatToolbar` deleted); the climb is `enclosingFrame`, the predicate `isPersistent`.
  - ⚖ Eight of the plan's own premises were falsified in execution (F13's zero-callers, C6's deletion clause, C13's letter, the dockThickness formula, F9's centring, the 4b/4c counts, "P4 pixel-identical") — the STATUS box is the as-executed log; amend the ledger with date+reason, never silently.
  - ⚖ The gauntlet caught three real defects presuite structurally cannot see (settle: a derive that CONSTRUCTS runs only inside a flush; capstone: the DISPATCHER owns the callback settle; revisits: arming a placement-owning spec clears pending wishes) — a hook-path phase never closes on presuite alone.
  - ⚖ Do NOT re-attempt: merge-on-drop, a "pinned menu" skin, a grip class, a per-frame barSide knob, a lifetime policy-object hierarchy (§8 of the plan carries the full list with reasons).

- **`all-constructors-settle-plan.md`** — COMPLETE. Converts ~27 inline-building constructors to the uniform self-settling _buildAndConnectChildren wrapper/core pattern.
  - ⚖ notification-settle gate refined to permit orphan-receiver settle in a callback, not weakened
  - ⚖ ScrollPanelWdgt needed a DISTINCT _buildScrollFrame name, not the shared leaf core name
- **`app-descriptor-unification-plan.md`** — COMPLETE. Gives each windowed app ONE descriptor (`src/AppCatalog.coffee`, keyed by class NAME) that both launcher modes read, replacing `title`/`buildIcon`/`toolTip` duplicated across 14 app classes and their two call sites.
  - ⚠⚠ the duplication had already SHIPPED a bug, and not by drifting: the lazy path simply lacked `toolTip`, so two desktop icons lost their bubble help — incompleteness is invisible to review and to every gate, which is why the fix is one reader, not a check that two copies agree
  - ⛔ the descriptor may NOT live on the app class — reading a field off an app at boot is the reachability the boot-cost arc removed; keyed by NAME always
  - ⚖ §5.1 — a launcher's caption belongs to the APP, not the placement: a per-placement name puts two differently-named icons for one app side by side once one leaves the folder. No label override
  - ⚠ meta-system constraint found in execution: a class-level object must be a METHOD and each entry ONE LINE (Class.coffee compiles each member's body alone) — invisible to `coffee -c`
- **`basement-dormant-layout-flag-plan.md`** — PARKED. Proposes a cached per-widget flag to skip layout-invalidations for widgets sitting in the CLOSED (dormant, off-world) basement.
  - ⚖ §4 — blanket orphan-skip REJECTED, previously broke 63 tests
  - ⚖ §6 — safer seam-by-seam alternative to weigh before building the flag
- **`caret-follow-in-place-settle-plan.md`** — PARKED. Proposes folding the caret's typing/delete scroll-follow into an in-place per-event settle instead of the end-of-cycle flush.
  - ⚖ purity refinement only — if not byte-exact with reasonable effort, LEAVE the status quo
  - ⚖ caret must NOT coalesce (a past coalesce-the-caret attempt was wrong, corrected)
- **`caret-scroll-follow-single-pass-plan.md`** — COMPLETE. Makes the caret's scroll-into-view converge in a single settle pass instead of re-visiting the layout loop up to 372 times.
  - ⚖ §4.3 internal-loop fallback NOT needed — §4.1+§4.2 alone reached zero re-visits
  - ⚖ Point.floor() clamp-to-≥0 was the root cause of the multi-pass crawl
- **`claimsspace-footprint-default-and-scroll-reachability-plan.md`** — COMPLETE. Makes 'footprint' the default claimsSpace mode and makes scroll extent track ink reachability in every mode (owner decisions D1+D2).
  - ⚖ shrink-to-fit stays FORBIDDEN (D3 — cyclic-constraint / LivelyKernel precedent)
  - ⚖ claimsSpace gates ASKING containers only, never DICTATING _applyExtent (owner correction)
  - ⚖ CSS-like-scroll-without-default-flip and slot-default-with-opt-in both SUPERSEDED by D1+D2
- **`coalesced-nomenclature-rename-plan.md`** — COMPLETE. Renames the layout deferred-settle family off the ambiguous word 'coalesced' before the dataflow-engine work begins.
  - ⚖ *EndOfCycle and *Streamed suffixes REJECTED as naming candidates
  - ⚖ the menu-takeover homonym (takesOverAndCoalescesChildrensMenus) is unrelated, handled separately
- **`connection-cascade-settle-fix-plan.md`** — COMPLETE. Adds a dedicated 'connector' settle-lane so reactive wiring cascades (e.g. the C↔F converter) settle once instead of throwing.
  - ⚖ rejected: relax setText's flow guard generally — would hide genuine internal-layout misuse
  - ⚖ rejected: dispatch the cascade to the raw _setTextNoSettle core — no cycle-guard, infinite-loop risk
  - ⚖ rejected: deferred/queued propagation — breaks the circuits' synchronous semantics
- **`deferred-layout-16-macro-breakages.md`** — COMPLETE. Catalogues 16 construction-macro breakages under the first deferred-geometry API and root-causes each one.
  - ⚖ the eventual fix was NOT either weighed option — a 3rd approach (self-settling setters) shipped instead
  - ⚖ M4 — slider thumb positioned from a stale parent origin (settle-before-add or framework fix)
- **`deferred-layout-OVERVIEW.md`** — COMPLETE. Former entry point for the deferred-layout campaign; its durable content was merged into the architecture assessment doc.
  - ⚖ its own §3 'deferred re-queue is the mechanism' framing was later OVERTURNED by the 2026-07-01 seam deletion
- **`deferred-layout-c2-execution-plan.md`** — COMPLETE. Converges the container re-fit cascade in-pass via a deferred re-queue instead of a synchronous re-fire.
  - ⚖ naive no-op seam removal broke 7 tests across 3 families — not a viable path
  - ⚖ the scroll/REACT arm breaks INDEPENDENTLY of the clock — a clock-only fix could never enable C3
- **`deferred-layout-capstone-execution-plan.md`** — COMPLETE. Retires the world._reFittingContents counter and tightens layering lint [E] to close the deferred-layout campaign.
  - ⚖ blanket wEl/wStk deletion and lazy GET-time capture BOTH falsified — a surgical elasticity-0 fix used instead
  - ⚖ forbidding _reLayoutChildren by name DECLINED as cosmetic, no real added protection
- **`deferred-layout-path-a-design.md`** — COMPLETE. Records why blanket 'pending-aware geometry accessors' diverges, and the per-reader taxonomy that replaced it.
  - ⚖ blanket pending-aware conversion made failures WORSE (16→17→18), regressed 3 previously-green tests
  - ⚖ a pending read over-sizes scroll content by 43px — proven wrong, not merely different
- **`deferred-layout-refit-and-add-design.md`** — COMPLETE. Design for the _reLayoutChildren re-fit chokepoint and a public self-settling add/addRaw over a private _addCore.
  - ⚖ construction-time settles are NOT byte-safe idempotent — half-built widgets reachable via .parent, fixed via isOrphan() skip
  - ⚖ giving stack/window panels the scroll-panel's _reLayout pattern directly HANGS — not a quadratic-add bug
- **`deferred-layout-residuals-audit.md`** — COMPLETE. Read-only audit mapping every synchronous relayout still at a non-settle point across the deferred-layout campaign.
  - ⚖ families 1 (scroll-input) / 6 (Slider) / 7 (LabelButton): verdict LEAVE SYNCHRONOUS, wrong problem class or no gain
  - ⚖ soft-wrap caret-follow deferral PROBED and REJECTED — broke 7 scroll-follow tests
- **`deferred-layout-slice2-completion-plan.md`** — COMPLETE. State record after Phase 3b of the deferred-layout migration; documents the FLOW RULE that raw setters never schedule layout.
  - ⚖ FLOW RULE violation froze 9/12 desktop apps — a raw/silent mutator must only mutate, never schedule
  - ⚖ createErrorConsole recovery must run OUTSIDE the flush, not inline in the recalc catch
- **`freefloating-invalidation-skip-centralization-plan.md`** — COMPLETE. Consolidates the ATTACHEDAS_FREEFLOATING teardown invalidation-skip into one Widget.invalidateLayout(triggeringChild) parameter across all 5 propagation sites.
  - ⚖ widening sweep found NO further safe skip targets — surface fully captured
- **`hover-resync-after-flush-plan.md`** — COMPLETE. Swaps hover re-sync to run after the coalesced end-of-cycle flush so hover reads settled geometry, matching what paint reads.
  - ⚖ capstone gate exited 0 on a crashing/failing suite — fixed to also fail on suite runner exit != 0
- **`layout-optimizations-and-oo-cleanup-plan.md`** — COMPLETE. Post-seam-deletion layout engine optimization + OO-cleanup campaign across Tiers A-J: dedup, geometry-cache versions, wart hunts, bounds-cache.
  - ⚖ Appendix X1-X9 bank every considered-and-rejected idea — do not re-derive
  - ⚖ layoutEngine-object encapsulation — RULED OUT by owner
- **`layout-regressions-2026-07-icons-plots-editghosts-plan.md`** — COMPLETE. Bisect-rooted fix plan for 4 layout regressions (desktop icons, plot collapse, edit/view ghosts, slide scroll drift), plus a paint-truthfulness capstone gate.
  - ⚖ Plot 'content-latency' ghost was NOT a bug — intended live animation, proven by freeze test
  - ⚖ Layering rule [D] blocks a dropped-invalidation macro even inside evaluateString strings
- **`layout-settle-tier-rename-plan.md`** — COMPLETE. Renames the layout-settle tier to private, layout-explicit names (mutateGeometryThenSettle to _settleLayoutsAfter, *Core to *NoSettle).
  - ⚖ check-layering.js hard-codes tier names by string — must update in lockstep
- **`layout-system-architecture-assessment.md`** — COMPLETE. Canonical description of Fizzygum's layout engine + the rulebook for introducing a new layout; absorbed the former deferred-layout overview doc.
  - ⚖ §4.3 layoutEngine-object encapsulation — RULED OUT (bury-it-deeper)
  - ⚖ §4.2 per-axis DAG lint — falsified as a convergence proof
- **`ordered-downwalk-stage-b-plan.md`** — COMPLETE. Builds the ordered root-down settle walk as an engine upgrade, deleting the last per-class composite-relay capability declarations.
  - ⚖ §2 — settled layout is order-independent; acceptance = re-lay SET trace not order
  - ⚖ OPS trap — build_it_please.sh aborts but exits 0 if umbrella misnamed
- **`orphan-settledness-plan.md`** — COMPLETE. Closes the I2-on-orphans settledness gap so public calls and constructors on orphan widgets settle synchronously via cores.
  - ⚖ @add→@_addNoSettle byte-identical only for standard Widget.add, not custom-add bases
  - ⚖ Constructors now DO settle via auto-deferring wrapper, superseding original framing
- **`paint-time-caret-resync-plan.md`** — COMPLETE. Plan to move the caret's paint-time layout re-sync out of the read-only paint pass, per the owner's events-flush-paint invariant.
  - ⚖ Owner rejected a 3rd _settleLayoutsAfter variant and try/finally flag toggles
  - ⚖ Clean/elegant code prioritized over dodging a benign inspector recapture
- **`private-noLayouting-core-callpaths-plan.md`** — COMPLETE (residual in BACKLOG.md). Gives every public layout-settling method a private NoLayouting core so private teardown/build chains never re-enter the public settle tier.
  - ⚖ _addCore/_addRawCore split byte-identical only because callers add fresh non-world children
  - ⚖ Orphan-guard must precede flow-throw so orphan construction under a flush defers
- **`proper-layouts-4.1-pure-measure-campaign-plan.md`** — COMPLETE. Builds the pure preferredExtentForWidth measure protocol (text, stack, window, scroll-panel) to delete the mutate-then-read-back sizing seam.
  - ⚖ Stage-D's 6 mismatches were deferred-relayout convergence lag, not bugs (Stage-E boundary)
  - ⚖ Measure, don't mutate-and-read-back — a 'measure' touching @bounds has failed
- **`proper-layouts-4.2-structural-arrange-plan.md`** — COMPLETE. Attempts a single-pass measure-up/non-notifying-arrange-down restructure to delete the re-fit seam; Stage 4's structural edge falsified, closed via the 4.4 arc.
  - ⚖ Option B scroll choke-points FALSIFIED — real edge is content's own base _reLayout
  - ⚖ By-PHASE split FALSIFIED — 6 of 10 job-B tests need in-pass firing
  - ⚖ world.layoutEngine relocation REJECTED — burying the boolean deeper, not deleting it
- **`proper-layouts-4.4-ordered-downwalk-plan.md`** — COMPLETE. Original down-walk-as-seam-replacement design; all seam-deletion paths falsified — the walk was later rebuilt as a pure engine upgrade elsewhere.
  - ⚖ Analytic position-frame decoupling FALSIFIED — container arrange already idempotent, iterating is a no-op
  - ⚖ Do-NOT-reattempt list: non-notifying conversion, sync in-arrange fixpoint, boundingBox() read-back drop
- **`proper-layouts-eliminate-suppression-booleans-plan.md`** — COMPLETE. Roadmap (Phases A-F) to delete Fizzygum's layout-suppression booleans; deletes @_adjustingContentsBounds narrowly, later fully completed elsewhere.
  - ⚖ Full seam deletion REVERTED — broke 8 tests, scroll 'careless' pushes ARE convergence
  - ⚖ Find WHICH non-idempotency perpetuates a cycle before fixing all of them
- **`proper-layouts-geometry-seam-removal-plan.md`** — COMPLETE. Removes the last geometry re-fit sub-seam via a settle-time ordered re-fit, proving the earlier 'irreducible' verdict over-general.
  - ⚖ Prior 'irreducible' verdict proven over-general 3x — don't over-generalize from failed stages
  - ⚖ Do NOT re-run the 8 already-falsified paths (§2); start from fresh angles
- **`retire-adjustingContentsBounds-via-text-measure-plan.md`** — COMPLETE. Investigates retiring @_adjustingContentsBounds via a pure text-height measure; the keystone premise (a height read-back) is false.
  - ⚖ measureWrappedHeight targeted the wrong read — real driver is in-pass contents positioning
  - ⚖ 'Silent' setters still fire the re-fit seam — only the flag suppresses it
- **`settle-tier-followups-examination-plan.md`** — COMPLETE (residual in BACKLOG.md). Post-orphan-settledness examination across 5 topics: determinism flake, lint symmetry, NoSettle naming audit, constructor settling, allowlist sanitization.
  - ⚖ Topic 1 'flake' was a false stall-timeout keyed off wall-clock, not a pixel bug
  - ⚖ Owner wants exemption markers/allowlists re-tested against today's code, not left standing
- **`layout-spec-family-unification-plan.md`** — COMPLETE (authored + executed 2026-08-04, one session). Deletes the layout-configuration split: the `LayoutSpec` enum + loose box fields + ad-hoc corner fields → ONE per-child spec-object family (`Widget.layoutSpec`, nil = free-floating), the division machinery extracted into the axis-parameterized `StackLayoutEngine` — which is what made vertical division, the axis-keyed divider, cross-axis alignment and border-layouts-by-composition fall out; the drag-drop scaffold promoted to product ("edit layout", chrome in the lazy `authoring` part).
  - ⚖ The two stack engines were complementary halves (main-axis DIVISION vs cross-axis FIT) — one axis parameterization, no new engine
  - ⚖ Specs SURVIVE detachment (per-class lifecycle: division box = per-widget knob; content-stack spec = per-placement, kept) — the faithful translation of the old tag+details split
  - ⚖ A capability predicate may only land WITH its callers (dead-method gate); macros forced the public `divisionBox()` door (rule [D])
  - ⚖ The `_`-tier rename of `addOrRemoveAdders` EXPOSED a pre-existing [G] violation its public name had hidden (in-arrange `fullDestroy`)
  - ⚖ The FrameWdgt resizer TODO was a pure deletion — it was corner-attached all along, the hand-place was a redundant re-commit
- **`layout-spec-family-followups-plan.md`** — COMPLETE (executed 2026-08-04/05; F4 in its own owner-ruled session). Finishes what the unification opened: the division CELL's knobs go menu-reachable (spec-carried "layout in row/column ➜" submenu, settle-on-element setters — the VSLS idiom transposed), "edit layout" drop-slots extend to CONTENT stacks (document surfaces the toggle, entries target the inner stack; spec-less droplets the arrange adopts; one membership-parameterized scan), y-axis crossAlign pinned, and the frame toolbar dock completed — `right`/`bottom` arranges as pure transpose twins through the one-home chrome math + the D9-tail "float the toolbar" context-menu entry (in-place pop-out into the creator-button float home).
  - ⚖ F4 resizer ruling: the bottom-right resizer MAY overlap a right/bottom dock — the same trade content makes under `resizerCanOverlapContents`; an inset dock would couple its extent to the resizer and break the mirror symmetry
  - ⚠⚠ FOUR latent bugs surfaced by FIRST-CALLER tests: the division reconciler's mid-pass FLOWRULE pair + careless `showAdders` (F2), and `FrameWdgt._addNoSettle`'s `removeChild @contents` clobbering a STILL-PARENTED ctor argument's old parentage (F4 — every prior caller passed orphans)
  - ⚖ CoffeeScript defaults swallow nil sentinels (`axis = 'x'` replaced the meaningful nil); ⚖ menus taller than the world clamp their TOP — the tail is unreachable at 960×440 (escape-hatch precedent re-affirmed on the shrink-to-fit beat)
  - ⚠ hierarchy-menu rows are CLASS-named ("a Document"), never colloquial ("a Docs Maker") — a menu-driving macro must descend by class name
- **`stretch-layout-spec-unification-plan.md`** — COMPLETE (executed 2026-08-06; P0 design owner-reviewed 08-05/06, P1–P4 single session). The stretch mechanism joined the LayoutSpec family: the fractional trio → `StretchLayoutSpec` (fifth member, EDGE-fraction encoded, `provisional`-provenanced), `StretchablePanelWdgt._reLayout` → the engine-standard grant-bounds arrange (founding "TODO antipattern" deleted), the desktop adopted the spec, the island transfer shrank to `_moveKeptStackSpecTo` (stack spec only); DELETED: the trio prototype fields, the half-recorders + fractional-derivation helpers, the two stale synchronous handle-remember hooks + `changeShouldRememberFractionalGeometry` + the `widgetStartingTheChange` param family, the world-add half-record, `consumesFractionalChildGeometry`. Suite 280→281 (detach/re-add lifecycle test, non-vacuous twice); gauntlet 14/14, census 0-movers, revisits exact, rigs + homepage green.
  - ⚖⚖ AUTHORITY DIRECTION is the family's real axis: `LayoutSpec.ownsPlacement() -> true` (arrange places FROM the spec, gestures edit the spec) vs the stretch FOLLOWER (`false` — the record trails the child's geometry); `isFreeFloating` = "no spec OWNS my placement", so redefining the ONE query preserved all 16 read sites by construction — the audit showed every site WANTS today's behavior for stretch children
  - ⚠⚠ an ARMED spec is not an inert field: firing the recorder unconditionally from F6 drop/uncollapse sites planted stretch specs on children of stacks/documents (TypeError in `setAlignmentToCenter`, a zero-screenshot shard stall) — the recorder now gates on `fig.parent.consumesFractionalGeometryOf(fig)`; the OLD field writes were harmless on any parent, which is why this class of bug had no precedent
  - ⚖ the dead-method gate REJECTED landing the spec class before its callers (the "capability lands WITH its callers" law, enforced structurally) — plan batches accordingly
  - ⚖ EDGE fractions (D7): two children recorded abutting share the rounded boundary at every holder size — the StackLayoutEngine boundary idiom carried into the spec encoding; churn was ±1px extents in 6 stretch tests + the inspector member-list wave, nothing else
  - ⚖ D8 provenance: heal → `provisional: true`, the fill drain re-derives provisional records ONCE at builder-final geometry — the place-before-add builder rule retired; the two drain Sets stay separate (two different measured MOMENTS, not a redundancy)
- **`kept-layout-spec-unification-plan.md`** — COMPLETE (executed 2026-08-06, single session; P0 in TWO ROUNDS — the round-1 slot-with-dormancy package superseded mid-review). The kept tier joined the one lifecycle as the KNOB MODEL — attachment ≠ preferences: the slot (`layoutSpec`) stayed byte-untouched (nil = free-floating, exactly as before); the family's lifecycle collapsed to TWO kinds — carrier-owned KNOB (`_divisionBox`, `cornerSpec`, and the re-classified content-stack spec, renamed `_stackElementSpec` → `_contentStackSpec`) vs per-attachment RECORD (stretch); container-side reads go SLOT-FIRST through the one accessor `Widget.contentStackSpec()`; `_moveKeptStackSpecTo` (the last island hand-carry) DELETED with ZERO replacement plumbing — the island's slot rides the `layoutSpec:` add-arg while the knob never leaves the wrapped content. Suite 281→282 (`macroContentStackKnobSurvivesDetachReadd`: the ⚖⚖ survival law on object identity + knob value through the desktop-sojourn coexistence flow; non-vacuous twice); gauntlet 14/14, both rigs, homepage round-trip, revisits exact.
  - ⚖⚖ A MECHANISM WHOSE ONLY PURPOSE IS PRESERVING BEHAVIOR IS A BACKCOMPAT SMELL: round 1 decided an `armed` flag + dormant state + a `parkedKeptSpec` parking rule (spike-proven suite-invisible!) — the owner's clean/uniform criterion exposed the parking rule as pure behavior-preservation machinery for a contention (armed stretch record vs dormant memory on every desktop sojourn) that slot-homing kept memory CREATES; the knob model makes it unrepresentable. Ask the clean-model question BEFORE ratifying a working design.
  - ⚖ the authoring reframe ("make dormancy first-class in the slot and the shadow tier dissolves") was HALF-WRONG by measurement: the hand-carry + the 14 bypass reads traced to READS bypassing the active tier (one slot-first accessor retires both), the fullCopy single-owner rule guarded a hazard the modern engines don't have (both identity-map every object — probe-proven: cross-widget sharing fullCopies and snapshot-round-trips as ONE object), and "dormant state has no home" was the mis-attribution — it HAS one, the carrier's field
  - ⚠⚠ HARNESS ROBUSTNESS CASE LAW: a prototype-member change (one added + one deleted, alpha's index −1) broke `macroDuplicatedInspectorDrivesCopiedTargetOnly` NOT via pixels but via the DRIVE — the toolkit's scrollbar-handle drag is quantized to scrollbar pixels, the target row landed edge-clipped, and the fixed-offset click (row.topLeft+2px) selected NOTHING, so the save no-opped; fixed by clamping the click Y into the pane's visible box (toolkit verb + the test's local twin), zero-delta for fully-visible rows
  - ⚖ E7's churn prediction was CONSERVATIVE: the P1 mechanics + the P2 rename produced ZERO suite churn beyond the one broken-drive test (the rename is length-neutral and no captured inspector window shows the `_`-prefixed own-prop block); the slot-first re-route was byte-identical everywhere the accessor degenerates to the field
- **`island-content-preferences-plan.md`** — COMPLETE (owner-ratified P0 + executed 2026-08-06, same day as authoring). The island is a LENS: base `TransformFrameWdgt`'s two content-stack initialiser overrides DELEGATE creation to `_soleContent()`'s own class-specific initialiser and SHARE the object (never read-time delegation, never a value copy) — completing the island's "invisible plumbing" doctrine family (isTransparentAt / escalate-only click / resolvesEditorSelectionToContent). A rotated clock's window is height-locked again while wrapped; a rotated stack element adopted by a document applies the content's alignment/grow edits; edits made through the island hit the one knob the content resumes on unwrap. Suite 282→283 (`macroIslandLensWindowHeightLock` — identity oracles + wrapped-resize height-lock, non-vacuous by pre-seed plant); both kept-spec probes extended (E sections) + updated to the post-fold rename; gauntlet + homepage green.
  - ⚠⚠ ONE SEAM ASKS WHILE THE ISLAND IS EMPTY: `_materializeSugarIslandNoSettle` homes the island into the former parent BEFORE reparenting the content (load-bearing skin-derivation order), so a FrameWdgt former parent's mount guard runs the initialiser on a contentless island — the lens override's empty-fallback correctly answered base defaults, i.e. the PRE-LENS bug reappeared through timing alone (the probe's E1 caught it; a delegating override is only as good as WHEN it is asked). Fix at the materialize: PRE-SEED the island's field with the content's knob before homing.
  - ⚖ the sharp NON-VACUITY oracle is the IDENTITY asserts, not the value assert: under the plant the content's own knob still reads canSetHeightFreely=false (true but ungoverned — the fresh island spec ruled), while the two sharing asserts + the wrapped-resize pixels flip; pick oracles that read the MECHANISM, not a value that survives its absence.
  - ⚖ owner ratification was sought and given on the PURE-CLEANLINESS question (is the lens the Right Thing regardless of compat) — the restored pre-fold behaviors are consequences of the model, not the motivation; the ratified framing is the doctrine-family completion.
  - ⚠ the `.scratch` kept-spec probes had rotted against the fold's own P2 rename (`_stackElementSpec` → `_contentStackSpec`) — a probe named as a standing oracle must be re-run (and re-aligned) when the field it reads is renamed; both now assert the LENS shape (island field shares the knob).
  - the surfaced observation (bar reads "transform frame" while wrapped) was LANDED same day as the THIRD lens member, owner-directed: `colloquialName` reads through to `_soleContent()` ("transform frame" only when empty), and — the pre-seed's timing twin — since the frame bar CAPTURES the name during the materialize's empty window, the island's `_reactToChildAdded` nudges the new intent-named public note `FrameWdgt.noteContentsNameMayHaveChanged()` once the content arrives. The name capture and the spec initialise are the SAME seam class: any lens answer a consumer captures during the empty window needs an arrival-time nudge or a pre-seed.
- **`desktop-corner-spec-dissolution-plan.md`** — COMPLETE (executed 2026-08-06, same day as authoring; both phases, single session). The LAST `instanceof` placement dispatch died: corner-spec sizing became OPTIONAL (a zero-size declaration only PLACES, by the carrier's own per-axis extent — no mode flag, the degenerate combo became meaningful), `BinOpenerWdgt`/`AnalogClockWdgt` carry creator-armed `cornerSpec` knobs (armed by `createDesktop`/`binIconAndText` explicitly — NEVER via `defaultLayoutSpecWhenAddedTo`, which would re-anchor on every user re-drop), `_reLayoutDesktop` shrank to the generic consumed-children loop + the corner pass, and `userMovedThisFromComputedPosition` was DELETED at all 6 sites ("corner-anchored until the user intervenes" IS which-spec-is-armed). Anchored placements byte-identical (probe-pinned at two window sizes); suite churn = exactly the 2 clock-inspector tests (new `cornerSpec` own-prop row), eyeballed benign, recaptured COMPLETE; gauntlet + apps + homepage green.
  - ⭐ THE FOLD FIXED A CONFIRMED LATENT PRODUCT BUG: pre-fold, a grabbed (spec-less) opener made `_reLayoutDesktop` throw `spec.leftFraction`-on-nil on EVERY browser resize, aborting the whole desktop reflow — the probe's baseline pins the grabbed CLOCK visibly frozen (not tracking) because the opener block ran first. The §1 census's "post-grab life is clamp-only" described the code's INTENT, not its behavior — a census claim about a path no gate exercises deserves a probe before it is believed.
  - ⚠⚠ AN ARMED OWNING SPEC IN A CONSUMING HOLDER was a case the family had never hosted, and TWO standing mechanisms each tried to destroy it: the __add seed drain would have replaced the corner knob with a stretch record one cycle after arming (the fill gate only asked `!isStretchElement?()`), and `loadWorldSnapshot` step 7's bare `_addNoSettle` resolved the nil default over every deserialized slot (invisible for years because world children only ever carried nil or re-derivable stretch records). Both fixed at ONE homes: the recorder refuses any spec that `ownsPlacement()`; step 7 passes `layoutSpec: child.layoutSpec` (the snapshot's attachment state is authoritative — which also stopped the per-load stretch-fraction re-derive drift).
  - ⚖ the corner pass placing by the carrier's OWN extent needed no square constraint — the opener is 95×92 and anchors exactly where the deleted hand formula put it (bottomRight − extent − padding is the corner formula verbatim)
  - ⚖ census fate correction: self-reads of a widget's OWN knob (StretchableWidgetContainerWdgt, the W6 toggles, the W7 builder edits) correctly stay FIELD-direct — the field is the knob's home; only reads that must see an ISLAND's riding spec route through the accessor
  - ⚠ per-attachment lifecycle: any reparent nils the active spec and re-entry derives FRESH — cross-panel stale-fraction carryover (old panel's proportions snapping in the new panel) died as a conscious behavior change
  - ⚠ never launch an fg run while the previous wrapper is ALIVE (shared verdict file + log fd: clobbered stamp, interleaved log — cost ~20 min)
- **`stretch-fractional-auto-bookkeeping-plan.md`** — COMPLETE (executed 2026-08-05, same day as authoring; P0→P4 single session). The stretch-panel/desktop fractional bookkeeping became FRAMEWORK-OWNED: a fill-only deferred seed at reparent (`Widget.__add` → the world's drain station) + the F6 RE-RECORD family; 33 of 41 manual `_rememberFractionalSituationInHoldingPanel()` sites deleted, the founding Widget TODO retired; found+fixed the handle-resize snap-back product bug (`HandleWdgt.mouseUpLeft` → post-flush re-record drain); TWO reflow SystemTests pin the fraction VALUES (suite 280), both proven non-vacuous.
  - ⚠⚠ the drain must FILL, never overwrite — existing bookkeeping (builder's, drop's, island-TRANSFERRED) is authoritative, and a re-derive over an integer imposition drifts fractions each round
  - ⚠⚠ the fractional imposers must stay on the polymorphic PLAIN twins (falsified both ways): extent needs `_applyExtent`'s schedule-valve second re-lay (interiors converge one pass late without it), position needs the island's anchor-carrying `_applyMoveTo` override (Bug-G) — Base twins stranded the anchor
  - ⚖ under a fill-only law, every lifecycle record (drop/duplicate/load/re-home/spawnNextTo/uncollapse/handle-release) is a RE-record of a bookkeeping-CARRYING widget and must stay explicit — the named F6 family
  - ⚖ a stable-but-wrong fraction is INVISIBLE to the census (zero movers) — only resize-reflow tests pin fraction VALUES; and a half-plant self-heals (the heal derives BOTH fields when EITHER is missing), so a non-vacuity plant must set both
  - ⚠ place-before-add when building into a stretch panel (the arrange-time heal records at the add's settle; world-side sites may place after — the drain fills them)
- **`census-as-built-extension-plan.md`** — COMPLETE (executed 2026-08-05, same day as authoring). Closes the census oracle's as-built blind spot: the truth-re-lay sweep now runs BOTH before (as-built) and after (post-resize) the resize battery, movers attributed separately, and the one known as-built defect fixed at its producer — `ScrollPanelWdgt.scrollTo` was the ONE unclamped scroll path in the class, letting the sample slide's build request commit a 27px over-scroll (map visibly short of its window edge).
  - ⚠⚠ a calibrated magic constant can enshrine a broken state as intent: the pin-at-(89,23) was MEASURED off a state itself over-scrolled by the 27px edit→view container shift — when re-deriving a constant from the live world, first ask whether the live world is right
  - ⚖ the one unclamped member of an API family is a defect factory — fix the family member, not the caller
  - ⚖ gate landed green AND proven able to fail (defect re-planted → FAILED on `asBuiltMovers` exactly; restored → green); sweep-semantics preservation MEASURED (post-resize target count byte-equal to the old single sweep)
- **`sizing-model-unification-plan.md`** — COMPLETE. Unifies Fizzygum's two sizing philosophies into ONE constraint-box model, deleting the proportional formula and last convergence residuals.
  - ⚖ Owner mid-arc: no serialization compat exists, so large behaviour changes were sanctioned
  - ⚖ Window drop-mounts content TWICE, re-arming a captured spec — fixed via remount detection
- **`softwrap-deferred-layout-conversion-plan.md`** — COMPLETE. Investigates converting soft-wrap and sibling handlers to deferred layout; concludes the whole family should stay synchronous, no code change.
  - ⚖ Path A pending-aware accessors FALSIFIED — one accessor can't serve pending and applied readers
  - ⚖ C2/C3 'unachievable' conclusions SUPERSEDED once the deferred re-queue mechanism shipped
- **`unify-layout-enqueue-primitives-plan.md`** — COMPLETE. Extracts the caret's open-coded bare layout-enqueue push into one named primitive, folded into _invalidateLayout via a state-derived branch.
  - ⚖ Purity/layering refinement, not a correctness fix — status quo was already byte-exact
  - ⚖ If lint fights back or torture finds non-convergence, leave the status quo
- **`upedge-endgame-plan.md`** — COMPLETE. Examines the 8 baseline settle re-visit flushes plus the last convergence-shaped boolean after sizing-model unification; converts 7, exposes 1 false positive.
  - ⚖ fg revisits + fg census promoted to standing gauntlet legs — empty baseline means any revisit is a regression
  - ⚖ Two falsified fix shapes on one target = STOP and document, not a third attempt
- **`window-content-negotiation-residual-plan.md`** — COMPLETE. Final proper-layouts residual: fixes window-over-stack re-visit waste; the 3 nested-window re-visits proven irreducible one-time construction costs.
  - ⚖ General 'non-freefloating content skips climb-enqueue' rule FALSIFIED — broke 9 tests
  - ⚖ Nested-window residual irreducible 3 ways: can't measure ahead, settle early, or reorder

## Transforms & geometry

- **`affine-geometry-api-plan.md`** — COMPLETE. Two-vocabulary geometry API (layout-box vs screen) for transformed widgets: TransformSpec.mapRectExact plus 5 Widget accessor methods.
  - ⚖ §1.3 — inspector 'honesty' row EXCLUDED by owner, record only
  - ⚖ §1.3 — screenQuad/inverse-maps DEFERRED, no real consumer yet
  - ⚖ §1.2 — island's internal two-faces methods stay internal, not public API
- **`drop-into-rotated-container-layout-transparency-plan.md`** — COMPLETE. Root-causes and fixes widgets dropped into a rotated/tilted container not stretching on resize (island layout-transparency).
  - ⚖ §3c content-forwarding hook DEFERRED — risked firing geometry-changing overrides, not needed for the headline fix
  - ⚖ F1 — an arrange-driven re-fit must NIL the pinned anchor, not Bug-G-normalize (locked choice)
- **`duplication-and-save-preserve-transforms-plan.md`** — COMPLETE. Root-cause + fix plan making widget duplication and per-widget save preserve affine transforms via the enclosing TransformFrameWdgt island.
  - ⚖ file status header never updated post-execution
- **`fractional-widget-bounds-investigation-plan.md`** — COMPLETE. Investigates and resolves fractional widget @bounds: rounds each arrange producer and adds a permanent NON_INTEGER_GEOMETRY hard gate.
  - ⚖ divider-drag reproportion is sub-pixel-sensitive — rounding shifts it 37-57px, not a bug
- **`widget-identity-decoupling-plan.md`** — COMPLETE. Widget-scoped true-polymorphism plan to stop Widget interrogating subclass identity; absorbed into the codebase-wide type-test-elimination-plan.md.
  - ⚖ 5c mechanical instanceof→isX?() sweep reverted — cosmetically better, not actually different
  - ⚖ Adding methods to common base classes is inspector-safe — zero recapture

## Rendering & performance

- **`shadow-reach-and-capture-point-plan.md`** — COMPLETE (both phases, 2026-08-10). Phase 1: broken-rect coverage of drop shadows made exact for EVERY shadow size/plane/transition — the painted footprint is recorded SHADOW-INCLUSIVE and the destination reach is applied PRE-map in the widget's own plane (`Widget.paintedShadowReach`); `WorldWdgt._shadowGrowFor` deleted. Phase 2: every raw-pixel read (macro screenshots + both serialization rigs) moved onto ONE end-of-cycle seam (`MacroToolkit.captureAtEndOfCycle`, drained right after `_updateBroken`); the `warmRepaintFlushPending` latch deleted.
  - ⚖ the authored §4 post-map grow was FALSIFIED by rotation: an island's rotation TURNS the shadow offset's screen direction (beyond `max(|x|,|y|)` at 35°, pointing LEFT at 90° where corner-ward growth cannot reach) — grow in the plane where the shadow PAINTS, before mapping; containment survives any affine map, so the map composes scale AND rotation with no cumulative-scale arithmetic
  - ⚖ buffer-INTERIOR staleness reproduces as `A==B≠C` — a plain full repaint re-composites the SAME kept buffer, so the diff oracle for island interiors must be the epoch-bump REBUILD, never `_fullChanged`
  - ⚖ non-vacuity plants must be SURGICAL: a whole-mechanism kill made under-paint equal under-erase (an oversized shadow added to a quiescent world was never fully painted), so the removal test passed vacuously — the proving revert is source-only; and a move fixture must move AWAY from the shadow trail or destination deposits mask the source stash
  - ⚖ Spike S2 measured the seam's one-frame sampling shift at ZERO mismatches over the full suite (event-time animation clocks + drained-queue settle); the seam POSITION is fuzz-proven load-bearing (delivered pre-`_updateBroken`, four tests capture placeholder boxes under atlas-delay injection)
  - ⚠ the arc landed the reach as a scalar (`Widget.paintedShadowReach`, corner-ward) with a recorded negative-offset caveat; a follow-up commit (2026-08-11) replaced it with the exact union — **`Widget.shadowExtendedRect(aRect)` = `(aRect ∪ aRect.translateBy(offset)).expandBy(1)`** — which covers any offset direction and is tighter on asymmetric offsets. Residual (BACKLOG): the shadow PAINTER itself (and `fullImage`, which grows bounds by the raw offset) is unexercised for negative offsets — no producer exists (→ closed by `arbitrary-direction-shadows-plan.md`, below)
- **`arbitrary-direction-shadows-plan.md`** — COMPLETE (2026-08-11). Any `ShadowInfo` offset (negative, asymmetric, mixed-sign) paints, erases and images correctly — the slide principle applied to shadows; the successor of the shadow-reach arc closing its recorded painter/`fullImage` residual. Three localized defects fixed subtractively, each probe-characterized FIRST: the shadow-pass cull translate degraded to a SCALAR by a two-argument call to the one-parameter `Rectangle.translateBy` (two sites: Widget + ClippingAtRectangularBoundsMixin → vector `offset.neg()`); `fullImage`'s corner-ward `growBy` shrinking/clipping the canvas for negative offsets (→ the shadowExtendedRect union concept, no expandBy — positive-offset images byte-identical); PopUpWdgt.addShadow's dead `color` param (deleted). Coverage: `macroShadowAnyDirectionRendersAndErases` (references dpr 1+2 + incremental-vs-full in-run assert) and `macroShadowAsymmetricInIslandRepaintsBuffer` (epoch-bump REBUILD oracle), both plant-proven. Zero churn outside the new test's own references; gauntlet 14/14.
  - ⚖ a latent mis-cull can be MASKED by margins downstream: the flesh-out lanes' corner-ward `.expandBy(1).growBy maxShadowSize` slack (+7 bottom-right, +1 top-left) fully absorbs (12,4)'s 8-px up-shift on-canvas while (−12,4)'s 16-px down-shift shows a 120-px band — an asymmetric staleness FIXTURE must pick an offset whose error exceeds the slack in its shift direction, or the plant passes vacuously (MACRO-PATTERNS "Staleness diff-oracles" rule 3); `fullImage` has no such margin, so probe-level checks there expose even the absorbed offsets
  - ⚖ a prediction that fails to reproduce gets ROOT-CAUSED, not dismissed: the (12,4) non-reproduction was reconstructed byte-exactly (flush rect [398,208,570,298] = shadowExtendedRect.expandBy(1).growBy(6)) before any fixture was adjusted
  - ⚖ CoffeeScript passes extra call arguments SILENTLY: `rect.translateBy -x, -y` against a one-parameter signature drops the second argument with no error — masked for YEARS because every product offset is symmetric (scalar == vector when x == y); when an API takes a Point-or-scalar `factor`, pass the Point

- **`fullchanged-trim-and-precision-plan.md`** — COMPLETE (2026-08-11, all six findings P0–P6). Executes the `_fullChanged` usage audit under the owner mandate "trim and precise, never a hammer": P1 unified FOUR appear/disappear verbs onto `_fullChangedIncludingShadowOwner` (90-px stale-band repro → exact-0, plant-proven test, suite 290); P2 deleted the two redundant trailing pop-up/tooltip calls; P3 guarded `bringToForeground` (92% of 3,173 raises were already-frontmost; the plan-missed clause: `isLastChild` is FALSE for a parentless node, so every empty-desktop click was full-repainting the ENTIRE canvas); P4 swapped three test oracles to the public reset verb, refused one with reasons at the site; P5 replaced the atlas-warm whole-world reset with per-draw cold-glyph attribution (fillText status chokepoint; poisoned-cache-key eviction; no epoch bump) + fixed the upstream SWCanvas transformed-text status swallow; P6 closed collect-union as NOT PURSUED on its own opening measurement and deleted the hand's no-op suppress window (−99.4% of all coalescing windows), flush instrument neutral, byte-identity everywhere.
  - ⭐⭐ **a frequency instrument's blind spot can MANUFACTURE a GO**: P0b's "75.1% of windows change <25% of the box" was 99.985% the hand's per-move window, whose carried composite moves via commit-tier `__commitMoveBy` — invisible to the deliberately-`_apply*`-only counter. The mandated per-owner-CLASS breakdown, run as the phase's OPENING step, dissolved the aggregate in one table: the true population was 27 windows/run. Bucket an aggregate by owner before building machinery on it
  - ⚖ a rigid whole-composite translation has NOTHING for a collect-union to improve — its single mark already yields the exact old∪new footprint via the two flesh-out lanes; precision machinery can only help when children move NON-rigidly inside a box that itself did not change, which the breakdown showed is rare (27/run) and small
  - ⚖ a suppress window that suppresses exactly the one mark its own trailing call reissues is a NO-OP by construction — probe both directions (mark counting AND stock-vs-patched byte-identity incl. ground truth) before deleting, then delete the whole override, not just the window
  - ⚠ the F3 site inventory drifted from the audit in ONE day's arcs: 28 windows (23 exact-idiom, 3 `_reLayout` with no trailing call, 1 build-time, + the hand) — re-grep before acting on any inventory, and note the no-trailing-call variants lean on the pre-window owner-bounds mark
  - ⚖ `_updateBroken`'s stack sanity assert uses `and` so it can effectively never fire; tightening it to `or` would alert-spam the serialization rig's DELIBERATE unbalance scenario — an assert can be wrong in a direction that makes fixing it wrong too (→ resolved by `repaint-as-one-unit-plan.md`, below)

- **`repaint-as-one-unit-plan.md`** — COMPLETE (2026-08-11, authored + executed same day; the successor closing the fullchanged-trim arc's two hands-off residuals). The paired `disableTrackChanges`/`maybeEnableTrackChanges` suppression idiom — pairing and the covering repaint both CALLER obligations — dissolved into ONE private self-covering block `Widget._repaintAsOneUnit fn` (depth-restore AND owner cover in `finally`), converted at all 26 sites (healing the three no-cover `_reLayout`s: Bin/ErrorsLogViewer/FridgeMagnets, whose safety rested on an unstated pure-function-of-frame invariant); the boolean stack (`trackChanges: [true]`, nothing ever pushed `true`) became `world._damageSuppressionDepth`; the De-Morgan-dead flush assert became a LIVE self-healing `DAMAGE_SUPPRESSION_UNBALANCED` console token wired into both headless runners' fail-gates; INV-1's gate reduced to a retired-verbs tombstone (its invariant is structural now); the serialization rig corrupts the raw field in the same JS turn as the load. Gauntlet 14/14 all-in-wave + fg homepage + four probes green.
  - ⭐ **make invalid states unrepresentable, then guard the remainder**: the `finally` block kills both caller obligations (a leaked frame, a forgotten cover — each had shipped real bugs); what stays representable (raw field tampering) gets the fail-gated token + SELF-HEAL, so a live world degrades loudly-but-visibly instead of silently going dark. The plant proved the whole wire: 8 corrupted shards, `failed: 0` screenshots (healed before pixels suffered), runner still EXITS 1
  - ⚖ moving the verbs off the world onto Widget as a PRIVATE method makes invalidation privacy structural — the construct can only coalesce into `self`, the rule the receiver gate polices

- **`appearance-local-coords-plan.md`** — COMPLETE (2026-08-12, Tiers 1–4; the coordinate CONVERSION byte-identical — zero reference changes; ONE benign inspector recapture from Tier 4's dead-method deletion, see below). Affine plan §7.7: every appearance paint body now draws its widget's OWN pixels in widget-local LOGICAL coordinates through the ctx matrix, inside the ONE preamble `Appearance._paintInLocalScope` (three coexisting preamble spellings dissolved; the un-harvestable fine-tail preamble duplication resolved at the design level). The absolute trio (Rectangular/LabelButton/Desktop) converted; the stroke landed as §4.4(A) — 1 DEVICE px spelled `1/ceilPixelRatio` (dyadic ⇒ exact; the (B) logical-thickness flip stays an open TODO); dead `_drawHighlightOverlay` deleted everywhere — which shortened the inspector's inherited-member list and re-scrolled `macroDuplicatedInspectorDrivesCopiedTargetOnly`'s member-list window: the one recaptured test, under the standing owner grant for benign inspector churn (root-caused at the pixel via `fg diffpage` first). Law: `docs/architecture/appearance-paint-convention.md`.
- **`menu-subject-routing-plan.md`** — COMPLETE (2026-08-16). The successor to the menu-action arc, taking its two deliberately-deferred findings — both the same question: *a verb wired to a menu needs to know which widget it is about, and the dispatcher does not reliably tell it.* **P1** (live: "dev tools ➜ > inspect" and "> console" did nothing) — `DemoMenus.popUpSecondMenu` dropped the widget its sibling forwards, so the Dev Tools menu it opened was built about `demoMenus`; fixed by building that menu `target: widgetThisMenuIsAbout`, which makes it structurally identical to the site that already worked (Widget's own "dev ➜"). Its acceptance test was the deletion of the two `UNRESOLVED_ACTION` entries from the sweep's `KNOWN` map, with the sweep still green. **P2** (LATENT, no behaviour change — and the commit says so) — `enable`/`disableDragsDropsAndEditingFromMenu` adapters on `Widget`, so the cores' `if !triggeringWidget? then triggeringWidget = @` fallback fires instead of the menu path permanently handing it a `MenuItemWdgt`. ONE benign inspector recapture (two added prototype members lengthen the member list). Law: `docs/architecture/constructor-and-parameter-conventions.md` R3.
  - ⭐⭐ **The dispatcher fact is CONDITIONAL, and half of it is the trap**: slot 1 = the `MenuItemWdgt` and slot 2 = the ENCLOSING PANEL's target **only when the panel carries no `environment`**; with one, slot 1 is the panel target and slot 2 is that environment. So a verb taking its SUBJECT from slot 2 is right exactly when the menu it sits in was built ABOUT that subject — and a row wired to a different RECEIVER does not opt out, because the receiver comes from the row while both leading arguments still come from the panel.
  - ⭐⭐ **A shrinking coverage number is a claim to be tested, not accepted.** The sweep's DISTINCT count fell 519 → 515 on a change that fixed a bug and lost no reach; a full-revert A/B + set diff showed the entire delta is ONE action — `Widget._attachToChosenParent` builds one row PER WIDGET CURRENTLY IN THE WORLD, so those pairs track the world's population, and items that now actually RUN change it. `menus walked` (324, unchanged) is the stable reach number; DISTINCT is breadth. The rig now says so and `--verbose` prints the set.
  - ⭐ **The static gate has TWO blind spots, not one** — the A/B demonstrated it fires on this bug shape once the signature DECLARES the parameter, which means a verb that needs a subject and never declares one is invisible to it. Nothing is unread, so nothing is flagged.
  - ⚖ `fg classify` called the recapture REVIEW, not BENIGN?: it cannot recognise a whole list SCROLLED by N rows, the commonest benign inspector shape. Reading the pixels settled it (both lists shift one row; `alpha = 0.25` left / `0.6` right — the copied-target assertion intact).

- **`menu-action-wiring-plan.md`** — COMPLETE (2026-08-16, authored + executed same day). The successor to the conformance arc, which closed noting that ONE menu item had shaped six `createReference` signatures; this asked how many OTHER verbs are wired straight at a menu and found **six live user-facing bugs** — four by reading, two more from the rig built to close its own residual. `ButtonWdgt` dispatches every action through a FIXED four-slot convention (`@target[@action].call @target, menuItem, panelTarget, arg1, arg2`) that the call site never names. Gate: `buildSystem/check-menu-actions.js` (on the build) — rules 1–2 HARD/sound (function literal in the action slot; string where `opts` goes), rule 3 a ratchet at 0 (an unread menu-action parameter must be NAMED `ignored`/`unused`). Proven to FAIL on planted violations of both classes, not merely to pass.
  - ⭐ **B1**: `SliderWdgt`'s "floor…"/"ceiling…"/"button size…" passed FUNCTION LITERALS as actions — `@target[@action]` coerces a function to a string key, so all three threw on every click, behind a runtime tripwire nothing ever tripped because the suite never clicks them. Now named methods reading the title from slot 1 (`transparencyPopout`'s idiom); `menuItem.parent.title` probe-verified identical to the captured `menu.title` FIRST, because that string is drawn.
  - ⭐⭐ **B2**: the three demo "…launcher" items CRASHED — `createOpener: (inWhichFolder)` received a `MenuItemWdgt` (slot 1) and ran `inWhichFolder.contents.contents.add`. A/B-proven on the real menu (old: 3× `Cannot read properties of undefined (reading 'contents')`, 0 launchers; new: 3 clean clicks, 3 launchers). **Fixed by DELETING the parameter, not by adding an adapter** — the in-folder arm had no caller in either repo and duplicated `IconicDesktopSystemWindowedAppLauncherWdgt.addToFolder`. ⭐ A verb that takes NOTHING cannot be mis-fed by the dispatcher: reach for that before an adapter.
  - ⭐⭐ **B3/B4**: thirteen verbs padded with unread slots. `showOutputPins: (a,b,c,d) -> world.pinouts?.show b` — the padding was **routing the RECEIVER back in as an argument** (slot 2 IS `@`), and is what forced the one macro caller to write `w.showOutputPins undefined, w`, i.e. two of the tests repo's argument holes.
  - ⚠ **the asymmetry that is the gate's rule 3**: a TRAILING unread slot can be dropped (extra dispatcher args fall away); a LEADING one cannot — dropping `a` from `(a, targetWidget)` slides `targetWidget` into slot 1 and silently retargets the menu, so it stays and is NAMED.
  - ⚠ **RESIDUAL (BACKLOG)**: rule 3 would NOT have caught B2 — `inWhichFolder` was read, just read as the wrong THING, which no text scan can see. The mechanism for that class is a rig that CLICKS every menu item; the demo menus are exercised by nothing today, which is exactly where both crash-class bugs lived.
  - ⭐⭐ **THE RESIDUAL IS CLOSED, and closing it found two more bugs.** `menu-click-sweep-headless.js` (`fg menusweep`, gauntlet wave-A leg) fires every reachable menu action and fails on a throw — **519 distinct (receiver class, action) pairs**, the number that means something, since "menus walked" is inflated by the same demo tree hanging off every context menu. Proven to FAIL by re-planting B2. It found: **B5** every widget wearing a `BoxyAppearance` threw when its numerical-setters menu was built (the appearance called `deduplicateSettersAndSortByMenuEntryString` — a **Widget** method — on itself, redundantly, since its only caller dedupes the result), and **B6** `cornerRadiusPopout` read `@widget.cornerRadius` raw, a field only `BoxWdgt` declares, so it threw for a frame or folder window (now via `getCornerRadius()`, which already owned the absent case).
  - ⚠⚠ **A RIG THAT MUTATES THE WORLD MANUFACTURES ITS OWN BUGS.** `make pointer` looked like a third find and did NOT throw when re-run in isolation: an earlier item in the same walk had detached the receiver. The rig now re-attaches a detached-but-alive receiver — skipping instead cost 73% of its reach, because the submenus that row would have opened go unwalked too — and skips a destroyed one out loud. **Re-run a sweep finding in isolation before believing it.**
  - ⚠ It dispatches the action DIRECTLY rather than calling `mouseClickLeft` (a real click tears the menu down under the walk), so it covers DISPATCH — where both bug classes live — and not click plumbing, which the suite covers.
- **`widget-practices-convergence-plan.md`** — CLOSED (W0–W10, 2026-08-16→17), with two RESIDUAL items named in its status box and in `BACKLOG.md`: **W6b** idempotence guards (owner-gated D3, and to be re-derived against the connector arc's §P2 rather than executed as written) and **`representativeIcon`** (D4 — unlike a colloquial name, an icon cannot be derived from a class name). Acted on `measurements/widget-practices-survey-2026-08-14.md`, which now carries an appended **"state at close"** delta table; the mechanical half is re-runnable as `buildSystem/census-widget-conformance.js`. Landed: undeclared instance fields 9 classes/11 fields → **0/0** (a true zero, finished off by the connector arc's P9), `_reLayout` prologue copies 24 → **8** stated exceptions, W9's three enforcement tiers, and W6a — which was a LIVE BUG (three `SliderWdgt` prompts stored the slider's own value instead of the typed one).
  - ⭐⭐⭐ **W7 IS THE ONE TO REMEMBER: the plan asked for 164 hand-written `colloquialName`s and the answer was ONE derivation.** The tree already contained `@constructor.name`-based naming that a truthy `"generic widget"` default was SUPPRESSING — every un-overriding widget was literally labelled "generic widget" in window titles and saved-file names. ⇒ **before sizing a gap by counting missing declarations, check whether a RULE for the thing already exists and is being shadowed.** It also INVERTED the census facet: not declaring a name is now correct, so the raw count went UP as redundant overrides were deleted.
  - ⭐⭐⭐ **For any INHERITED member the question is never "what does this class say?" but "what does the CHAIN say, and what would it say WITHOUT me?"** — the chain-resolving survey caught 4 deletions that eyeballing would have broken (3 in W7, 1 more in the capitalisation pass, where `GenericPanelWdgt` extends `FrameWdgt` and would have answered "window").
  - ⭐⭐ A cosmetic normalisation can turn a "deliberate" override into a redundant one: lowercasing made 8 of 15 identical to the derivation. **Re-run the resolver AFTER the edit, not before.**
- **`constructor-parameter-conformance-plan.md`** — COMPLETE (P0–P9, 2026-08-15→16). Brought `src/` onto the head/tail convention (`architecture/constructor-and-parameter-conventions.md`): a positional head for IDENTITY capped at 4, one trailing `opts = {}` for CONFIGURATION, the **hole test** decisive. `positional-hole` 51 → **0 (HARD)**; the honest counter the arc had to BUILD when that stink proved blind — `buildSystem/check-argument-holes.js`, sharing `census-call-arity.js`'s paren-aware parser — 25 → 9 → **2**, which is its FLOOR (both survivors are `undefined`-as-a-VALUE, annotated at their call sites). Families: `SliderWdgt`, `MenuItemSpec`, the text trio, the button family, the prompt family, the stragglers, the METHOD families, the polymorphic `add` (7 overrides of arity 4/5/6 → one `(aWdgt, opts)`), and P8/P9's tail — `Appearance._paintInLocalScope`, the `createReference` family, `cleanupMenuWdgts`, `fullImage`, `_insertAddersSuchThat`, `Class.findUpTo`.
  - ⚠⚠ **ARCHIVED AS COMPLETE ONCE BEFORE AND RE-OPENED THE SAME DAY** — `positional-hole` read 0 while ~50 holes stood, because its regex needs two `undefined`s ADJACENT ON ONE LINE. Hence the arc's motto: **a green ratchet is a FLOOR, never an inventory** — ask the scanner, never the gate. This archiving is backed by an accounting of every surviving hole site in both repos.
  - ⭐⭐⭐ **the HOLE-vs-VALUE test** (now R3): a `undefined` is a HOLE iff some OTHER call site of the same callee OMITS that position's trailing tail. A parameter's own documentation cannot settle it — `_insertAddersSuchThat`'s axis is documented as "MEANINGFUL" and is a hole all the same, because the other caller stops one argument short.
  - ⭐⭐⭐ **ONE MENU ITEM SHAPED SIX SIGNATURES.** Two flagged holes in `_createReferenceAndCloseNoSettle` were really a six-method family in which all three implementations carried the same `typeof(referenceName) != "string"` sniff — triplicated because **a bare `super` compiles to `.apply(this, arguments)`**, forwarding RAW args rather than the parameters the sniff just rebound. Splitting a `createReferenceFromMenu` adapter off the verb deleted all three and freed the order. The adapter and the reorder are NOT separable. Sweep by the FAMILY name, not the method the count flagged.
  - ⭐⭐ **"I checked the callers" is HALF a sweep** — a signature is owned by its call sites AND its `super` sites; the arc broke the Drawings Maker TWICE this way (P3, then P8 in the same class family, with a descendant closure of ONE).
  - ⭐⭐ **a HOLE is a SYMPTOM**: underneath them sat a dead-and-broken callback, a slider silently never placed (`Point` → `Number` → NaN → `splice` index 0, fixed by renaming the key `atIndex`), a dead parameter appearing only in its own signature, a dead field, and a runtime type-test triplicated across a class and both overrides.
  - ⚖ open by decision, in `BACKLOG.md`: the sibling tests repo stays UNGATED (a third of its "holes" are English prose in metadata strings — a gate there would fail builds over doc edits), and `Widget.textPrompt` keeps a `msg` operand its only receiver has no title bar to show.
- **`stroke-flip-and-fracplane-coverage-plan.md`** — COMPLETE (2026-08-12, same day as authored; the local-coords arc's three owner-commissioned residuals). P1: rectangular border AND teal editor-selection overlay flipped 1 DEVICE px → 1 LOGICAL px (the §4.4(B) decision; raw Boxy spelling; 139-test dpr2-ONLY recapture, dpr1 byte-invariance verified); P3: `SystemTest_macroDropStrokedRectIntoRotatedPanel` pins stroke × fractional figure position; P2: the umbrella-local `fracplane` presuite rider (dpr2 run-sequence over the two rotated-container tests; plant-proven dpr1:PASS + fracplane:FAIL).
  - ⚖ after a MASS recapture, sweep EVERY old-vs-new ref with a color-pair inventory — the expected change has a signature (one-way bg→border = thickening) and the sweep found the one corrupted capture among 454 changed images; a diffpage spot-check would have missed it
  - ⚖ a recapture gate's COMPLETE certifies suite-vs-refs AGREEMENT, not ref CORRECTNESS — a bistable render can match its own corruption; the dpr1-invariance oracle is what caught it
  - ⛔ do NOT re-attempt device-grid snapping inside `paintStroke` for fractional-figure solidity: a widget's own plane position is integer by the placement law (snap = provable identity, captured byte-identical and reverted); the fractional offset + rotation live in the CTM, which a law-compliant body never reads. The dashed thin strokes inside compensating wrappers are a pre-existing rasterization-class limit (old hairline border included, selection overlay included) — BACKLOG, owner-gated
  - ⚠ a hand-carried window's pixels are not refreshed when a pending atlas arrives mid-drag (pre-existing; the deterministic face of the open flake-A class; BACKLOG) — it corrupted `macroDragEmbedWindowTransitNeverArms` image_0 at BOTH densities during the mass recapture; fixed by settling text BEFORE `pickUp()` in that macro (the screenshot verb's own wait cannot help — the corruption predates the read)
  - ⚖ SWCanvas fast paths gate on `isAxisAligned`/`isUniformScale`, NEVER `isIdentity` — the feared "legacy identity fast path to keep" did not exist; dispatch authority = the SWCanvas repo's `DIRECT-RENDERING-SUMMARY.MD` §3, not older summaries
- **`swcanvas-scale-path-smoothing-plan.md`** — COMPLETE (2026-08-13, the day after authoring; owner gates D2a AMENDED/D2b/D2c; closes affine §7.8's scale half — both halves now done). SWCanvas `drawImage` smooths whenever the EFFECTIVE SAMPLE STEP ≠ 1 (CTM scale × rect ratio composed; step-1 blits — glyphs, back buffers — byte-exact NN by construction) and `imageSmoothingEnabled` is implemented per HTML5 (default true, both API layers, save/restore; SWCanvas 058 pins step-1 byte-equality + the opt-out + seam-free clipped composition). Owner D2a: INTEGER-scale islands stay CRISP — a Fizzygum-layer opt-out (`_compositeScaleOnly` sets the flag false at integer s, both backends agree), which shrank the re-baseline to ONE test; 90°-multiple extension BACKLOG'd behind quadrant-exact `_cosSin` (trig residues make NN floor-pick wrong texels). T-scale pin `SystemTest_macroScaledStrokedRectSmooth` (1.5× smooth / 2× crisp); law in `architecture/transforms.md` §8-§9.
  - ⚖ S1 falsified the plan's own composite model: per-strip src sub-rects under bilinear = a half-texel PHASE SHIFT per damage strip + tap starvation at seams (incremental ≠ full, 708/1420 px on the Oversized numeric A/B) — the scale composite now draws the WHOLE buffer through ONE mapping under a rect damage clip, the rotation path's shape (strips byte-identical BY CONSTRUCTION, same cost via the Tier-0 clip)
  - ⚖ S2 falsified the compounding worry: N StretchableCanvas resizes ≡ 1 resize byte-exactly (it re-blits the PRISTINE original every refresh) — no opt-out needed
  - ⚠⚠ `npm run build` in SWCanvas does NOT refresh `dist/swcanvas.min.js`, and the boot bundle embeds the MIN bundle — the first S1 suite ran the OLD sampler and returned an all-green PHANTOM; caught only because green contradicted the sampling math. `npm run minify` before EVERY vendor, and treat an implausible green as an injection-not-live signal
  - ⚠ parallel suite logs interleave per-line FAIL output — trust ONLY the `failed tests (N): [...]` summary line; and a parallel leg can PHANTOM-PASS a test whose macro errors before its screenshots (zero-failed-screenshots gotcha) — re-verify any surprising pass single-process
  - ⚠ `fg recapture --auto` classified a genuinely pixel-stale test as "needs-a-fix (failureImages=0)" while a single run dumped a visible failing image — when the classifier and a direct run disagree, the direct run wins; per-test `capture-macro-test-references.js` + a re-gate closed it
  - ⚖ engine defaults follow the STANDARD; per-product looks are product policy — the integer-crisp ask landed as Fizzygum setting the standard flag, never as a SWCanvas default carve-out
- **`swcanvas-bilinear-rotated-composite-plan.md`** — COMPLETE (2026-08-12, same day as authored; D1 owner YES; discharges the stroke-flip arc's BACKLOG'd thin-stroke-dashing item + affine §7.8's rotation half). SWCanvas's transformed `drawImage` samples BILINEAR on non-axis-aligned transforms (SWCanvas `619dc1c`: premultiplied, dest-pixel-center, zero-fraction pure-texel fast path; axis-aligned blits byte-identical NN) — thin features (1-logical-px borders, selection-overlay rings, rotated glyphs) can no longer dash at the island warp, once- or twice-resampled. 37-test rotated-only re-baseline at BOTH dprs; T2 `SystemTest_macroRotatedStrokedRectSingleComposite` = the single-composite pin; law in `architecture/transforms.md` §8.
  - ⚖ §4.1(3)'s floor-convention spelling ("zero fraction = NN result", fraction from the floor texel) was FALSIFIED in-spike: it keeps the gaps and shifts content −0.5px per resample (a wrapper would drift a full px); the correct shape is DEST-PIXEL-CENTER sampling with texel centers at +0.5 (native semantics), whose zero-fraction case (texel-center landings) keeps exact-90° composites crisp
  - ⚖ pixel INCLUSION and sampling must share a convention per path — corner containment + center sampling drops an edge row/column at 90° (caught by SWCanvas `tests/core/057`'s crisp-90 case)
  - ⚠ a probe walking a thin feature must index device pixels by floor(p), not round(p) — round() masked the double-resample repro entirely (0 gaps read where 29/29 centerline pixels were gone)
  - ⚖ in a SAMPLING-upgrade recapture, one-way color pairs are the EXPECTED sweep signature (staircase retreat / fringes appearing / dropped features returning) — even bg→text-color can be legit (rotated glyphs regaining NN-dropped pixels); triage by WHAT the pair is and WHERE, verified visually, not by one-wayness
  - ⚠ a capture step can silently write ONE density and report success — `macroTransformFrameSlotTracksContentResize`'s dpr1 refs were simply absent; only the recapture completeness gate's suite re-run caught it
  - ⚖ the teal selection overlay is drawn exactly ON a 1-logical-px border ring and fully hides the stroke — a stroke-continuity test must DESELECT first (MACRO-PATTERNS, affine section)
  - ⚖ `benchmark-session.js` has NO drawImage case — it is blind to compositor changes by construction; measure with `debug/bench-rotated-drawimage.js` (rotated ~2.4× = the 4-tap cost, rotation path only; axis-aligned unchanged)
  - ⛔ body-side fixes (device-grid snap / thicker strokes / AA stroke rasterization) remain falsified — §9's do-not-re-attempt list stands, with the byte-identity evidence in the predecessor arc
  - ⚖ byte-identity under an integer translate + dpr scale is PROVABLE for integer/dyadic geometry and was proven empirically first (Phase 0 scratch-convert + `cmp` at both dprs) — non-dyadic arithmetic is the C1 association-order hazard (`integer-pixel-placement-and-sizing.md`)
  - ⚖ a `CanvasPattern` anchors to the coordinate space: under the scope's CTM the tile SCALES with the dpr and its phase follows the translate — the desktop wallpaper fill stays device-space by declaration, verified by a dedicated A/B probe because wallpaper has ZERO suite coverage
  - ⚠ `Widget.backgroundColor` defaults to NIL and `paintRectangle` silently skipped nil colours — inlining a fill must reproduce the guard or the plots/clock crash (their "background fills" had been no-ops for years)
  - ⚠⚠ **an ADDED damage clip is NOT pixel-free**: plane geometry IS legitimately fractional in one place (a rotated-container payload's reparent-transparency figure — fractional position, fractional island-slot origin), and there a rect-clip's boundary quantization differs from the fills' under the buffer's fractional ambient translate, shaving an edge column (dpr2-only; found by the close gauntlet, tier-bisected, then pinned by wrapping SWCanvas fillRect + the appearance paint in probe preludes). The legacy Rectangular paint never clipped ⇒ the converted one may not either: `opts.clip: false` + self-bounding draws
  - ⚖ fills that legacy code routed through `Widget.paintRectangle` carry its `Math.round` device-grid quantization as a CONTRACT — `_fillLocalRectSnappedToDevicePixels` reproduces it through the CTM (identity for integer geometry); raw fractional fills would re-quantize at the rasterizer
  - ⚖ the dpr1 suite is BLIND to this whole class (the fractional case rounds identically at cpr 1) — fractional-plane scenes need the dpr2 leg, i.e. the gauntlet, before a tier can be called byte-identical
  - ⛔ do not convert the SizeAwareIconAppearance family (dpr-conditional integer-device pixel art by documented design — the law's exception clause), and do not re-attempt logical-space pattern fills
  - ⚖ a prototype METHOD addition is not pixel-free: the inspector's inherited-members list gains a row, and any test that reaches a row via scrollbar-drag quantization shifts its list scroll (`macroDuplicatedInspectorDrivesCopiedTargetOnly`, the kept-spec-arc precedent; the test's own ⚠ comment names the class) — root-cause to the census, verify the diff bbox is confined to the list pane, then recapture
  - ⚖ the build gates DROVE the staging: dead-methods rejects an uncalled construct (so sites convert immediately), then flags the migration bridges the moment the last site converts (so the verbs die immediately) — a gate that forces the right order is a feature, not friction
  - ⚖ probe case law: an assertion over the pending-mark arrays must account for leftover PAINT-TIME marks (text-cache re-renders land after the flush clears) and for the fact that a re-lay at UNCHANGED bounds attempts ZERO marks — assert attempts-suppressed vs recorded-live at CHANGED bounds, not "arrays empty"
  - ⚠ follow-up sweep (same day): a survey for other members of the class found the block+finally shape already standard (`_deferredSettleDeclare`, `_settleLayoutsAfter`/`_inLayoutMutation`, `recalculateLayouts`) and THREE residual bare restore-obligation windows, fixed with `finally`/iterate-a-copy: TransformFrameWdgt's island-buffer save/restore (a throwing island child left `world.paintingIntoIslandBuffer` stuck → spurious buffer-dirty deposits on every later paint, nothing healed it), InspectorWdgt's reverse-then-restore of `@classesButtons`, `healingRectanglesPhase`'s bare clear
  - ⭐ follow-up (2026-08-12, owner-directed): **the cover is now SKIPPED when provably vacuous.** `world._suppressedMarkAttempts` (MONOTONIC) increments in both suppressed early-return branches of `_changed`/`_fullChanged`; the construct snapshots it at open and fires the cover in `finally` only if it advanced or fn threw. This deliberately SUPERSEDES, with new numbers and by owner direction, the fullchanged-trim P6 ledger's hands-off note on the ~683 zero-change windows — and is NOT the falsified P6 collector (no rects preserved; the cover is merely skipped when nothing attempted a mark). Nesting sound by construction (an inner cover fired-SUPPRESSED itself counts as an attempt for the outer unit); pre-land safety grep: zero `__commit*` inside all 26 unit bodies (a commit-tier write moves geometry with NO mark, which would defeat the skip — ListWdgt's `__commitMoveTo` sits AFTER its unit); counter exempted by name in the resetWorld ratchet (monotonic group); rig hygiene key renamed `trackChangesRebalanced` → `damageSuppressionRezeroed`. Gates: probes (1b FLIPPED — cover absent from the pending-full set at identical-bounds re-lay, present at grown; 1c empty unit → no cover; exception path still covers), presuite paint 0/290 offenders, gauntlet 14/14, fg homepage; ZERO reference churn (a world FIELD adds no prototype member → no census shift, unlike the arc's method addition)
  - ⚖ the measured payoff is ~NIL and that is the finding: the skip fires 426×/suite-run (29% of 1,473 outermost windows) yet the flush aggregate is unchanged within noise (rects 45.2k → 45.2k, area −0.36%, wall-clock flat) — the eliminated covers were almost always flag-deduped into an existing same-cycle mark of the same owner, CONFIRMING the P6 "orders of magnitude below mechanism cost" assessment; this mechanism is bought for one counter increment on suppressed paths. And 254 of the ~680 bounds-idle windows carry suppressed CONTENT marks (`_changed` with no bounds motion) and correctly STILL cover — the attempt-counter skip condition is strictly safer than the instrument's bounds-identity classification, which had overstated the waste
  - ⚠ harness finally-sweep (2026-08-12; the 2026-08-11 sweep covered `src/` only): TWO real instances in `Automator-and-test-harness-src/` fixed — AutomatorPlayer's `screenshotHashPendingCount` decrement moved onto the promise chain's OWN `finally` + a guarded launch (a rejected digest or sync throw left the count stuck → `screenshotHashSettled()` false forever → the `waitForScreenshotHash` pump gate never opens → shard STALL instead of a loud fail), and AutomatorLoader's `grabDragThreshold` write to the STATIC preferences bag (survives world teardown; outside the resetWorld fingerprint) now captures the boot default once and applies-or-restores per test (dormant leak — no test declares it today — but the DETERMINISM §2d class exactly)

- **`direct-shape-fastpaths-followups-plan.md`** — COMPLETE (authored 2026-08-07, executed 2026-08-08; P6 added in execution). Finishes what the Boxy→roundRect conversion started: sliders/scrollbars fill ONE `fillStadium` (new SWCanvas `StadiumOps` primitive, tier-0 from birth), title-bar rings + the default icon ring stroke ONE mid-radius `strokeCircle`, and the mechanically-safe icon path-rects/circles go direct (`fillRect`/`strokeRect`/`fillCircle`); upstream, CircleOps got tier-0 clip wiring + crisp-contract tests (050–052), the fused `fillStroke_AA_Any` got tier-0 + a radius<1 fill clip-drop fix, and ALL six circle/arc entries gate on `isUniformScale` (non-uniform → generic pipeline → correct ellipse, test 054). 133-test re-baseline at dpr 1+2; closing gauntlet 14/14 incl. WebKit.
  - ⚡ the founding premise ("relax CircleOps' transform condition") was FALSE — a stale doc table; circles already fast-pathed under Fizzygum's transforms. The real gaps were tier-0 and two latent corruption bugs (`fillStroke_Any` off-surface span wrap; the fused fallback's unclipped fill)
  - ⭐ stadium ≠ `fillRoundRect(r=min/2)`: RoundedRectOpsAA's edge-sampled corner x-extents lose a HORIZONTAL stadium's apex columns (masked on normal rects by their straight vertical edges) — measured, rejected, `StadiumOps` built on `CircleOps.generateExtents` so caps ≡ `fillCircle` pixels by construction
  - ⚠⚠ the SWCanvas vendor script requires `npm run build:prod` FIRST — a plain `npm run build` leaves `dist/swcanvas.min.js` stale, and the entry pages load the MIN: symptom was `fillStadium is not a function` per repaint → main-thread wedge → the suite crawling at ~25 s/test with healthy-looking shards
  - ⚠⚠ a shared paint helper's STYLE ASSIGNMENTS are contract: `_paintButtonRing`'s old `fillStyle` set was load-bearing for the Collapse/Uncollapse dashes (they `fill()` without setting their own → context-default BLACK); 54 of 128 failing tests were that one defect, caught by the owner's diff-page eyeball
  - ⚠⚠ hairline (sub-1px) strokes must stay on the path pipeline: `lineWidth 0.5` is below `STROKE_1PX_TOLERANCE`, so a direct `strokeCircle` re-routes through a device-space fallback that LOSES the ring inside a scaled island (rotate-handle knob reverted; the four-swirlies glyph redesign in BACKLOG is the real fix) — **SUPERSEDED 2026-08-09 by `hairline-direct-strokes-plan.md` below: the dispatchers now implement the sub-pixel rule and the ring IS a `strokeCircle`**
  - ⚖ SWCanvas's direct `strokeRect` ignores `lineJoin` — round-join stroke rects stay paths, else corners square on one backend only
  - ⭐ recapture forensics: ~97% of the ~1M changed pixels were the ±1 single-channel blend-rounding shift of the direct alpha path (whole shadow regions); color-transition histograms separate that class from real change in seconds, and a one-sided gain/loss balance is the something-stopped-rendering detector

- **`hairline-direct-strokes-plan.md`** — COMPLETE (authored 2026-08-08, executed 2026-08-09). Eliminates the hairline (sub-1px-stroke) inconsistency across SWCanvas's direct stroke dispatchers by restating the generic pipeline's ONE sub-pixel rule — 1px geometry at opacity proportional to the true DEVICE width — as a first-class branch in all five entries (rect/roundRect/circle/arc/line, rotated branches included), then re-converts the one consumer this unlocks: the rotate-handle knob ring back to `strokeCircle` (the prior arc's revert). SWCanvas `d6e6765` (+ core test 055, probe `debug/probe-hairline-strokes.js`); Fizzygum pin bump + ring + integer-pixel doc §7; 16-test re-baseline dpr 1+2; gauntlet 14/14 incl. WebKit.
  - ⚡ keying on the DEVICE width is what makes the rule transform-robust AND what re-weights dpr 2: a 0.5-logical stroke there is a TRUE 1px stroke — the ring sharpened from a 2-device-px half-opacity generic band to a crisp opaque Bresenham ring (the generic pipeline keys on the LOGICAL width; the two agree at identity and diverge under scale by construction)
  - ⚡ threshold continuity beats generic-agreement: a hairline paints EXACTLY the exact-1px stroke's pixel set, only fainter (test 055's vanish-catcher oracle); agreeing with the generic path's POSITION was never available — direct and generic already rasterize lw=1 differently (188px of 96 for a rect at identity), shipped behaviour of every lw≥1 caller
  - ⭐ the rule surfaced two latent renderer inconsistencies, both fixed WITH it: `ArcOps.stroke1px_Opaq` walked a DIFFERENT Bresenham variant than CircleOps/its own alpha twin (a partial arc changed shape with its opacity, 16 vs 19 px at r=11 — the twin invariant "geometry must not depend on opacity" is now pinned for all five families), and `RectOpsRot`'s 1px DDA double-blended shared corner pixels under translucency (seen-set dedup; lastPos is insufficient there because edges iterate min→max, not p1→p2)
  - ⚖ terminology (owner): it is the HAIRLINE FAINTNESS RULE, not anti-aliasing — both backends source-over-blend the faint stroke with what lies beneath (probe-measured on Chrome: 0.5px white over red reads pink); SWCanvas stays non-AA, one uniform level per stroke
  - ⚖ suite blast radius split cleanly by density: 9 ring tests changed at BOTH dprs, 7 uncollapse-icon tests at dpr 1 ONLY (their sub-1px strokeRect reaches 1 device px at dpr 2 — the device-width keying made the dpr-2 legs byte-identical by construction)
  - ⚠ a diffpage dump group can exit non-zero with ZERO images (a transient that fails before the screenshot compare) and masquerade as "no diff" — audit dumped-group counts against the expected failing set before trusting the page

- **`resetworld-teardown-completeness-audit-plan.md`** — COMPLETE (2026-07-29). Audits `WorldWdgt._resetWorldNoSettle` for completeness after two field-caught leaks of the same shape in three weeks (`widgetsToBeHighlighted` 2026-07-10, `UntitledNamingService` counters 2026-07-28). 26 rows inventoried and decided; **14 further leaks found and fixed**. Delivers the structural guard `_auditWorldResetCompletenessNoSettle` → the `RESETWORLD_INCOMPLETE` token, gated by both headless runners, with the allow-list `WorldWdgt._worldStateAuditExemptions` (every exception carries its reason). §7.5 is the execution record. Durable case law: `Fizzygum-tests/DETERMINISM.md` §2d.
  - ⚖ reactive patching had left an ORDER OF MAGNITUDE more holes than the two caught instances implied — when a bug shape recurs, audit the whole surface instead of fixing the new instance. The worst rows were ones nobody had noticed: `numberOfIconsOnDesktop` (the desktop grid cursor — moves later tests' icons, a pure GEOMETRY leak), the world's own EXTENT, `errorConsole` (a dangling console makes every later paint error report into a dead widget, silently swallowing what the fail-gate exists to catch), `wdgtsWithOngoingScrollMomentum` (dead ref ⇒ `anyScrollMomentumOngoing()` true forever ⇒ later tests STALL rather than fail)
  - ⚖ a state-fingerprint guard must compare VALUES, never own-ness: a first cut swept own properties and fired 1469 times on a GREEN suite, because a prototype-declared field the teardown merely ASSIGNS (`@x = nil`) reads as `(absent) → nil`
  - ⚠⚠ prove a guard FAILS, don't just observe it silent — planting one leaking FIELD (not a no-op) made the suite fail 188 tests; only then is silence evidence
  - ⚠ the guard is LOAD-SENSITIVE for per-frame fields and ONLY the PARALLEL gauntlet showed it (the `settle`/`storage` legs warned in-wave, passed serially): a teardown is not a frame boundary, so Widget-level damage bookkeeping on the world reflects where in the cycle it landed. Validate such a guard under the parallel gate, not only the quiet inner loop
  - ⚖ §2's serializer oracle was a floor, not a ceiling exactly as written: it named the app-slot/`infoDoc*`/prefs rows, but the two worst finds are not serialized, so only the direct class sweep could reach them
  - ⛔ the PRODUCT twin `_teardownForSnapshotLoadNoSettle` has the same dangling-ref gaps — evidenced, deliberately NOT changed here (ships in `--homepage`; wanted owner sign-off + serialization round-trip). CLOSED the next day by `teardown-shared-core-plan.md` below, which deleted the twin outright
- **`teardown-shared-core-plan.md`** — COMPLETE (2026-07-29). Successor to the audit above, closing the reverse direction and then removing the ability to have a direction: the "drop every reference to what was just destroyed" half of both world teardowns becomes ONE **shipping** core, `WorldWdgt._teardownWorldStructureNoSettle`, that `_resetWorldNoSettle` (test, homepage-stripped) and `loadWorldSnapshot` (product) both call. The product twin `_teardownForSnapshotLoadNoSettle` was DELETED — all five of its lines were in the core — and `_resetWorldNoSettle` shrank 42 → 19 code lines. Fixes measured PRODUCT bugs on the snapshot path. New gate: six `world.teardownHygiene.*` checks in `Fizzygum-tests/scripts/serialization-roundtrip-headless.js` (both backends, the gauntlet's `serialization` leg). Durable residue: `Fizzygum/CLAUDE.md` Testing, `docs/architecture/serialization-duplication-reference.md` §11 step 2, `Fizzygum-tests/DETERMINISM.md` §2d.
  - ⚖ **the seam was never product-vs-test.** Only the *pristine-LOOK* half is (grey desktop, wallpaper, world extent, page scroll); the *dangling-reference* half is an obligation `fullDestroyChildren()` creates for whoever calls it, and neither caller's name claimed it — which is exactly why it kept being satisfied on one path and forgotten on the other. Believing the "product vs test" story is what kept two twins hand-synchronised through two drifts in two days, in OPPOSITE directions
  - ⚖ measure the split before moving code: a ~20-minute Phase 0 spike (dirty a field → load a snapshot saved BEFORE it existed → read it back) confirmed all 19 leak rows AND all six "not a leak" rows, so no row moved on an argument. It also found one nobody predicted — a stale highlight declaration makes the reconciler RE-MATERIALISE a `HighlighterWdgt` onto a restored desktop, so a load can produce a widget the file does not contain
  - ⚠⚠ **check the `»>>` strip markers around a FIELD's DECLARATION before moving its line into shipping code, not just around the line you are moving.** §4 called "the 6 highlight/pinout sets" one shared row; the three PINOUT sets are declared inside homepage-exclusion markers, so a shipping core clearing them would have thrown on the first snapshot load in a production build. They stay test-side — the strip boundary draws the seam, not a judgement call
  - ⚠ an assertion that can pass while broken is worse than none: the gate's first "no resurrected overlay" check (child-count equality) fired on one page and not another, because `addHighlightingWidgets` only re-parents the destroyed overlay when the dead target reports changed paint bounds. Replaced by a true invariant (nothing DESTROYED back in `world.children`), documented in-place as NOT forced by the fixture — the real highlight coverage is the collections check, which does fire
  - ⚖ writing the falsification probe paid for itself twice: besides proving the gate fails, it exposed a `Map` member-extraction bug in the check itself (`widgetsToBeHighlighted` maps target→style, so `.values()` never sees the dead target)
  - ⛔ `RESETWORLD_INCOMPLETE` was deliberately NOT extended to the load path: its baseline is *the pristine world*, and a snapshot load installs new state, so it would need a second baseline + its own exemption list to say what six named rig assertions say directly. The core is not under-guarded — the suite ratchet covers it through `resetWorld` on all 268 tests, the rig through `loadWorldSnapshot`
- **`suite-nondeterminism-flakes-plan.md`** — COMPLETE (2026-07-29). Investigation record for the three non-boot SystemTest flakes: A `macroClosingRotatedIslandChildClearsFootprint` (a glyph atlas warming BETWEEN the macro's two pixel reads — 7798px, text-shaped; the framework's invalidation was correct), B the serialization rigs' BISTABLE `pixelParity` (a half-warm atlas render is STABLE, and `anyTextDirty()` was under-reporting), C `macroSaveAsPromptAboveTiltedWindow` 100% at `--shards=1` (`UntitledNamingService` counters surviving `resetWorld`). Durable case law: `Fizzygum-tests/DETERMINISM.md` §3g/§3h/§3i.
  - ⚖ a green from an instrument you have NOT seen reproduce the failure is not evidence of absence (a state-leak hypothesis was wrongly "falsified" by a runner that cannot reproduce the bug)
- **`atlas-delay-fuzz-tool-plan.md`** — EXECUTED (2026-07-29). Built `fg fuzz` (`Fizzygum-tests/scripts/run-atlas-fuzz.js` + `audit-preludes/atlas-delay-prelude.js`): delays every SWCanvas glyph-atlas load by a seeded-random 0..250 ms and runs the suite + BOTH serialization rigs, to find pixel reads not covered by the text-settle gate. ON-DEMAND ONLY — ⛔ not a gate, not a gauntlet leg (owner-declined; nondeterministic by construction). Plan §9 is the execution record.
  - ⚖ a fault injector must PROVE it injected before it may say PASS — three verdicts (PASS/FAIL/**INVALID**, exit 0/1/**2**), never two. The prototype produced four invalid runs in one morning, three printed `failed: 0`, and a no-op injector reporting 268/268 certifies coverage that was never tested. Pinned by a 31-case canned-transcript corpus in `npm run selftest`.
  - ⚖ a BIGGER fault dose is NOT more sensitive: the §3i window is ~one frame, so too large a delay lands the atlas after BOTH reads and the diff returns to 0. Measured — the reverted flake-A fixture passed 13/13 under injection AND 3/3 with none.
  - ⚠ `LAYOUTAUDIT installed` contains the substring "stall", so it matched `run-all-headless.js`'s `/STALL/i` relay and printed twice per shard — which reads exactly like a double-wrapped injector, i.e. a silently doubled dose.
  - ⚖ match the CADENCE you are investigating, or you measure a different system (an over-settling probe nearly read as a refutation)
  - ⚖ read `gen` (`immutableBackBufferGeneration`) BEFORE the pixel magnitude — a large diff is NOT evidence of a real dropped invalidation
  - ⚖ shard count is a PREDECESSOR-SET axis, not a load axis; no standing gate runs 1 shard, and an s1 leg is owner-DECLINED on cost
- **`end-of-cycle-flush-drawdown-plan.md`** — COMPLETE. Case-study playbooks, code patterns, and verification protocol for converting/eliminating end-of-cycle layout-flush contributors one at a time.
  - ⚖ §7 snapshot STALE — superseded by flush-inventory §4
- **`end-of-cycle-flush-endgame-plan.md`** — COMPLETE. Endgame plan driving the end-of-cycle careless set to zero via CONVERT/ELIMINATE/DECLARED-COALESCED, then shipping the audit-fail capstone gate.
- **`end-of-cycle-flush-final-records-plan.md`** — COMPLETE. Plan driving the last 5 careless end-of-cycle records to zero (handle-construction, buildOverflow, scroll-factory groups) and shipping the capstone.
- **`end-of-cycle-flush-inventory.md`** — COMPLETE. Survey report + executed self-settle conversion history for the end-of-cycle layout flush; the campaign's binding record of results.
  - ⚖ paint-time caret re-sync flagged as latent — later found inert
- **`end-of-cycle-flush-survey-plan.md`** — COMPLETE. Original cold-executable design for surveying what reaches the end-of-cycle layout flush and whether it should self-settle.
- **`end-of-cycle-self-settle-conversion-plan.md`** — COMPLETE. Plan making widget teardown (close/destroy/fullDestroy) self-settle via a freefloating-skip, without redundant re-layout of unaffected parents.
  - ⚖ blanket freefloating-skip alone FAILED — destroy-then-recreate callers relied on deferred settle
- **`interactive-render-perf-A-C-plan.md`** — COMPLETE. Perf plan for SWCanvas's full-cover canvas-wide fast path (A) and static-face back-buffering (C) on a busy interactive desktop drag.
  - ⚖ C1 lesson: a cached back buffer can differ from direct draw via FP non-associativity
- **`island-buffer-cache-plan.md`** — COMPLETE. Completes the affine-transforms island buffer cache so transform-only changes never re-rasterize content; measured 1.40x.
  - ⚖ async glyph-atlas freeze — SWCanvas-only race, needs an epoch bump; native unaffected
- **`island-buffer-cache-rectlist-plan.md`** — COMPLETE. Upgrades the island buffer cache's single dirty rect to a disjoint rect-list so multi-region damage rebuilds only touched sub-rects.
- **`selection-overlay-unification-plan.md`** — COMPLETE + LANDED (2026-07-21). Replaces the world-attached HighlighterWdgt editor-focus indicator with a per-widget PAINT-TIME selection overlay (`Widget._drawSelectionOverlay`, drawn after the subtree, clipped to the widget's visible footprint), folding the spreadsheet cell ring into one mechanism; hover hook `paintHighlight`→`_drawHighlightOverlay`.
  - ⚖ the overlay respects a widget's OWN clipping — a selected widget overflowing its clipping island shows only its VISIBLE edges; an open-bottomed frame there is CORRECT, not a bug (the old world-child indicator drew the full frame only because it escaped clipping)
  - ⚖ the frame is TRANSIENT editor-focus state — it breaks byte-identity round-trip tests that screenshot two same-document points with different selection (deselect before such shots); a dropped item in an editing container is SELECTED (D21)
  - ⚖ "flakes only in the parallel suite" ≠ determinism bug — confirm with single-process runs + heavy-cycle injection first; here it was stale refs (dpr1-only footprint) + boot-storm infra flakes, not nondeterminism
- **`swcanvas-invisible-pixel-hash-nondeterminism-plan.md`** — PARKED. Investigates a raw-pixel-hash test failure with zero visible PNG difference; the diagnosed A=0 mechanism was disproven and the plan parked.
  - ⚖ Diagnosis tell — pixel-identical PNGs with differing hashes means THIS bug class
  - ⚖ Do not backfill references on the strength of the now-contradicted A=0 hypothesis
- **`dropped-background-fill-investigation.md`** — COMPLETE (2026-08-13). Finds the mechanism behind a `SimpleTextWdgt`'s explicitly specified `backgroundColor` that silently never painted (surfaced as 2 of the 165 references moved by the one-rect-fill campaign, re-baselined while the cause was unknown). Root cause is in Fizzygum, not the rasterizer: `TextWdgt`/`SimpleTextWdgt` declared `@backgroundTransparency = nil` as a constructor `@`-parameter, which assigns unconditionally and so shadowed `Widget`'s class default of `1` with nil; that nil reached `ctx.globalAlpha = undefined` in `StringWdgt::_prepareTextBufferContext` and the fill composited at NaN coverage. Fixed by taking both background params as PLAIN parameters forwarded to `StringWdgt`'s existing "only when passed" guard, plus a nil-safe coercion at the fill site. Regression test `macroSpecifiedBackgroundActuallyPaints` (assertion-only).
  - ⚖ a CoffeeScript `@param` in a constructor signature ASSIGNS THE FIELD UNCONDITIONALLY — it shadows the class-level default, and no later "only when passed" guard can undo it (second sighting; cf. the layout-spec follow-ups' nil sentinels). Take it as a plain parameter and assign it guarded
  - ⚠⚠ an invalid value handed to a canvas property is NOT necessarily loud: HTML5 says to ignore an out-of-range `globalAlpha`, but SWCanvas stores it raw and a fill then composites at NaN coverage — correct `fillStyle`, correct extent, zero pixels, no error anywhere
  - ⚖ the plan's own authored framing was falsified TWICE and both are traps worth knowing: its "the mechanism must be INDIRECT because the missing paint is a `fillRect` while the trigger commit changed `fillRoundRect`" was wrong (it IS `fillRect`'s own direct arm, removed at B1 `8f11434`) — because the BISECT was misattributed: `dist/swcanvas.min.js` at B1 was STALE (its build-info names `f3e6ac9`, its own unminified dist names `45dffae`), and the browser loads the MINIFIED bundle. **Bisecting a vendored engine through the browser bisects the minified artifact — check `swcanvas.min.build-info.js`, not the commit SHA** (third sighting of the stale-min trap)
  - ⚖ the regression guard asserts the FIELD, not pixels, and that choice was measured: the pixel assertion PASSES on the pre-fix tree because the currently pinned engine tolerates an undefined alpha — a screenshot test would have certified a framework still handing the canvas an invalid value

## OO cleanup, lint & modernization

- **`world-inventory-instruments-plan.md`** — EXECUTED IN FULL 2026-08-20 (authored + executed same day; Arc A of the three-arc object-lifetime program, whole program stated in its §0; close gauntlet 16/16, zero recaptures). Builds `WorldInventory` (src/dev-tools): a ~6 ms read-only walk of the reachable graph gated at every suite teardown beside the world-field ratchet, covering exactly that ratchet's blind spots — class statics, collaborator/module state, the DOM, and widget IDENTITY (`WORLD_INVENTORY_DRIFT/_ESCAPED/_ZOMBIE`, escaped widgets named WITH their retainer path). §5.1/§5.2 = the execution ledger: on its way to green the gate forced SEVEN product repairs (frame mid-teardown rebuild + the off-tree spare placeholder, hand gesture bookkeeping, the prompt measurer widget, the spreadsheet retain-throwaway, the corpse-layout drain guard, and the UNBOUNDED-LRU `DoubleLinkedList` stale-pre corruption affecting all eight caches — capacity 20 measured at size 4999), four macro-orphan fixes, and the `run-sequence` swallowed-console tooling repair. Durable truth: `architecture/world-lifetime-and-inventory.md`; Arcs B (CDP/forced-GC riders) and C (reset-by-reconstruction) stay in `BACKLOG.md`.
- **`world-vm-truth-riders-plan.md`** — EXECUTED IN FULL 2026-08-20 (authored + executed same day; Arc B of the object-lifetime program; close gauntlet 18/18 with the new `vmtruth` wave-C leg, zero recaptures). The tier the in-band gate structurally lacks: it never walks FUNCTIONS, so closure/listener/promise-tail retention is invisible to it — this arc asks V8 instead. Lands `vm-truth-gate.js` (`fg vmtruth`: per-teardown forced-GC WeakRef collectibility oracle at ONE-TEARDOWN grace — load-bearing, conservative stack scanning pins a heavy teardown's corpses through the immediate GC — plus a per-page post-GC heap-FLOOR ceiling of 96 MB, min-based because the heap balloons and deflates with test composition; INVALID exit 2 when a bucket carries no sample), `heap-forensics.js` (queryObjects registry cross-check + snapshot retainer paths naming the pinning `context:<var>`; `--selftest` plants and must find a closure-held corpse; absorbs the scratch detach probe), and the DOOR-CALLBACK LAW: a lazy-part tail acting on a widget opens with a destroyed-check — the S3 probe measured `editLayout` + destroy-mid-load silently BUILDING chrome on the corpse (permanent escaped widget pinning it, zero errors, only visible on index.html); five tails guarded, the race gated in the `parts` leg. ⚖ Case law: `FinalizationRegistry` as a gating oracle REJECTED — the WeakRef deref after forced GC is the same oracle made synchronous; heap-snapshot node NAMES are unusable for meta-built classes (instances are anonymous/wrongly named — map suspects by `HeapProfiler.getHeapObjectId`, snapshot FIRST or ids answer 0); a WeakRef created or deref'd in the current JS turn keeps its target till end of turn; `new Array(n).fill x` is too cheap to be a heap plant (unique strings are the honest ballast); and gating `page.metrics()` WITHOUT forced GC gates the GC heuristics, not the code.
- **`world-reset-by-reconstruction-plan.md`** — EXECUTED IN FULL, ARC CLOSED 2026-08-21 (Arc C, the last of the three-arc object-lifetime program; close gauntlet 18/18 + `fg homepage`, zero recaptures across the whole arc). Makes `resetWorld` a CONSTRUCTION: destroy the old world, `new WorldWdgt`, `finishWorldSetup`, swap `window.world` — so the two-lifetimes doctrine is enforced by the machinery instead of by a cleanup pass that had to remember everything. Phases: three spikes (GO — reconstruction is CHEAPER than the audited reset it replaces), eight independently-gated pre-repairs (per-instance containers, the `minimumFontHeight` boot memo, teardown hygiene, a page record of loaded parts, the listener detach verb shipped to product, the door-callback riders, NAMED constructors 513/513, the Automator made page-lifetime), the flip in ONE commit (suite 307/307 BYTE-IDENTICAL), then the acceptance tier and the promotion of `WorldInventory` into core. ⭐⭐⭐ The arc's central correction, and the reason its own §0 mandate did not survive contact: the acceptance invariant it was written around — "is the old world COLLECTIBLE?" (WeakRef + forced GC) — is a PROXY with false positives by construction, and the residue it kept reporting turned out to have NO RETAINING HEAP EDGE AT ALL. Proven by freezing the page in the detection turn (`doOneCycle` → no-op) before snapshotting, with a planted retainer the same BFS had to name. The gate was rebuilt to ask REACHABILITY in two tiers (prelude raises + PINS a suspect; the runner takes one heap snapshot per shard and marks forward from the GC root with the pin subtracted BY NODE, failing only on a still-reachable suspect and printing its path) and THE GRACE PERIOD WAS DELETED — a tolerance that only ever absorbed the proxy's noise. §D4b is the falsification ledger (five models, incl. the handoff's own). ⚖ Case law worth carrying: a recorded negative result must be checked against WHAT IT WAS MEASURED AGAINST (this plan's ⛔"deferring the sweep changed nothing" was true evidence gathered against a different, since-fixed bug whose 2990 findings hid the effect underneath); a PLANT MUST NOT BE REACHABLE FROM WHAT IT MEASURES (it bit twice); an instrument's own pin must be excluded by heap NODE and entirely, since a JS array reaches its elements through an `(object elements)` backing store; and §D5c — a deliverable's PREMISE can be false: D5(3) asked that a leg "lose its positive-coverage line" and there was none to lose, which measured out as `capstone` reporting ✓ PASSED on 307 real violations when its flag is lost. Durable truth: `architecture/world-lifetime-and-inventory.md` (§1 doctrine, §6 the VM-truth tier).
  - ⚖ gating tiers: identity + instances.* + declared-store policies GATE; bulk aggregates are REPORT-ONLY (every bounded-cache admission moves them — the ratchet's 1469-storm lesson)
  - ⚖ typed arrays are LEAVES to any walker (`Object.keys` on the world canvas's pixel buffer = 1.69M index keys, ~40× the rest of the walk)
  - ⚠ a `WORLD_INVENTORY_*` line fires at a test's OPENING reset — the leaker is the PREVIOUS test on that shard page, and a page-lifetime escape re-reports at every later teardown (date it by total teardowns − report count)
  - ⚖ prove-it-fails: both D5 plants failed the full suite (a planted static named by the escape's retainer path; an unregister outage's `<registry>` zombies), then removed
- **`paint-time-scroll-translation-plan.md`** — EXECUTED (Phases 0–3, 2026-08-19→20, each phase gauntlet-gated 17/17; Phase 4 ⛔ owner-gated and banked). The moved-plane scroll model dissolved: the offset is STORED TRUTH (`scrollOffsetX/Y`, frame-relative, integer) written only through `ViewportWdgt._writeScrollOffset` (which breaks the `geometryVersion` caches — the offset is mapping state), applied as a paint-time translation around the contents recursion (`_scrollTranslation`, dormant at 0) and carried by the TWO-ARM screen↔plane walks for input/hit/damage/clip; the plane is PINNED forever, `_applyMoveBy` demoted to whole-panel moves. Case law: the "7 benign churn failures" re-eyeball that falsified itself on a CONSEQUENCE pixel (an un-faded rectangle) into three real stacked defects — stale geometry caches (hit-invisible rows that paint perfectly), `escalateEvent`'s plane-local pos crossing the plane boundary (a stationary click slammed the scroll to the clamp), and in-plane click aims in test macro sources; plus `managesOwnScrollPinning`'s verdict (transforms into pure reset-on-resize POLICY, kept). Durable truth: `architecture/viewports-and-planes.md`; the Phase-4 lift seam (offset+interception to the clipping-mixin level = scrollability as a property of every panel) stays in `BACKLOG.md`.
- **`menu-sandwich-dissolution-plan.md`** — EXECUTED IN FULL 2026-08-20 (authored + executed same day; Phases 0–3, close gauntlet 17/17). Deletes `PopUpRowsPaneWdgt`: `MenuRowsPanelWdgt` becomes the pop-up rows viewport's DIRECT contents, byte-identically (306/306, zero recaptures, zero tests-repo changes). The doctrine correction: frame ownership's law is AGREEMENT-AT-FIXPOINT, not sole-committer — the panel keeps its hug self-write, and `scrolledContentMeasure` (full self-box) + `scrolledContentMeasureIsMyFrame` (verbatim commit: no window-floor/grow-to-fill against a tight hug) make the committer write the identical box in every state. STATUS BOX = the ledger, incl. the §3-CORRECTED shape and the two Phase-0 finds: drag-by-header TEAR (`contentsPanelHoldsLooseContent`, the ListWdgt opt-out) and the duplication-path livelock (⇒ the state-independent re-frame). ListWdgt ASSESSED = KEEP (its anti-vacant `_applyExtent` sizes the panel past the hug — there the pane is load-bearing). Truth: `architecture/viewports-and-planes.md`; resolution record: `BACKLOG.md` §7.2.

- **`scroll-frame-role-architecture-plan.md`** — EXECUTED IN FULL AND CLOSED (P0–P6, all on 2026-08-19, each phase gauntlet-gated 17/17). The Panel/ScrollPanel role refactor: `scrollPolicy 'auto'|'never'` (the runtime crop⇄scroll flip, test `macroScrollPolicyNeverFlip`), the `ScrolledPaneWdgt` plane class + deletion of `PanelWdgt`'s `@scrollPanel` personality switch, the inheritance cut (`ViewportWdgt extends Widget` — a viewport's children are chrome, so the panel-family answers were wrong on it), the scrolled-content contract (4 declared queries replacing the arrange's instanceof ladder, byte-identical), the window-product collapse (one button, policy-flippable), and the vocabulary rename `ScrollPanelWdgt`→`ViewportWdgt` (+4 construct classes, the member lexicon, the drawn strings; 8 menu-label recaptures were the arc's ONLY pixel churn). Durable truth: `architecture/viewports-and-planes.md`; deferred: `BACKLOG.md` (transform-island scrolling, the menu-sandwich revisit, the non-default-plane up-relay, deeper role names).
  - ⚖ policy over structure: a conditional/lazily-materialized scroll frame is a threshold somebody must cross mid-life (the PopUpWdgt precedent, adopted arc-wide); the plane is ALWAYS present
  - ⛔ a width-OWNING content as a viewport's direct width-constrained contents does not terminate (`RECALC_NONCONVERGENCE`) — the pop-up pane sandwich stays *(superseded 2026-08-20: `menu-sandwich-dissolution-plan.md` above dissolved the POP-UP sandwich by making the writers agree byte-exactly; ListWdgt's pane stays, and the falsification itself still holds for any measure the committer does not reproduce)*
  - ⛔ deleting a base-class member: grep every OVERRIDER for bare `super`, not just every dispatcher — two provably-no-op supers turned into TypeErrors and 60 island/stretch tests red in one run
  - ⛔ a rename sweep's `\b` rows cannot see mid-token targets — enumerate every token first (`grep -ohE "[A-Za-z_]*(Old)[A-Za-z_]*" | sort -u`); exclude the plan file itself (self-mangling old→new tables); keep test names + retired-method history verbatim

- **`nil-global-retirement.md`** — DONE (2026-08-13). Retires the `nil = undefined` global introduced in 2017 as a CoffeeScript 1→2 default-parameter workaround: ~2000 sites across both repos renamed to `undefined`, preceded by removing the "pass a hole, get the default" call sites and 41 dead `= nil` defaults. Residue: the one-absence-value convention + the `nil-literal` (baseline 0) / `null-literal` stinks.
  - ⚖ the decisive argument was not verbosity but that the alias had leaked into the REFLECTIVE layer — `ClassInspectorWdgt` emitted the token `"nil"` as generated source, so the meta-system could only emit code requiring the global it also defined
  - ⛔ a naive `\bnil\b` replace is NOT sufficient — string literals (one of them emitted source), prose broken by substitution, `nils` as a VERB, and uppercase `NIL` all escape it
  - ⛔ deleting a `= nil` default does NOT disarm the `@param` trap: `constructor: (@x) ->` writes `undefined` over the prototype default just as `(@x = nil)` did. The hazard is the `@param`. (eb063b3d's commit message claims otherwise and is wrong.)
  - ⚖ the source-DISPLAY surface is far narrower than the source-TEXT surface — rewriting ~1500 lines of class source moved exactly ONE test, because the inspector renders source only for the member SELECTED
  - ⚖ found + fixed on the way: `DemoMenus` had been building a stack panel with `@padding = null` → 0 instead of 5, the very CS2 breakage `nil` existed to prevent, alive in one of the ten tolerated `null` literals
  - ⚠ left open, each needing its own pass: `SliderWdgt` (8 hole-passing sites, wants an opts constructor) and the `add` family (four overrides carry a param named `unused` while `FrameWdgt` gives that slot the meaning `notContent` — a latent bug independent of `nil`)
- **`container-regularization-plan.md`** — COMPLETE (2026-07-19). De-byzantinates the Menu / List / Prompt / Divider family: `DividerWdgt` extracted, `MenuRowsPanelWdgt` lifted, `MenuWdgt`/`PromptWdgt` recomposed as `PopUpWdgt` composing a titled row panel.
  - ⚖ menu-ness already lived in `PopUpWdgt`, so the untie was a lift, not a rewrite — every section landed byte-identical
  - ⚖ §5.4 — the "one container becomes a window" merge was deliberately NOT done; ruled and recorded in `architecture/layering-naming-convention.md`
- **`menu-row-conformance-plan.md`** — ARC COMPLETE (2026-07-19). The follow-on ironing out `container-regularization-plan.md` §5.2e's residual wrinkles (menu-row conformance + hug-class stack).
  - ⛔ Phase 4 RUN AND FALSIFIED (falsification #2) — the tracking-pop-up shape is permanently CLOSED; do not re-attempt it
- **`immutability-and-canonical-instances-plan.md`** — COMPLETE (2026-08-04). Finished the Bloch treatment of the value classes: eliminated the last 6 in-place Point mutations, DELETED `Point/Rectangle::copy()`, added `Point.ZERO` + return-`@`/canonical shortcuts (exact-`===` bar), made the 137 Color constants permanently interned (`createConstant` + `Class.coffee` regex widening), converted TransformSpec to withers, and put `keptByReferenceOnDeepCopy` on Point/Rectangle/ShadowInfo/TransformSpec. Zero pixel churn across every gate. Residue: `docs/architecture/immutable-value-classes.md`.
  - ⚠⚠ a class-level constant may reference ITSELF (deferred-static mechanism) but NEVER another class: the boot dependency scanner only sees `new X` edges in class-declaration-level initializers, so `@NO_SHADOW: new ShadowInfo Point.ZERO, 0` class-evaled before Point existed and hung the interactive boot — the harness page masked it; the boot smoke named it. Fix shape: lazy memo in the factory.
  - ⚖ shortcut guards must be duck-typed (`delta.isZero?()`), not `instanceof` — the type-test stink ratchet (baseline 93) correctly rejected the first guard shape
  - ⚖ `multiplyBy 0 → ZERO` needs a `Number.isFinite` guard (NaN·0 ≠ 0); `-0`-for-`+0` accepted under the `===` bar
- **`accidental-complexity-reduction-plan.md`** — COMPLETE. Five-dimension refactor plan (dead code, duplication, over-engineering, control-flow, tooling) across ~470 .coffee files; all actionable items landed+pushed.
  - ⚖ P4 — ScrollPanelWdgt scroll-delta LEAVE-AS-IS, determinism-critical
  - ⚖ P5 — ctor-arg reorder safe for serialization/duplication (Object.create bypasses ctor)
  - ⚖ §0.1 — layout suppression booleans + naming tiers explicitly OUT of scope
- **`census-findings-triage-plan.md`** — COMPLETE. Triages the remaining hierarchy/property census findings added by the Pharo generic-lint carryover.
  - ⚖ a census finding is a QUESTION never an instruction — the 2 top 'best wins' were false positives
  - ⚖ case law 11 — mixin injection onto subclass prototype defeats a naive pull-up
  - ⚖ case law 15 — WorldWdgt.removeEventListeners silently missed 7 of 20 listeners
- **`class-modernization-playbook.md`** — COMPLETE. Process playbook for 'bringing a class to latest' (delete/rename/re-author-tests), reused across all 23 rename batches.
  - ⚖ presentation is part of 'to latest' — a call site can render broken even after a purely mechanical rename
  - ⚖ test-facing API method names called BY NAME from macros must be enumerated before any vocabulary sweep
- **`cross-branch-duplication-refactors-plan.md`** — EXECUTED 2026-07-27. The inverse audit's non-mixin findings: R1 all nine custom painters (Handle, Pen, LabelButton, LayoutChrome family, Cell, SheetHeaderCell, AnalogClock, GraphsPlotsCharts family, Example3DPlot) converted to per-class Appearance delegation — `Widget::paintIntoAreaOrBlitFromBackBuffer` is now overridden ONLY by BackBufferMixin; R2 the `CodeAreaWdgt` base for the Console/Script/CodePrompt/ErrorsLog code-runner family; R3 `Rectangle.largestCenteredSquare()` + `PreferencesAndSettings.normalizedWheelDeltas()`.
  - ⚖ subclass-supplied drawing tails become PUBLIC protocol methods (`drawPlot`, the `drawLayoutChrome` precedent) — an appearance calling a widget's private helper would bump the call-separation ratchet; helpers private to ONE class move INTO its appearance instead (the clock's five)
  - ⚖ moving members OFF a widget's prototype churns any test that shows that widget's inspector member list (the clock's lost 6 rows → 2 gated recaptures, verified one-mechanism-benign via fg diffpage) — inspector-list churn is the R1 conversion's only pixel cost
  - ⚖ R2's "fold the shared `_reLayout`" was FALSIFIED by the verbatim diff (button fields, 2-vs-3 geometry, `_fullChanged` presence differ) — the base carries only byte-identical members; and R3.3's "pixel-alpha pair" was MIS-CHARACTERISED (they are `RectangularAppearance.isTransparentAt` copies; unification would leak shape menu entries — LEAVE with twin cross-references)
- **`disable-editing-family-convert-plan.md`** — COMPLETE. Converts the disable/enable-editing family (7 classes) to the self-settling wrapper + _*NoSettle core idiom.
  - ⚖ transitive-settle lint blind spot — check-layering only discovers LITERAL settling wrappers, not transitive ones
  - ⚖ Phase 7 witness test caught a real disable-path throw no existing test had exercised
- **`duplication-triage-2026-07-15-hierarchy-round4.md`** — COMPLETE. Snapshot of the hierarchy-aware duplication census (IDENTICAL-TO-INHERITED, PULL-UP, DEMOTE); all tranches actioned, zero recaptures.
  - ⚖ A write-only field is enumeration payload, not a local, until proven otherwise
  - ⚖ A mixin augmented onto a subclass injects only the subclass prototype, not the base
  - ⚖ Deleting a Widget-family method does NOT churn the 15-test inspector set
- **`god-class-decomposition-plan.md`** — COMPLETE. Splits the Widget/WorldWdgt/MenusHelper God classes into delegated collaborator classes, following the MacroToolkit mixins-to-OO-delegation precedent.
  - ⚖ recapture reality corrected the backlog — moving a Widget method DOES recapture the inspector test
- **`lint-generic-rules-carryover-plan.md`** — COMPLETE. Carries Pharo SmallLint/Renraku-inspired generic lint rules (unresolved-sends gate, 7 stinks, 2 censuses, fg critique) into Fizzygum's build gates.
  - ⚖ console.log policy RESOLVED: accept as-is, fix 6 wrong verbs to console.error
- **`lint-ratchet-static-checks-plan.md`** — COMPLETE. Ratchets the layout-flow lint (rule [G] direct form) from partial runtime-throw enforcement to build-time static coverage.
  - ⚖ Phase 1b transitive closure prototyped and REJECTED as intractable
  - ⚖ Phase 5 allowlist lint superseded by the runtime auditUndeclaredEndOfCycle capstone
- **`menu-slider-ctor-conversion-plan.md`** — COMPLETE. Converts SliderWdgt/MenuWdgt/prompt constructors to the wrapper+NoSettle-core settle pattern, retiring 4 constructor-build-exempt lint markers.
  - ⚖ a suite RED on WebKit was LOAD-FLAKE, not a regression — reproduced clean 3x
- **`mixin-application-tidyups-plan.md`** — EXECUTED IN FULL 2026-07-27. The inverse mixin audit's three survivors: new `BubblesEditModeToCoordinatorMixin` (the byte-identical edit-mode-bubbling cores triplicated across SimpleVerticalStackScrollPanel/StretchablePanel/StretchableWidgetContainer, unrelated branches), `HighlightableMixin` + colour triple hoisted from the three `IconicDesktopSystemLinkWdgt` subclasses to the parent, and documentation of the two deliberate pinned-`@ratio` non-consumers of `KeepsRatioWhenInVerticalStackMixin`.
  - ⚖ the KeepsRatio "just augment" conversion was FALSIFIED at authoring — both classes are deliberate pinned-`@ratio` VARIANTS (field-based, super-fallback), and Stretchable's old comment gave a WRONG reason (claimed injection would clobber the class body; class body wins) — do not re-attempt the conversion or unify the two ratio protocols (D6 aspect contract)
  - ⚖ case law 11 applied in REVERSE — to de-duplicate a mixin applied across every subclass, hoist the INJECTION to the parent (fields pulled up without the augment are dead text under injection shadowing); the only churn is any UI that COUNTS consumers (the mixin-edit test's save popup went "9 consumers" → "7")
- **`mixin-editing-v2-plan.md`** — EXECUTED IN FULL (same day, 2026-07-26). Completes live mixin editing atop v1's donor routing: the four-form mixin super rewriter, the receiver-side "override in this class" gesture (via the new `Class.applyMemberEdit` choke point, shared with snapshot replay), the "from `<Name>Mixin`" donor label, add/remove mixin members with registry replay, and mixin class-side statics; closed by `SystemTest_macroMixinEditDonorAndOverride`.
  - ⚖ donor attribution was METHOD-scoped at close — a donated FIELD showed its plain value un-attributed and a field-targeting UI flow silently took the plain class-edit path; field parity landed the same day, owner-directed (see the plan's residual ADDENDUM)
  - ⚖ inspect the class that DECLARES the `@augmentWith` — a subclass's inherited-hidden member list has no such row, and the row-select helper's thumb-press then degenerates into a window float-drag (the naked-inspector vBar gotcha, windowed edition)
- **`oo-smells-refactoring-backlog.md`** — COMPLETE. OO-smell cleanup backlog (dead code, base-class extraction, IconWdgt thinning, MenuItemSpec, Widget decoupling); phases 0-5 landed, 6-8 superseded elsewhere.
  - ⚖ Phase 5c — instanceof→isX?() predicate sweep REVERTED, still a type-test
  - ⚖ Ordering rule — Phase 5 must precede Phase 6 (God-Class split)
- **`public-private-call-separation-plan.md`** — COMPLETE. Command/query discipline campaign (rules [S]/[T]/[U]) privatizing public methods not provably public API; fully executed via census-driven tranches.
  - ⚖ Census heredoc bug misclassified a self-call, letting 2 macro verbs into rename list
  - ⚖ [A]-collision rule — a public method driving public settling on OTHERS can't go private
- **`type-test-elimination-plan.md`** — COMPLETE. Codebase-wide capability-first campaign eliminating instanceof/isFoo? type-test smells; absorbs widget-identity-decoupling-plan.md.
  - ⚖ Capability-named queries legitimate where behaviour can't move — 5c's flaw was faithfulness, not queries
  - ⚖ ε LEAVE example: SliderButtonWdgt identity shape falsified by documented detach-then-duplicate state

- **`damage-vocabulary-unification-plan.md`** — EXECUTED IN FULL 2026-08-18 (same day as authoring). Unifies the three-word repaint-region split: pixels say DAMAGE (`world.damageRects`, `_repaintDamagedRects`, `_fleshOut(Full)Damage`, `_islandBufferDamageRects`, `damageRectMargin`, 33-name table), DIRTY survives only as layout invalidation; the law lives in `architecture/appearance-paint-convention.md` § "The vocabulary law".
- **`dispatch-slot-protocol-plan.md`** — EXECUTED IN FULL 2026-08-18 (authored, censused, decided, executed and gated the same day). Fixes ButtonWdgt's four dispatch slots to ONE meaning each: the trigger passes ITSELF (slot 1) and the panel-filled `subjectOfAction` (slot 2); prompts deliver the extracted VALUE (`PromptWdgt.deliverValue`/`_promptValue`), so every pin setter takes one argument and the ⚠ SHAPE convention is deleted (`widget-authoring-guidelines.md` §11 is the one-value contract). The `MenuRowsPanelWdgt` crossover, the `if !@environment?` fork and `environment` on the menu classes are dead. Census: `measurements/dispatch-slot-census-2026-08-18.md` (the two fills partitioned exactly by delivery family — zero verb migrations). Gate: menusweep's PROMPT_CALLBACK_ARITY (a prompt callback declaring >1 parameter fails).
  - ⚖ the "cleanly layered" synonym-map option was REJECTED BY MEASUREMENT: the strata are era-correlated, not subsystem-correlated (the three core files mixed all three words)
  - ⚠ scoped OUT deliberately: text-atlas `*Dirty` settle-gate booleans, `SWCanvasBrokenTests` (broken = failing), English "is broken" alerts, TextWdgt line-"breaking" prose
  - ⚖ `@damageQueue` was wrong: the list is rebuilt each cycle inside the paint station, nothing queues across cycles
- **`connector-ubiquity-and-reflection-plan.md`** — COMPLETE 2026-08-18 (authored 2026-08-14; P1–P9 plus P10(c)+(d) landed 2026-08-16→18). Makes the controller-is-a-view law hold where the tree had given it up: `PinSpec` replaces the write-only setter tables so pins are READABLE (P1), `bind ⇄` is two ordinary wires and a gesture rather than a new edge kind (P2), one announcement verb plus the edge's `firesOnAnyChange` (P3), a controller owns a LIST of wires (`@wires`/`WireSpec`, P4), non-widget state becomes nodes (`Wallpaper`, `PreferencesAndSettings`, P5), the palette draws the colour it holds (P6), menu ticks become a reflection instead of a redraw (P7), scroll joins the public wire vocabulary and the field plumbing retires (P8), and `@target`'s four meanings are disambiguated (P9). P10(d) landed a switch as a view of its INDEX (`SwitchButtonWdgt`'s `shown button` pin); P10(c) was RE-AIMED by measurement — not a command table but a LIFETIME, so a pinned menu now gives up its command rows.
  - ⛔ **P10(a) is a recorded REFUSAL** — do not route `ButtonWdgt.trigger` through the dataflow drain: pools destroy click counts, a button has no value to PULL, commands are not pins, and 328 menu actions would each need the `_*NoSettle` lattice. Revisit only after the wire-vocabulary plan's W1, opt-in per button
  - ⇢ **P10(b) (index button `@target`s as non-traversed COMMAND edges) is RE-HOMED, not dropped** — it rode `archive/graph-edges-and-lifecycle-plan.md` §4.2 ("one graph index, not two"), where it was ANSWERED (decision G4: no index) and landed 2026-08-18
  - ⚖ §8's questions were nearly all answered BY MEASUREMENT rather than by choice, and two DISSOLVED with their premise: the `@wires` serialization bump (Q6 — the accessor shim it presupposed is not buildable in this codebase, and there are no serialised worlds of interest) and the boolean payload (Q9 — a switch is n-way and its state is an INDEX, so a boolean is only the n=2 case seen from outside; no fourth payload kind)
  - ⚖ a tracking pin pair does NOT shear (Q1, measured 0 frames): `doOneCycle` plays INPUT before the dataflow station, so a gesture-driven change is announced in time for the same cycle's drain. This licenses a readable geometry-ish pin where a consumer needs one — it does NOT make `width`/`height` readable
  - ⚖ the reverse scroll edge carries NO payload (Q5): the bar is woken and RE-READS (`firesOnAnyChange`), because a node has one value and a scroll frame has no one number — which is why the wire-vocabulary plan's W2 still has no customer
- **`onion-widget-composition-plan.md`** — COMPLETE 2026-07-20 (the Frame-model flagship arc, phases A · C · B · D · E, 2026-07-19→20). Supersedes its own earlier "three-ring onion" framing: the middle "editor panel" ring is DISSOLVED — the toolbar is a *slot inside the frame*, not a ring — so a naked capability widget becomes a manipulable citizen by being wrapped in ONE frame that owns the chrome, the toolbar slot and the close policy. `ReconfigurablePaintWdgt` and `StretchableEditableWdgt` are DELETED (D1b, byte-identical); focus unified as a POLICY (D2a–D2c); close policy became a tracked field, resolving the code's own monkey-patch TODO (E2). Program siblings: `container-regularization-plan.md` (+ its `menu-row-conformance-plan.md` follow-on), and the still-active `graph-edges-and-lifecycle-plan.md` / `creation-and-templates-plan.md` / `reference-widgets-plan.md`.
  - ⚖ **the mandated four-way focus abstraction was FALSIFIED as structure-without-a-consumer** (§5.D D-2) — the honest deliverable was the focus POLICY unification, not the abstraction the plan had mandated
  - ⚖ most of §5.E was ALREADY DELIVERED by A/B/D by the time it was reached — re-verify a phase's substrate against `src/` before executing it, which is what each phase's "deltas" block records
  - ⚖ E1 (no-pencil readOnly) DEFERRED under the LOCKED-D8 argument, and the info-widget factory collapse with it — both owner-gated on `BACKLOG.md`
  - ⚖ every phase landed BYTE-IDENTICAL with zero recaptures (gauntlet 11/11, revisits 0, census 0) — a composition change that moves pixels is a signal to re-read the design, not to recapture
- **`graph-edges-and-lifecycle-plan.md`** — COMPLETE 2026-08-19 (authored 2026-07-18; §4.1 landed 2026-08-16 via connector §P9, §4.2 2026-08-18, §4.3 2026-08-19; decisions G1–G8 owner-ratified 2026-08-18). Makes the three widget-graph edge kinds — containment, target/action (flow wires + button commands), reference — ONE enumerable vocabulary (`Widget.graphEdgesOut`, composed like `pins()`), and lifecycle ONE reachability question: `StorageSorter._runClassifier` walks containment ∪ flow ∪ reference from the attached forest + furniture with a fixpoint over storage residents (the tracker iteration left the classifier); wire death is severed at both chokepoints (`severWiresIntoDyingNode` + fire-time self-heal — no wire record outlives its target); the three close sites ask `anyReferenceOrWireIntoWdgt` (walk-based, subtree-aware) so close and filing agree.
  - ⚖ **command edges confer NO liveness anywhere** — G8's ephemeral-chrome rationale generalized from close to filing (a menu row IS a ButtonWdgt; an open menu on a bin resident must not shelf it); the §4.3 spec text's four-kind union was consciously narrowed at landing
  - ⛔ reference-counting is NOT the GC mechanism (§4.4); no standing command-edge index (G4) and no world-level edge registry (§7) — each a recorded rejected alternative with its grounds
  - ⚖ the dead-target-wire raggedness was SPIKE-PROVEN before fixing: the drain silently delivered into a destroyed widget and the tracking lane re-subscribed FROM it — and a delivery-only guard was rejected because the lying wire record would serialize, reflect in menus, and mislead the collector
  - ⚠⚠ **the collector surfaced a LATENT meta-system bug**: same-mixin-twice-in-one-inheritance-chain makes the emulated mixin `super` recurse forever (members compile to ONE shared function; the `_class_injected_in` marker resolves per-INSTANCE) — SimpleTextWdgt's redundant re-augment removed; `addInstanceProperties` now throws on double injection (`architecture/mixins.md` §2)

- **`basement-to-bin-plan.md`** — COMPLETE (executed same day, 2026-07-22). Basement→Bin conversion: unpinned pop-ups DESTROY on dismissal (one `PopUpWdgt._closeNoSettle` override covers all four paths), doGC marks world-slot furniture reachable, permanent lost-only view + confirmed Empty bin, full Bin rename (classes/files/keys, both repos), TrashcanIconWdgt deleted. Its §6 "physically split the roles" rejection was SUPERSEDED the next day by `bin-shelf-eager-sorting-plan.md`.
  - ⚖ the thin-wrap gate forced the cleaner shape: `PopUpWdgt.close` DELETED, `openPopUps` bookkeeping moved into the `_closeNoSettle` core (also fixed the NoSettle-drain leak)
  - ⚖ the lost-only filter must be SYMMETRIC (re-show lost) once permanent — with the toggle gone, an item going lost while hidden had no other path back to visible
  - ⚖ Phase-2 `show()` companions in the SAME commit as the oracle fix (hidden-furniture landmine); owner declined the dev-mode show-all escape hatch (deleted outright)
- **`bin-shelf-eager-sorting-plan.md`** — COMPLETE (authored 2026-07-22, executed 2026-07-23). Bin/Shelf split with EAGER sorting: `ShelfWdgt` (bare, never-viewed store) holds the reachable, the bin exactly the lost, at all times; reachability chokepoints mark, a `StorageSorter` doOneCycle drain station (before `recalculateLayouts`) classifies once and moves; whole lazy view machinery (hide/show, revival un-hides, on-open refresh) DELETED; Tier A `STORAGE_INVARIANT` console-token guard on every suite leg + Tier B `fg storage` deep-audit leg (from-scratch reclassify, zero-moves idempotence, empty profile) → 13-leg gauntlet.
  - ⚖ the doGC "bin on-screen" precondition dissolved by ONE pass-1 change — discard only `isOrphan() and !isInStorage()`; an orphan reference IN storage is a pass-3 relay (spike-proven ≡ open-bin on chains, first shape)
  - ⚖ subclass lifecycle bookkeeping goes in the NoSettle CORE: `IconicDesktopSystemShortcutWdgt.destroy`'s tracker delete was bypassed by every bulk destroy (core chains never call public wrappers) — a years-old silent tracker leak the newborn Tier A guard exposed in minutes
  - ⚖ a drain station must suppress its own relay ECHOES (`@_draining`, the dataflow no-re-entry rule): its container moves re-fire child-add/removed hooks, and a mid-drain reclassification overwrites the marks the drain is still reading
  - ⚖ per-event immediate sorting is FALSIFIED by construction — a shortcut registers into the tracker in its ctor while still orphan; only an end-of-cycle drain classifies it attached
- **`drag-embed-implementation-plan.md`** — COMPLETE. Implements dwell-to-arm drag-embed UX: destination edit-mode gate, 450ms dwell arming, offset landing, derived internal/external.
  - ⚖ Phase 4 pill+hint UX BUILT then OWNER-REJECTED — replaced by plain land-at-release, no popup
  - ⚖ S2 gap-credit mechanic FALSIFIED by real-mouse test — revised to elapsed event-time + ring feedback
- **`fizzytiles-sw3d-port-plan.md`** — COMPLETE. Ports Fizzytiles 3D rendering from WebGL/twgl onto vendored SWCanvas SW3D and makes the 3D pane run real tile-authored code.
  - ⚖ §9 AS-BUILT overrides the unticked §landing checklist — read that, not the boxes
  - ⚖ block-scoping bug found post-landing: primitives must return truthy or transforms leak
- **`folder-population-entry-points-plan.md`** — COMPLETE (authored and executed 2026-08-02). Makes the lazily-populated Examples folder fill itself however it reaches the tree, not only via the one ritual that honoured its readiness protocol: `ExamplesFolderWindowWdgt` registers in `world.steppingWdgts` while it still owes itself content and populates from `step` the first time `@root() == world`, unregistering on that one shot. Gauntlet 14/14, zero reference churn.
  - ⚠⚠ the KNOWN LIMIT the code documented was UNREACHABLE AS WRITTEN — "a window dragged out of the SHELF" cannot happen, `world.shelfWdgt` is never added to a parent and never painted. The real route is the BIN, and there the comment's consolation ("shows empty once; the next bring-up fills it") was FALSE: no shortcut survives that route, so it was empty for ever. A plan's first step being a spike that could have ended in "correct the comment instead" is what caught this
  - ⚖ the spike MOVED the fix: the folder is painted-empty *inside the bin window* before any drag, so the onset is "the bin is opened", not "the drag completes" — §4.C (hook the bin's revival path) was insufficient, not merely awkward
  - ⚖ both of §1.5's stated costs shrank on contact: stepping is opt-IN via `world.steppingWdgts` (the `DataflowSource` precedent), so the per-cycle cost ENDS at the first fill and a snapshot-restored populated folder never registers at all (the Serializer already records stepping membership); and `_playQueuedEvents` runs earlier in the SAME cycle as `_runChildrensStepFunction`, both before the paint, so an already-fetched part fills the folder in the very frame the drop lands — no flash
  - ⚖ the gate asserts BOTH halves — that deleting the shortcut fetches NOTHING while the folder is off the tree, and that being shown fills it; the first is what would catch a "fix" that simply populated eagerly, i.e. one that bought correctness by giving up the laziness the whole tier exists for
  - ⚠ a gate for this can only be a Node rig, never a SystemTest: `createDesktop` runs only `if world.isIndexPage` and the harness page has no desktop at all
- **`pencil-eye-edit-mode-toggle-plan.md`** — COMPLETE. Makes the window edit-mode button show pencil/eye as a state glyph, later refined to monochrome-rest plus yellow hover feedforward.
  - ⚖ §2 — rejected SwitchButtonWdgt [pencil,eye], would add a second source of truth
  - ⚖ CLICK-THEN-PARK — must move pointer off button before screenshotting rest state
- **`spreadsheet-standard-caret-editing-plan.md`** — COMPLETE (authored + executed 2026-07-24). Cell editing became STANDARD CaretWdgt editing on a real editable in-cell editor: type-to-edit replace / Enter / F2 / double-click-at-clicked-slot enter; Enter commits + Escape reverts via the caret's accept/cancel escalations landing on the CellWdgt; acting elsewhere commits. The 2b buffer model, the sheet's editing-mode keys and `StringWdgt.showsEndOfTextBar` are DELETED. Framework repairs it forced: dispatch snapshots the receivers Set (fixed a REAL Tab runaway — one press hopped every entry field), accept/cancel escalate from the TARGET (as written they climbed from the destroyed caret's nil parent — delivered to nobody, ever), click-away funnel = `caret.accept()`, `StringWdgt.alwaysEditsInline`, non-editable `mouseDoubleClick` escalates.
  - ⚖ the reframe's "plumbing already exists" was HALF-true: the parenting fact held, but the escalation FIRE-ORDER made it dead — spike before building on a "verified" claim about event delivery
  - ⚖ the "caret blinks (non-deterministic)" fear was wrong the whole time: `BlinkerWdgt.step` is suppressed under the Automator's animations pacing, so carets in references are byte-stable
  - ⚖ a reference can BAKE IN a framework bug: the fitting-modes test's image_4 carried a double-delivered duplicate glyph from the live-Set dispatch — the fix made the render MORE correct and the ref had to follow
  - ⚖ drag-SELECT inside the cell editor is NOT free: the editor is solid with its cell (`wantsDetachOfChild` false ⇒ `grabsToParentWhenDragged` true), so a down+drag is the window-drag gesture — click-to-place / shift-click / double-click-word are the selection story
- **`serialization-deserialization-plan.md`** — COMPLETE. Phased plan building the Serializer/Deserializer pair, file save/load, whole-world snapshot, and source-edit capture, replacing the buggy prototype.
  - ⚖ Duplication (DeepCopierMixin) kept fully untouched/pixel-identical while building the new serializer
  - ⚖ Any out-of-subtree pointer not a well-known singleton = a path-carrying error at serialize time

## Build & toolchain

- **`boot-cost-reduction-plan.md`** — COMPLETE (2026-08-01). Cuts Fizzygum's boot cost via a pre-compiled dev tree and maximal laziness; ends with every app icon lazy and the launcher parts gone.
  - ⭐ **AN ICON IS NOT ITS APP** — an icon is core art plus the app's class NAME, so it draws without its app being defined; asserting absence alone passes for the wrong reason, so the gate checks the PAIR (icons drawn AND no app class defined)
  - ⚠ the SUITE cannot see laziness at all: the harness page presets `FIZZYGUM_EAGER_ALL_PARTS`, so a "lazy" SystemTest would pass while proving nothing — laziness is only observable on `index.html`, hence `fg lazyprobe`
  - ⛔ **a creator button CANNOT await** (`WidgetCreatorAndSmartPlacerOnClickMixin`) — the door has to be somewhere that can
- **`core-app-slices-partition-plan.md`** — EXECUTED AND CLOSED (2026-07-30). Extracts the app-like slices — spreadsheet, maps, plots — out of `core`.
  - ⛔ `dataflow` STAYS IN CORE (owner decision D2): it is settled core, not an app slice
  - ⚠⚠ two CORE doors were dead on a `lean` tree — `createDesktop` drew their icons unguarded while `launch` used `whenAllLoaded`, so the click could only reject. Required-vs-optional is now a deliberate choice at every door: `whenAllLoaded` when the part CONSTITUTES the result, `whenOptionalPartsLoaded` when it merely ENRICHES it
- **`budgeted-source-compile-scheduler-plan.md`** — COMPLETE (2026-08-04). Replaces BOTH one-class-per-turn source-ingest pacings (boot chain + `PartsRegistry._ingestPartPromise`) with ONE `window.SourceCompileScheduler`: a synchronous end-of-frame drain (after `_updateBroken()`) fitting as many classes as the leftover frame budget allows — always ≥1, strict dependency order — with a per-line EWMA cost estimator, and a `setTimeout(0)` chunk pump when no world exists. `framePacedPromises` paces batch FETCHES only now. Dev boot 3219→946 ms; spreadsheet part 12 classes in 2 frames (was 12). Gauntlet 14/14, zero reference churn. Measurements: `measurements/budgeted-compile-scheduler-2026-08-04.md`.
  - ⚠⚠ 40 ms sync pump chunks COLLAPSED the parallel test wave, twice, reproducibly — all 8 suite shards died at once while 5 paint pages boot-compiled beside them; either leg alone passed. Chrome closes targets when a page holds its core in long uninterruptible blocks under ~13-page contention. **Chunks ≤10 ms on pages sharing a loaded machine; batching wins come from fewer timer turns, never longer blocks**
  - ⚖ the ≥1-per-drain floor IS the biggest single class (fizzytiles' `LCLCodePreprocessor`, 1887 lines ≈ 28 ms) — the old one-per-frame pacing paid the identical cost; don't assert drain bounds tighter than budget + one-class overshoot
  - ⚖ size-based / dependency-safe cherry-picking DECLINED (owner): `findLoadOrder()`'s sequence is the only ordering ever exercised and the dep map is known-approximate (arc 4) — when the next class doesn't fit, STOP, never scan ahead
  - ⚖ resolving a pacing promise only schedules a microtask, so the old compile already EXECUTED after paint — the drain move bought MEASURABILITY (synchronous timing per item), which the promise-chain shape structurally could not provide
  - ⚠ estimator priors derived from the 2026-07-31 boot measurement are ~4× conservative (those per-source figures included per-turn timer overhead); the EWMA converges to ~0.014–0.02 ms/line

- **`build-arc-1-test-serving-link-plan.md`** — COMPLETE (2026-07-28). Arc 1 of the build-and-packaging program: replaces the per-build ~4,150-file tests COPY with ONE relative symlink (`Fizzygum-builds/latest/js/tests` → `Fizzygum-tests/tests`), moves the two manifests into the tests repo as derived/gitignored files regenerated by the build and by every runner at startup, and retires the flatten, `--keepTestsDirectoryAsIs`, build.py's manifest generation, and every publish-rebuild in the capture/recapture flow. Gauntlet 13/13 with ZERO reference churn.
  - ⚖ the "file:// can only load from its own folder or lower" folklore is FALSE on both engines the suite drives (it is Firefox's fetch/XHR rule; Fizzygum never fetches) — `<script src>` follows `../` and symlinks fine
  - ⚠⚠ `rm -rf <link>/` — ONE trailing slash — DELETES THE WHOLE TARGET, and `find -L` descends through the link; `rm -rf <parent>` does NOT (measured both ways). Hence exactly two operations on that path anywhere: `ln -sfn`, and a guarded `[ -L p ] && rm -f p` whose `rm -f` cannot recurse. A PreToolUse hook rule blocks the dangerous spellings
  - ⚖ FALSIFIED (§10.1): the `Fizzygum-builds` slimming was a NO-OP — `latest/` was never tracked on `master`; only a 2016 `gh-pages` snapshot holds the old copy, deliberately left alone
  - ⚖ FALSIFIED (§10.2): `--homepage` needed the two manifests to EXIST during its `?generatePreCompiled` pre-compile pass (the boot condition was not `BUILDFLAG_LOAD_TESTS` alone), so the build wrote two EMPTY stubs and cleared them afterwards — **SUPERSEDED by arc 2**, which narrowed that condition; a homepage build now writes no `js/tests` at all
  - ⚖ a manifest regenerated at runner startup is read LIVE by browsers through the link, so the write must be no-op-when-unchanged + atomic `rename()`, never a truncating `writeFileSync`
  - ⚖ reference filenames carry a `systemInfoHash` derived from SCREEN SIZE/COLOUR DEPTH — capturing on a different display renames every file it touches while the pixels and `dataHash` stay identical

- **`build-arc-2-backend-split-precompile-plan.md`** — COMPLETE (2026-07-28). Arc 2 of the build-and-packaging program: splits the rendering backend at BUILD time (the runtime `?sw=1` switch is deleted) and externalizes pre-compiled-image generation. One build pass now emits TWO boot bundles from ONE terser output and three entry pages from one page source (`build.py`'s `ENTRY_PAGES`): `index.html` native (31,898 B, carrying only SWCanvas's new subtractive 3D-core dist target for SW3D), `index-sw.html` + `worldWithSystemTestHarness.html` SWCanvas (314,491 B). `--homepage` is native-only: no SW bundle, no `index-sw.html`, no 90 MB `font-assets/`. Gauntlet 13/13 with ZERO reference churn.
  - ⚖ splitting a bundle is NOT "two builds": the seam is the post-terser concatenation, so a second flavour costs a `cat`, not a build pass
  - ⚖ the 3D path needs ~14 KB of SWCanvas, not 263 KB — and the subset is PIXEL-IDENTICAL to the full engine (SHA-256 over the whole surface, unminified and minified); `examples/3d-core-node.js` is the witness, and it requires ONLY the core so a leaked dependency throws in SWCanvas's own repo
  - ⚠⚠ the homepage's instant-boot precompile had been SILENTLY INOPERATIVE on macOS since the machine stopped being WSL: the driver's whole body sat inside `if [[ "$(uname -r)" == *microsoft* ]]`, so `--homepage` shipped the `window.preCompiled = false` stub and still booted fine. A gate that can pass on the stub is not a gate — `fg homepage` now asserts `window.preCompiled === true`
  - ⚖ every dev boot was accumulating ~2.4 MB of compiled-JS string that nothing read (`JSSourcesContainer.content`); gating the two appends on `generatePreCompiledJS` took a normal boot to a measured 0 bytes
  - ⚠⚠ a 4-leg gauntlet failure that reproduced on EVERY serial retry was still not a regression: the same gate fails on a fully reverted tree (§12.8). `SystemTest_macroClosingRotatedIslandChildClearsFootprint` is the load-sensitive incremental-repaint canary — the SLOWER run is the one that fails. Do the revert-and-rebuild A/B early; reasoning about which code path "could" matter costs more than the ~8 min A/B
  - ⚠⚠ **decision D3 (no det-trig in the native bundle) STANDS, but its premise "it exists solely for SW cross-engine determinism" was FALSE** — the prelude also defined `DetTrig`, a global two source files named DIRECTLY (`HandleWdgt._pointerAngleToTargetAnchorDegrees`, `TransformSpec._cosSin`), so from arc 2 until 2026-07-30 rotating anything on `index.html` or a `--homepage` build threw `DetTrig is not defined`. Fixed by routing both call sites through `Math.*` — which the SW pages' prelude patches to the same fdlibm port before any class compiles, so gated pixels could not move (zero reference churn) — NOT by shipping det-trig natively. See the plan's D3 follow-up note
  - ⚖ **a bundle-content decision is also an API decision**: before calling a payload "backend-specific", grep `src/` for every global it defines — a prelude is not just bytes for the engine, it is part of the namespace every class compiles into
  - ⚖ **booting is not exercising**: a smoke test that only LOADS a page certifies nothing about the paths a user reaches. `smoke-boot-headless.js` now drives a real rotate gesture on both shipped entry pages
  - ⚖ retirements completed in-arc (doctrine): the WSL generator + its `configure-these-paths.sh`, the JSZip/`saveAs` drain, arc 1's homepage manifest stubs, and `remove_tests_link`'s real-directory branch

- **`build-arc-3-world-harmonization-plan.md`** — COMPLETE (2026-07-30). Arc 3 of the build-and-packaging program: ONE world design for homepage and dev/test, and the `»>>` region-exclusion mechanism retired ENTIRELY — 63 → 0 sites (promotes returned the full layout engine + real constants to the homepage; test machinery moved to five harness-side `*TestSupport` extension files; demo/dev content extracted to whole-file-marked collaborators `DemoMenus`/`PinoutsOverlay`/…; dead accretion deleted incl. `MouseSensorWdgt`/`PinType`/`ProfilingDataCollector`); all three build.py region regexes DELETED; `check-region-markers.js` holds every kind at baseline 0 as a HARD rule. Menu topology unified per the owner-ratified phase-7 sub-plan (below). 18 recaptures (dpr 1+2), suite 268 → 269, gauntlet 13/13 + homepage green.
  - ⚖ H-R3's `world.menuContributors` hook was deliberately NOT built — the class-existence collaborator pattern (`if DemoMenus?`; the file is whole-file-marked so the check is false in production) won on consistency with `world.widgetFactory`/`world.pinouts`, and it is exactly the right guard when arc 4 turns these files into lazily-loaded parts
  - ⚖ `MenusHelper` was 689 of 758 lines demo content — "extract the demo class" was really "extract the class"; the three shipping members stayed behind
  - ⚖ comments may MENTION the retired `»>>` mechanism as provenance (4 such mentions survive); the gate counts marker OPENERS, not the glyph

- **`build-arc-3-phase-7-menu-topology.md`** — COMPLETE (2026-07-30, committed with arc 3 phases 5–7). The owner-ratified ONE menu topology (three design calls made with the code in view; two owner follow-ups folded in: scrollbar spacing, keep BOTH duplication verbs). Its §0 is a mid-execution handoff snapshot kept verbatim — the status stamp carries the final truth.

- **`build-arc-4-dynamic-parts-plan.md`** — COMPLETE (2026-07-30). Arc 4 of the build-and-packaging program: turns "everything compiles at boot" into named, lazily-loadable **parts**. `SourceVault` (`src/boot/source-vault.coffee`, first in the boot bundle) became the ONE registry of source text, retiring the 499 `window.<Name>_coffeSource` globals and the `Object.keys(window)` suffix-scan that discovered them — and stopping the emission of 499 per-class source files nothing ever loaded (514 files/6.6 MB → 17/2.7 MB per dev build). `buildSystem/parts.json` now holds the WHOLE partition (10 parts, core included) and subsumed build.py's ~25 hand-maintained globs; 32 files moved into part directories; **all 45 whole-file exclusion-marker lines and all three build.py regexes are GONE** (`FILE_NOT_IN_FIZZYGUM_HOMEPAGE` 43, `FILE_ONLY_FOR_MACROS` 2, `FILE_ONLY_FOR_VIDEOPLAYER` **0 carriers — a live exclusion rule that had matched nothing for years**). Fizzytiles is the lazy pilot on `index.html`: engine + LCL compiler + the SW3D vendor payload all arrive on demand through `world.parts` (`PartsRegistry`), with `loadWorldSnapshot` gaining a pure pre-scan + tail re-entry so opening a file that contains a lazy part's widgets works. Three new gates at zero (`check-source-vault.js`, `check-whole-file-markers.js`, `check-part-edges.js`); two new headless rigs (`parts-lazy-load-headless.js`, `parts-snapshot-load-headless.js`) as the gauntlet's `parts` leg. Gauntlet 14/14 in-wave with ZERO reference churn (269 tests × dpr1/dpr2/webkit) + homepage green. Phase 3 (more lazy parts) declined by the owner — optional by construction, carried forward in `BACKLOG.md`.
  - ⚖ **INCLUSION ≠ EAGERNESS** (R6/P-D7) — the retired whole-file markers only ever expressed *does it ship*; a part needs that AND *is it loaded at boot*, as two independent axes (`inHomepage` in `parts.json` vs per-part `eager` + the per-entry-page `FIZZYGUM_EAGER_ALL_PARTS` preset). Keeping them separate is what let Phase 1 repartition the entire tree while being unable to move a pixel, and Phase 2 flip ONE part on ONE page
  - ⚖ **the `if X?` existence guard is right for INCLUSION and WRONG for LAZINESS** — for a part that was never shipped it means "silently no-op", which is correct; for a part that merely has not loaded yet it silently swallows the user's click. A lazy part's entry point must await `world.parts.ensureLoaded`. (A registration hook / part-declares-itself callback was considered and rejected: the plain class-existence guard is the tree's existing idiom, owner-confirmed)
  - ⚖ **an `extends` / `@augmentWith` edge from core into a part is not guardable at all** — unlike a construction site, there is no "skip it" branch, so it means the partition is drawn wrong. Two different verdicts from one check
  - ⚖ **parity must be MEASURED, not derived** (owner) — before the marker regexes could be deleted, a homepage-tree fingerprint (stored source NAMES + file list + sizes + SHA-256) had to match byte-for-byte, because "still 433 sources" can be true while the SET differs (a swapped pair nets to zero). It came out identical, 433 both sides. Arc 5's PR-D4 pattern, applied early. Corollary drawn when two batch *hashes* did move: "a batch changed" is also what an escaping bug looks like, so all 433 stored sources were decoded back against their `.coffee` files rather than argued about
  - ⚖ **don't write registry API ahead of its callers** — the dead-method gate rejected `SourceVault.partOf`/`forgetPart` on the first build. The right response was to DELETE them and grow the API in the phase whose code calls it, not to allowlist them
  - ⚠⚠ **all four execution bugs shared ONE shape: a rule encoded in two places.** (1) the class→part map read the `SourceVault`, which only knows sources whose batch already loaded — unusable for the lazy case that is its only reason to exist ⇒ it is build-manifest data; (2) "is this part eager here?" existed twice, and the copy the boot loader used ignored the entry-page override ⇒ every Fizzytiles SystemTest STALLED while the registry reported the part LOADED; (3) that one predicate then had to live in the boot BUNDLE, because a pre-compiled `--homepage` boot builds the world before the separately-fetched loader script lands — **`fg homepage` was the only gate that could catch it**; (4) the staleness census's battery reached a window that a lazy page no longer had
  - ⚠⚠ **the dependency scanner does NOT see `new X` in a method body** (only `extends`/`REQUIRES`/`@augmentWith` and 2-space-indented class-body field initialisers) — so the originally-planned "derive the core→part edge check from the dep scan" would have passed VACUOUSLY, since every real launch site is a method-body `new`. The shipped check is an identifier-level scan, with a documented deliberate blind spot for `new (window[name])` after `ensureLoaded` (the sanctioned lazy path) so a future session does not "fix" the hole
  - ⚠⚠ **`loadWorldSnapshot` is NOT async-shaped** — `result.whenReady` is an image-decode promise, not a completion hook. The parts await is a pure pre-scan + tail re-entry, and its ORDERING is the whole risk: it must precede `_teardownWorldStructureNoSettle`, so a REJECTED part load leaves the old world standing (asserted: 12 desktop children before, 12 after)
  - ⚠⚠ **anything the world's CONSTRUCTOR needs must be in the boot bundle**, not in a `js/src/*-min.js` file — those load later in the boot sequence, and a precompiled build has already built the world by then

- **`build-arc-5-packaging-profiles-plan.md`** — COMPLETE (2026-07-30). **The LAST arc of the build-and-packaging program.** A build FLAVOUR became DATA: `buildSystem/profiles/<name>.json` = `{parts, form, sources, entries}`, read by the one reader `buildSystem/buildProfile.py`, which DERIVES everything else. `--homepage` and `--notests` are DELETED (no alias — owner ruling), and with them ten `if $homepage` branches, the per-part `inHomepage` boolean, `requiresFlag: "tests"`, and the per-flavour prune list at the end of the build; `build_it_please.sh` now contains zero flavour branches. Four profiles ship: `dev`, `dev-notests`, `homepage` (production — precompiled, `sources: "lazy"`) and the new `lean` appliance (`core` only, `sources: "none"`, **10 files / 1.3 MB**). Production went **5.36 MB / 28 files → 3.64 MB / 26 files**, of which the 2.28 MB of class source text (62.7%) is no longer fetched unless someone opens an inspector. New: a `meta-tools` part (the inspectors, split out of core so a lean profile can drop them — the one non-core part production ships), `bootPrelude` as a part's fourth kind of belonging, and `Fizzygum-tests/scripts/build-tree-fingerprint.js` promoted out of a gitignored `.scratch/` into the PR-D4 parity gate. Current-state reference: **`docs/architecture/build-and-packaging.md`**.
  - ⚖ **A FIELD IS ONLY EARNED IF SOMETHING READS IT** — the authored schema had eight keys and shipped with four: `name` mirrored the filename, `extras.fontAssets` derives from the shipped entries, `extras.tests` derives from "does the `harness` part ship" (and deleting it also deleted `requiresFlag: "tests"`, which asked the same question twice), and `sources` was held back from phases 0+1 entirely and arrived in phase 2 WITH its consumer. Writing it early would have been the `FILE_ONLY_FOR_VIDEOPLAYER` species: a rule with no carriers
  - ⚖ **the two branches that would not derive were facts about a PART, not about a flavour** — `numbertimes.coffee` exists for fizzytiles (⇒ the new `bootPrelude` field), and the Automator dead-branch `sed` is valid exactly when the `harness` part is absent. After re-homing both, no part name appears in the build script at all
  - ⚖ **ONE READER, THREE CALLERS** — `build.py` imports it; the three shell scripts `eval` its `--shell` output; it owns `--profile` parsing too. `ENTRY_PAGES` moved OUT of build.py into the reader, because the shell needs its SWCanvas column to decide the SW bundle and the font assets. ⚠ `V=$(cmd)` then `eval "$V"`, never `eval "$(cmd)"` — the latter reports the EVAL's exit code, so a failed profile read would silently build a nameless flavour
  - ⚠⚠ **`build_it_please.sh` NEVER CHECKED `build.py`'s EXIT CODE, for years** — found by insisting the new hard-fail be PROVEN: a planted missing asset made build.py print its ERROR and exit 1 while `fg build` reported `BUILD EXIT=0 OK`. The build's most important step (wrapping every source, the parts manifest, the entry pages, the assets) could fail while the build printed `done!!!`. Every downstream gate passes because none depend on build.py's status
  - ⚠⚠ **the parity gate itself was LYING, and the same drill caught it** — its comparer keyed sources in a `Map` by NAME, but source names are NOT unique: `Class` and `Mixin` are each stored TWICE (their own individually-fetched source file for the compile bootstrap, plus a regular batch). The Map collapsed 502 → 500 AND the later entry overwrote the earlier, so a planted part change on `Class` was reported as *"identical"*. Fixed as a multiset of `(name, part)` + a parse-completeness self-check ⇒ **three verdicts: 0 equivalent / 1 differs / 2 INVALID**
  - ⚖ **A NEW FAILURE PATH IS NOT DELIVERED UNTIL IT HAS BEEN OBSERVED TO FAIL.** Both gates above were caught this way, and eleven new refusals were then planted-and-watched rather than reasoned about (retired flag, typo'd profile name, `--profile` with no value, a profile naming a nonexistent part — the one that matters at a RENAME, since it turns "production silently ships less" into a build failure — an unknown key, `sources` missing on a precompiled profile, `sources` present on a compile-at-boot one, an unknown `sources` value, `sources: "none"` with `meta-tools`, and `sources: "none"` with a LAZY part)
  - ⚖ **survey the OUTPUT tree, not the machinery** — §2.2 catalogued the pruning tail by reading the prune block, so it could only ever find things that were pruned. Fingerprinting a real production tree and asking of each file "who loads this?" found 1.99 MB more: `js/pre-compiled-max.js` (1.97 MB, 35% of the tree, loaded by NOTHING, kept alive by a `mv`) and an 18.9 KB software-3D vendor payload shipped to a tree that ships no fizzytiles, under a comment claiming it was "emitted for every flavour that ships the part" while its only condition was `[ -f <file> ]`
  - ⚠⚠ **under `sources: "none"` the artifact's own build CONSUMES what the policy removes** — the pre-compile driver harvests the image by booting the freshly-built tree at `?generatePreCompiled`, which is a COMPILE-AT-BOOT boot. Skipping the source-text emission ⇒ "pre-compiled generation failed"; skipping `dependencies-finding-min.js` (reachable ONLY from inside the layer, so it looked like a free saving) ⇒ the generation boot 404s mid-chain and surfaces minutes later as puppeteer's `protocolTimeout`, naming nothing. Both are build INPUTS, dropped after the driver — never skipped before it
  - ⚠ **`compileFGCode` is NOT part of the reflective layer**, though it is defined in a file the layer fetches: `ScriptWdgt`, `Widget.evaluateStringAsScript` and the spreadsheet's `FormulaCompiler` call it at runtime, in core, in every profile. Cutting the seam one file wider ships a lean build that dies the first time someone types a formula
  - ⚖ **a consumer awaiting a lazy resource must keep its already-loaded path SYNCHRONOUS** — `if reflectiveLayerIsLoaded() then open() else ensure().then open`. On every non-lazy build the layer is long since there, and deferring by even a microtask moves the effect a world CYCLE later, which the suite measures across the ~15 inspector tests. The obvious code (always return a promise) would have been the wrong code
  - ⚠⚠ **moving work off a boot path means auditing what ELSE that path was carrying** — the Automator manifests and `?startupActions=` sat in the ingest chain's TAIL, so `lazy`/`none` builds silently stopped firing them (⇒ a guarded `runPostBootActionsOnce()`). Same species as the `compileFGCode` trap, one level up
  - ⚖ **per-part source loading, MEASURED (owner question)** — for a LAZY part it already works that way (`PartsRegistry._loadPartPromise` fetches its batches then ingests them), and on a precompiled tree such a part's source IS its code, since the image is harvested by booting `index.html` where lazy parts do not load (hence the refusal of `sources: "none"` + a lazy part). For eager parts there is nothing to divide: **core is 80% of all source bytes**, so on production the split is 96% / 2%. The levers that pay are `lazy` and making MORE parts lazy
  - ⚠ **measurement corrected the plan four times, twice reshaping the arc** — part selection was ALREADY declarative (so the `parts` axis was a rename, not a mechanism); arc 3 produced class-existence GUARDS, not registration hooks; `sources: lazy` could NOT reuse arc 4's `ensureLoaded` (it filters to `not window[name]?`, empty on a precompiled tree, and compiles-and-executes, which would redefine live classes); and `--notests` shipped `macros`, so the planned "every part except harness/macros" would have been a silent behaviour change
  - ⚠ **a stale comment that names a dead flag costs a reader a moment; one that tells them to maintain a DELETED mechanism costs them an hour** — the phase-3 sweep took `src/` to zero `--homepage` references, but two sites were rewritten rather than word-swapped: `FittingSpecText` instructed the reader not to re-add a whole-file marker header (a mechanism arc 4 deleted and a gate now forbids), and `PatchNodeWdgt` asserted that two classes "keep their own exclusion markers" — false; they are in a part
  - ⚠ **headless probes must poll INSIDE the page, never `page.waitForFunction`** — Fizzygum extends `Object.prototype`, which breaks puppeteer's injected-utility argument path with a bare `Cannot read properties of undefined (reading 'consumerClassNames')` that names nothing you touched
  - ⛔ **DO NOT re-propose:** an omit/prune list per profile (owner-rejected even as a "first cut" — it is `build_it_please.sh:904` surviving under a new name); a `dev-notests` gauntlet leg (owner-rejected: the gauntlet is already 14 legs and that flavour is a by-hand check); compiler-less profiles (product-critical inventory); per-part precompiled chunks in v1; and moving the video flags into profiles (owner ruling: `--includeVideos` copies gigabytes from an external drive and `--keepPreviousPrivateVideos` exists so as NOT to re-copy them — that describes an invocation, not an artifact, so `requiresFlag` deliberately keeps its one carrier)

- **`shared-base-layer-part-plan.md`** — COMPLETE (2026-08-02). Gives the SHARED BASE LAYER a part of its own: nine classes no boot path reaches, that two or more LAZY parts DERIVE from, became the lazy **`app-kit`** part (`src/app-kit/` — `IconicDesktopSystemWindowedApp`, `ToolbarWdgt`, `ToolbarCreatorButtonWdgt`, `CreatorButtonWdgt`, `ToolPanelWdgt`, `GlassBoxTopWdgt`, `SpeechBubbleWdgt`, `ParentStainerMixin`, `WidgetCreatorAndSmartPlacerOnClickMixin`), which ELEVEN parts now declare `requires` on (nine of them because they DERIVE from it). `fg whatpins` **11 movable → 2**, reached-from-boot UNCHANGED at 202; `js/pre-compiled.js` **683,030 → 669,855 B (−13.2 KB, −1.93%)**, cumulative **−28.5%** since the program began. ZERO reference churn. Current-state reference: **`docs/architecture/build-and-packaging.md`** §2 (the shared-base-layer paragraph) and §5 (the measurement row + the two-that-stay decision).
  - ⚖⚖ **"WHO NAMES IT MOST" IS NOT "WHO OWNS IT", and pricing against the wrong home is what made these look immovable for two arcs.** `fg whatpins` proposes the part that MENTIONS a class most — `authoring` — and against that home every one of the nine is a bad trade: `plots`/`maps` would declare `requires: ["authoring"]`, so opening a chart or a map would first fetch 225.5 KB of Makers. The standing verdict "movable but none are free" was therefore a statement about the *candidate home*, not about the classes. 44 inheritance edges from 11 parts is the definition of a shared base; the answer is to INVENT the part (23.3 KB), not to pick a consumer
  - ⚖ **when several parts derive from one group, no existing part is a right answer by construction** — every choice makes the other consumers pay for a part they do not use. A tool that ranks candidate homes can only price the homes that exist, so read its "movable but not free" as a prompt to ask whether the right home has been invented yet
  - ⛔ **`CanvasWdgt` + `PatchNodeWdgt` (4.3 KB) STAY — blocked by RULE, not cost.** The EAGER `video-player` and `patch-programming-experimental` parts EXTEND them, and an eager part cannot derive from a lazy one at all (it runs from the first frame, so `requires` promises a part SHIPS but never that it ARRIVED). Unblocking is not packaging work: `video-player` is auto-launched AT BOOT (a product decision about a flag-gated draft feature) and `patch-programming-experimental` is entangled with core's dataflow wiring, where `ControllerMixin.ensureWireEdge` has no async seam. ⚠ `CanvasWdgt` is also defensible core material on layering merits alone — "no boot path reaches it" is not "it does not belong in core"
  - ⚖ **`requires` on the five `example-*` doors does NOT reverse their standing ⛔** — that prohibition bans naming the app's OWN parts (which would drag the app in behind the door instead of behind the click); this is the base class the door EXTENDS. `requiredParts` is awaited inside `launch`, long after the class had to be DEFINED, so only `requires` can order a base class. What a door drags in behind itself vs what it is MADE OF
  - ⚠ **deviation from the plan, and the plan told the executor to earn it**: §4.3 prescribed an `if SpeechBubbleWdgt?` guard at `dev-tools/WidgetFactory.coffee` and said to read the method first. Reading it made the guard wrong — `app-kit` is LAZY, and a guard answers "is it here?" when the question is "get it here", so the menu click would be swallowed. The site awaits, exactly like the two `demos` factories beside it
  - ⚠⚠ **GATE GAP FOUND AND FIXED IN THE SAME ARC: `check-part-edges.js` let an EAGER part's `requires` excuse its references AND its inheritance edges, contradicting its own header** (`declaredRequires` handed every owner its full list, and that set is tested BEFORE the inheritance check). Precisely the catastrophe the gate exists for, and §5.1 of this plan leaned on it. ⚖ The fix turns on a distinction worth keeping: **`requires` always promises the other part SHIPS, but promises it has ARRIVED only when the OWNER is LAZY** — so an eager owner's LAZY requirements are dropped and its EAGER ones kept (`harness requires ["macros"]` stays covered; `dev-tools`' two lazy ones are discounted and its awaits carry it). Proven both ways: planting an unguarded `new SpeechBubbleWdgt` and an `@augmentWith ParentStainerMixin` in `dev-tools` gave `0 / 0` before and a named failing site after
  - ⚖ **the prediction landed for once (−13.2 KB against a stated −12…−16 KB, +369 B of boot bundle against +250…450 B), and that is not a calibrated estimator** — it is one slice resembling the row above it. The half of the ritual that paid was "treat any unpredicted FILE as a finding": the fingerprint surfaced the source-batch repacking (one batch replaced, seven rewritten) that a size-only check reads straight past

- **`minimal-coffeescript-runtime-compiler-plan.md`** — COMPLETE + SHIPPED (2026-07-23). Forks the vendored in-browser CoffeeScript compiler down to a minimal compile-only build; shipped as the npm/GitHub package `fizzygum-coffeescript-min@1.0.0` and vendored into Fizzygum (`fc9750b5`, `auxiliary files/CoffeeScript/fizzygum-coffeescript-min.js`). Final 208,604 B (−18.9% vs stock 2.0.3), byte-identical emitted JS over all 4901 corpus fragments + full gauntlet green.
  - ⚖ sub-257 KB is INFEASIBLE on a 2.7.0 base (parser+nodes core alone = 278 KB > the whole 257 KB 2.0.3 bundle) — re-based on 2.0.3 (owner pivot), whose smaller core is the point; the §1/§5 2.7.0 mandate is superseded by §12
  - ⚖ "DEAD in V8 coverage" ≠ safe to cut — the error-reporting path is dead only because the corpus compiles clean yet fires on live-edit typos (KEEP); real language features unused-in-corpus stay usable (KEEP); only exotic tooling (sourcemap/JSX/import-export/literate/shebang/baseFileName) was cut
  - ⚖ the vendored file has THREE refs, not two — the plan AND memory undercounted, missing the runtime boot loader (`src/boot/globalFunctions.coffee`); grep the WHOLE tree before a rename, not just the build scripts
  - ⚖ byte-identity to STOCK 2.0.3 (not 2.7.0) is what keeps the pixel-exact gauntlet refs valid — the fork is frozen on 2.0.3; tracking upstream is an anti-goal, which is why it's an own-package derivative, not a CoffeeScript branch

## Process & workflow

- **`dev-workflow-optimization-plan.md`** — COMPLETE. Post-transcript-audit plan to parallelize the gauntlet, shard the paint audit, and add fg lint/status/recapture-inspector tooling.
  - ⚖ P7h arity assert FALSIFIED — a live 6-arg polymorphic add() contract exists, reverted not re-tuned
  - ⚖ P2 inspector churn driver is ctor-assigned INSTANCE FIELDS, not prototype methods
- **`backlog-closed-items-ledger.md`** — LEDGER (2026-08-18). The closed `BACKLOG.md` items whose owning arc never had a plan file: the naming-gloss audit's own ledger, the `SliderWdgt` hybrid-constructor decision and the analysis it superseded, the `add`-family 5th-slot hunt, `Widget.add`'s redundant signature default, and the serialization rig's frame-count capture fix. Verbatim, in their BACKLOG order.
  - ⚖ every OTHER closed item went to the `## BACKLOG ledger` section of the plan that owned it — check the owning plan first; this file is only for the homeless ones
  - ⚖ `BACKLOG.md` is an index of OPEN work only (README rule 2); a closed item there is a filing error, not a record

## Starting prompts (`archive/prompts/`)

- `PROMPT-simplify-layout-arc.md` — session starting prompt (from the umbrella root)
- `PROMPT-simplify-transforms-arc.md` — session starting prompt (from the umbrella root)
- `class-modernization-planning-starting-prompt.md` — Session-bootstrap prompt orchestrating the *Morph→*Wdgt class-rename campaign, batch by batch, plus the naming-consistency follow-up.
- `drag-embed-execution-starting-prompt.md` — Session-bootstrap prompt directing execution of the drag-embed dwell-to-arm implementation plan, phase by phase.
- `drop-into-rotated-container-starting-prompt.md` — Session-bootstrap prompt to implement F1 (pinned-anchor render-drift fix) atop the already-implemented §5 layout-transparency fix.
- `duplication-preserves-transforms-starting-prompt.md` — Paste-ready starting prompt handing a fresh session the duplication/save transform-preservation plan to implement cold.
- `serialization-execution-starting-prompt.md` — Copy-paste session-starter prompt for resuming the serialization arc at Phase 5/6 (whole-world snapshot, source-edit capture), now both landed.
