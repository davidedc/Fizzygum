You are picking up a series of "bring a class to latest" modernizations in the Fizzygum CoffeeScript GUI framework (workspace: /Users/davidedellacasa/code/Fizzygum-all/, with sibling git repos Fizzygum/ = source, Fizzygum-tests/ = the 160 macro SystemTests). You have NO prior context, so start by reading these, in order:

 1. The PLAYBOOK distilled from these arcs (process + gotchas, written to be reused): Fizzygum/docs/class-modernization-playbook.md — read it in full first.
 2. The two prior approved plans (read both — together they show a clean rename arc AND a "rejected target → pivot to the tractable one" arc):
    - /Users/davidedellacasa/.claude/plans/add-an-option-or-typed-squirrel.md (filename is stale — its CONTENTS are the InspectorMorph cleanup).
    - /Users/davidedellacasa/.claude/plans/you-are-picking-up-sparkling-galaxy.md (filename is stale — its CONTENTS are the inspect/inspect2 consolidation; its "Why this, and not StringMorph" and "Potential follow-ups" sections record the StringMorph/Text findings you should NOT re-derive).
 3. The macro-test subsystem docs: Fizzygum/src/macros/CLAUDE.md and Fizzygum/src/macros/MACRO-PATTERNS.md, plus the /author-macro-test skill in Fizzygum-tests. (An auto-memory note "InspectorWdgt windowed cleanup" should also surface — it summarizes both prior arcs' key findings.)

 WHAT IS ALREADY DONE (committed + pushed; do NOT redo):
 - Arc 1: DELETED legacy InspectorMorph, renamed InspectorMorph2 -> InspectorWdgt (+ ClassInspectorMorph -> ClassInspectorWdgt), made it windowed everywhere, re-authored its ~14 macro tests.
 - Arc 2: CONSOLIDATED the two inspector entry points — deleted the duplicate inspect2/spawnInspector2, kept inspect/spawnInspector as the single windowed entry point (repointed the "dev -> inspect" menu item), and dropped its homepage-exclusion guard so the one inspector ships in --homepage. Pixel-identical except macroDuplicatedInspectorDrivesCopiedTargetOnly (it screenshots the inspector's inherited-method list, which legitimately lost the two deleted Widget methods — references recaptured). Suite 160/160.

 KEY FINDING from Arc 2 (do not re-investigate from scratch): the originally-requested "delete StringMorph, rename StringMorph2 -> StringWdgt" is NOT a clean single "delete legacy + rename the *2 duplicate" arc. StringMorph and StringMorph2 are DIVERGENT ARCHITECTURES, not a redundant pair: incompatible constructor signatures (different arg order/count), and behaviour that tests deliberately depend on — StringMorph2 opens an "edit:" prompt when cropped, `currentlySelecting` is a method (it's a property on StringMorph), the caret uses gotoSlot vs gotoPos, plus fitting specs / alignment / undo. Two tests pick the OLD StringMorph ON PURPOSE (macroEditingStringInScrollablePanelCaretAlwaysVisible, macroScrollPanelCaretBroughtIntoViewWhenMoved). And TextMorph extends StringMorph (TextMorph2 extends StringMorph2), so the families move together. See the "Why this, and not StringMorph" section of plan #2 for specifics.

 YOUR TASK: produce a PLAN (do not implement yet) for the NEXT modernization, applying the playbook.

 PRIMARY TARGET — the String + Text family, planned HONESTLY as a multi-step arc (NOT a single mechanical rename): delete legacy StringMorph AND TextMorph, rename StringMorph2 -> StringWdgt and TextMorph2 -> TextWdgt (HhmmssLabelWdgt already sits on the modern base), making the modern widgets the only string/text widgets everywhere. Because of the divergences above, the plan MUST reconcile, not just rename: the constructor-signature differences at every `new StringMorph`/`new TextMorph` call site (~3 source + ~15 test fixtures for StringMorph; plus TextMorph sites), the edit-prompt-on-crop / currentlySelecting property-vs-method / gotoPos-vs-gotoSlot divergences, and the two tests that rely on old-StringMorph semantics (decide per-test: re-point to the modern widget with an equivalent fixture, or preserve the behaviour another way). Structure it in PHASES (e.g. StringMorph first, then TextMorph) with continuous verification, per the owner-workflow memory note.

 IF, on planning, the full String+Text arc is too large/risky to land safely even as a multi-step arc, instead recommend and plan the most tractable remaining playbook-§7 candidate, and explain why: the trivial dead-code deletion of StringMorph3 (homepage-excluded experiment, one demo-only construction site, no subclasses); a pure ListMorph -> ListWdgt rename (no legacy to delete — lower value, against the "don't mass-rename *Morph" convention without a reason); the button family is BLOCKED (MenuItemMorph extends the deprecated TriggerMorph). The inspect/inspect2 consolidation is DONE — do not propose it.

 In the plan, specifically:
 - Use Explore/Plan agents to (re)confirm the touch-list: every bare-identifier reference to StringMorph/StringMorph2/TextMorph/TextMorph2 across Fizzygum/src (extends/new/instanceof/findTopWidgetByClassNameOrClass — use \b word boundaries, StringMorph is a substring of StringMorph2/StringMorph3), the build hooks, serialization (constructor.name string checks, e.g. CaretMorph), the menu-demo string literals, and the homepage/precompiled paths — confirm the rename is name-extraction-only (no manifest), per playbook §1.
 - PRESENTATION/BEHAVIOUR CHECK (playbook §2): for each legacy call site, confirm the modern widget renders/behaves right when swapped in — and surface every behaviour divergence (edit-prompt-on-crop, currentlySelecting, caret API) as an explicit owner decision BEFORE test work, not a silent port.
 - Enumerate EVERY macro SystemTest that FUNCTIONALLY uses any of the four classes (not just prose) and classify each: relabel-only vs. constructor-swap-only-then-recapture vs. genuine re-author. Note the two tests that depend on old-StringMorph semantics need special handling. (Arc 2 already mapped these — start from plan #2's findings.)
 - Call out the playbook-§4 gotchas likely to bite for a text/string widget (TextMorph2 right-click menus not macro-drivable, editable-only-after-focus, softWrap, empty-text click crash, caret/selection, hover/determinism, inherited-method lists in inspector shots changing when a method set changes, the --clean --no-build trap) and how each affected test will handle them.
 - Make the plan SELF-CONTAINED for a future no-context session (Orientation + Glossary like the prior plans), list the source touch-list and the test list per phase, give the verification steps (build_and_smoke, per-test capture at dpr 1&2, build_and_test = 160/160, a --homepage build+boot if any homepage-scope changes), and a "Potential follow-ups" section.
