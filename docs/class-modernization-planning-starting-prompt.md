DEFERRED FOLLOW-UP (from the FIT_BOX_TO_TEXT arc, Arc 6 — pick up if/when you want it; not blocking): A bare FBT TextWdgt reflows on a container resize but not on its own setText — that trigger stays on SimplePlainTextWdgt (noted in its header + the auto-memory `textwdgt-fit-box-to-text`). A clean follow-up if you ever want full parity.

You are picking up a series of "bring a class to latest" modernizations in the Fizzygum CoffeeScript GUI framework (workspace: /Users/davidedellacasa/code/Fizzygum-all/, with sibling git repos Fizzygum/ = source, Fizzygum-tests/ = the 160 macro SystemTests). You have NO prior context, so start by reading these, in order:

 1. The PLAYBOOK distilled from these arcs (process + gotchas, written to be reused): Fizzygum/docs/class-modernization-playbook.md — read it in full first (its §7 records the arcs that have closed).
 2. The prior approved plans (read them — together they show a clean rename arc, a "rejected target → pivot to the tractable one" arc, a dead-code-deletion arc, a prerequisite-first chrome-migrate-then-delete-and-rename arc, and a delete-deprecated-then-restructure-a-family arc):
    - /Users/davidedellacasa/.claude/plans/add-an-option-or-typed-squirrel.md (filename is stale — its CONTENTS are the InspectorMorph cleanup).
    - /Users/davidedellacasa/.claude/plans/you-are-picking-up-sparkling-galaxy.md (filename is stale — its CONTENTS are the inspect/inspect2 consolidation).
    - /Users/davidedellacasa/.claude/plans/stringmorph3-dead-code-deletion.md (the StringMorph3 deletion arc).
    - /Users/davidedellacasa/.claude/plans/you-are-picking-up-streamed-yeti.md (filename is stale — its CONTENTS are the String/Text arc: migrate the menu/button/tooltip CHROME onto the modern text family, THEN delete StringMorph/TextMorph and rename StringMorph2 -> StringWdgt / TextMorph2 -> TextWdgt).
    - /Users/davidedellacasa/.claude/plans/triggermorph-deletion-menuitem-fold-in.md (filename is stale re: "fold-in" — the executed route was RE-BASE: delete deprecated TriggerMorph, re-base MenuItemMorph onto the modern button family KEEPING the flat menu look, then a follow-on owner-requested refactor renamed the button-family base and extracted a shared base. Read its owner-decision-up-front handling of the "may menu pixels change?" question — that is the template for the menu-pixel decision the arc now recommended below will face.)
 3. The macro-test subsystem docs: Fizzygum/src/macros/CLAUDE.md and Fizzygum/src/macros/MACRO-PATTERNS.md, plus the /author-macro-test skill in Fizzygum-tests. (Two auto-memory notes should also surface: "TriggerMorph deletion / button-family modernization" — the arc just closed — and "String/Text Wdgt modernization" — the arc before it. They summarize the key findings so you don't re-derive them.)

 WHAT IS ALREADY DONE (committed + pushed; do NOT redo):
 - Arc 1: DELETED legacy InspectorMorph, renamed InspectorMorph2 -> InspectorWdgt (+ ClassInspectorMorph -> ClassInspectorWdgt), made it windowed everywhere, re-authored its ~14 macro tests.
 - Arc 2: CONSOLIDATED the two inspector entry points — deleted the duplicate inspect2/spawnInspector2, kept inspect/spawnInspector as the single windowed entry point, dropped its homepage-exclusion guard. Suite 160/160.
 - Arc 3: DELETED the dead StringMorph3 "Morph3" experiment (a stale, feature-stripped FORK of StringMorph2) and the sibling dangling TextMorph3 demo plumbing. Suite 160/160.
 - Arc 4 (the String/Text arc): DELETED legacy StringMorph and TextMorph; renamed StringMorph2 -> StringWdgt and TextMorph2 -> TextWdgt; and FIRST migrated the menu/button/tooltip CHROME onto the modern family so the delete was unblocked. Two pushes, suite 160/160 at dpr 1 and 2.
 - Arc 5 (the TriggerMorph / button-family arc — the one that used to be "the standing goal" of this very prompt; now CLOSED): DELETED the deprecated TriggerMorph and modernized the button family. Verified current reality (file:line):
     * Final button hierarchy: `Widget -> ButtonWdgt -> { SimpleButtonWdgt, LabelButtonWdgt -> { MenuItemMorph, MagnetWdgt } }`.
     * RENAMES (Morph->Wdgt): EmptyButtonMorph -> **ButtonWdgt** (`src/ButtonWdgt.coffee`; the abstract button base — "a labeled button is-a Button", not "is-a EmptyButton"); SimpleButtonMorph -> **SimpleButtonWdgt** (`src/SimpleButtonWdgt.coffee`); MagnetMorph -> **MagnetWdgt** (`src/fizzytiles/MagnetWdgt.coffee`). A `\b`-bounded grep of Fizzygum/src finds the old names only in historical COMMENTS.
     * NEW shared base **LabelButtonWdgt** (`src/LabelButtonWdgt.coffee`, `extends ButtonWdgt`) holds the generic flat-label-button machinery (label fields, the flat `paintIntoAreaOrBlitFromBackBuffer` keyed on STATE_*, `reLayout`/`setLabel`/`alignCenter`/`alignLeft`, the mouse handlers, the ButtonWdgt-constructor mapping, and a DEFAULT single-line `StringWdgt` createLabel). It inherits the target/action/trigger machinery + HighlightableMixin STATE_* from ButtonWdgt but KEEPS the flat fill (menuBackgroundColor / SILVER hover / GRAY press) via its own retained paint — the button family supplies NO flat fill (ButtonWdgt body is transparent; SimpleButtonWdgt is a rounded BoxyAppearance box). This is the role the deleted TriggerMorph filled, now non-deprecated and on the modern family. Menus are pixel-IDENTICAL.
     * `MenuItemMorph extends LabelButtonWdgt` (`src/basic-widgets/menu-system/MenuItemMorph.coffee:9`) — keeps ONLY the menu-specific bits (self-sizing multi-line TextWdgt createLabel, tick toggling, list-row selection, representsAMorph hover-highlight, getTextDescription). `MagnetWdgt extends LabelButtonWdgt` (no label/paint code of its own).
     * **MenuItemMorph was deliberately NOT renamed.** Renaming it would change the `"a MenuItemMorph ➜"` hierarchy-nav LABELS (the Arc-4 "menus strip Wdgt" rule: a *Wdgt class shows Wdgt-stripped) and recapture the menu tests that photograph that label. That deferral is exactly the load-bearing decision for the arc recommended below.
     * Reconciliation traps now documented in the auto-memory note and LabelButtonWdgt.coffee comments: (1) HighlightableMixin.updateColor calls setColor → would clobber @color (the flat fill), so mouseDownLeft replicates Widget.mouseDownLeft INLINE instead of calling super; (2) the mixin's mouseUpLeft resets state→NORMAL → would break list-row selection (isSelectedListItem reads STATE_PRESSED), so it's overridden to a no-op; (3) ButtonWdgt.doLayout lays out a faceMorph a label button doesn't have → doLayout calls `Widget::doLayout.call @, …`; (4) the ButtonWdgt ctor's slot-4 is faceMorph vs labelString — the LabelButtonWdgt ctor maps its 17 args onto it (faceMorph=nil, its "environment" arg -> dataSourceMorphForTarget).
     * Tests: ZERO reference-image churn across the whole arc. The only 2 changed tests (macroBareButtonFloatDragsWithoutTriggering, macroEditButtonLabelText) both build `new MenuItemMorph` now (the faithful fixture — the original recordings tore off a real menu item; a bare MenuItemMorph keeps the flat big centred label) and render byte-identical to the original refs. macroEditButtonLabelText re-hugs its label after each inline edit (`button.label.sizeToTextAndDisableFitting()`) so the modern TextWdgt label keeps its set font size instead of scaling a longer typed caption down. A reusable `Fizzygum-tests/scripts/make-before-after-comparison.js` was added (diffs committed-HEAD refs vs the working tree into one HTML).
 - Arc 6 (the content-text reflow / FIT_BOX_TO_TEXT arc — ROADMAP #1 below, just CLOSED): made a BARE `TextWdgt` re-wrap + auto-grow/shrink its height as window/panel/scroll CONTENT (like `SimplePlainTextWdgt` already did), done PROPERLY — not the cheap "widen `instanceof SimplePlainTextWdgt` + propagate `maxTextWidth`". WIRED the pre-scaffolded `FittingSpecText` fitting MODE (`fittingSpec` FIT_TEXT_TO_BOX/FIT_BOX_TO_TEXT + tight/loose + which-dimension sub-axes) into `StringWdgt` (+ `createBufferCacheKey`), `TextWdgt::reLayout`/`rawSetExtent`, and `reflowText`; RETIRED the dead-`TextMorph` `maxTextWidth` knob (→ `softWrap` + the mode) and REMOVED the 3 `instanceof SimplePlainTextWdgt` render leaks; moved the contained-reflow engine onto `TextWdgt::reLayout` gated by the mode, leaving `SimplePlainTextWdgt` a thin mode-specialization; the 3 content sites (`WindowWdgt`/`SimpleVerticalStackPanelWdgt`/`ScrollPanelWdgt`) now RESPECT the mode (`if morph.fittingSpec == FIT_BOX_TO_TEXT`), NOT impose it on `instanceof TextWdgt` (which would catch the empty-window placeholder `WindowContentsPlaceholderText extends TextWdgt`). Two completion fixes the NEW bare-TextWdgt tests exposed (both byte-identical for SPTW, which dodged them via its ctor): `reflowText` must SHORT-CIRCUIT `fitToExtent` for the mode (else SCALEUP's font search blows the font up), and `WindowWdgt.contentsRecursivelyCanSetHeightFreely` must be false for the mode (else the window follows the dragged height — no auto-SHRINK on widen). Suite 162/162 (Chrome + WebKit), `--homepage` boots (the 3 enums were de-excluded from the homepage strip, since the base now references them in production). The ONLY recapture: 3 inspector introspection tests (`macroAddEditSaveRenameRemoveProperty`, `macroInspectorScrollbarUnplugged`, `macroMovingSlidersSidewaysDoesntCauseContentToMoveSideways`) — adding the 3 `fittingSpec*` props to `StringWdgt` makes the Object Inspector's StringWdgt property LIST 3 rows taller; confirmed inherent (a literal-default build rendered byte-identical → NOT load-order), owner-OK'd via a pixel-diff. New tests: `macroBareTextWdgtAsWindowContentReflowsOnResize` (window; round-trip asserted via TWO resizes to the SAME width — drop-vs-resize is a different sizing path) + `macroBareTextWdgtInVerticalStackReflows` (stack); 6 existing macros migrated `maxTextWidth`->`softWrap`. SCOPE BOUNDARY left (documented): a bare FBT `TextWdgt` reflows on a container RESIZE but not on its own `setText` (that trigger stays on SPTW). Full record: playbook §7 + auto-memory `textwdgt-fit-box-to-text`.

 ====================================================================
 SERIES PHASE CHANGE: the "delete a legacy/deprecated class" phase is COMPLETE. What remains is a little non-rename TECHNICAL cleanup FIRST, then the (WANTED) incremental Morph -> Wdgt MIGRATION done LAST.
 ====================================================================
 A fresh survey (re-confirm with greps) finds NO remaining candidates of the DELETION kind the first five arcs fed on:
   - NO `*Morph2` / `*Morph3` modern-duplicate pairs remain (Inspector, String, Text, the Morph3 fork — all deleted/renamed).
   - NO live deprecated-class-with-a-living-successor remains (TriggerMorph was the last; the only "deprecated" hits in src now are WorldMorph's JS-keypress-event comments and historical prose about TriggerMorph).
 The stated end state is all-`*Wdgt` (Fizzygum/CLAUDE.md, playbook §7): `Morph` is legacy and the `*Morph` -> `*Wdgt` migration is WANTED — done DELIBERATELY and INCREMENTALLY, a coherent family at a time, NOT in one sweep. **Owner ordering decision (settled): do the renaming LAST.** Clear the substantive non-rename work first; then run the rename batches. So the renaming question is never "should we?" (yes — eventually all of it) but "which batch, and is its menus-strip-`Wdgt` recapture cost accepted?" — and it comes AFTER the technical items.

 ROADMAP (in order; renaming LAST):
   1. Content-text reflow — generalize `maxTextWidth`/`softWrap` to bare `TextWdgt` content.   ✅ DONE (Arc 6, above).
   2. Naked-inspector fix — make InspectorWdgt render/function/RESIZE correctly WITHOUT a window (keep+wire its `@resizer`; add a naked-inspector test); + unify the two `save` paths.   ← THE STANDING GOAL (next arc)
   3. (TextWdgt text shadows — ONLY if a concrete caller appears; none today, so not an arc yet.)
   — then the renaming / migration phase, LAST —
   4. Button-family `*Morph` -> `*Wdgt` completion (icon/rect buttons ± MenuItemMorph).
   5. ListMorph -> ListWdgt.
   6. RectangleMorph -> RectangleWdgt.
   7. The long tail of remaining `*Morph` -> all-`*Wdgt`.

 ====================================================================
 THE STANDING GOAL NOW (the next arc, ROADMAP #2): make the NAKED (chrome-less) InspectorWdgt a first-class, self-resizable widget.
 ====================================================================
 (ROADMAP #1 — content-text reflow — is ✅ DONE: see Arc 6 in "WHAT IS ALREADY DONE" above, and playbook §7 + the auto-memory note `textwdgt-fit-box-to-text` for the full record + lessons. The how-it-works reference is the FITTING MODEL comment in `StringWdgt.coffee`. The load-bearing behaviour question it carried — "does every TextWdgt-as-content auto-wrap, or only when opted in?" — was SETTLED: the content sites RESPECT the mode, i.e. a TextWdgt opts IN by being set to FIT_BOX_TO_TEXT, exactly so the empty-window placeholder is left alone.)

 THE STANDING GOAL is now ROADMAP #2, detailed here:

 #2 — Make the NAKED (chrome-less) InspectorWdgt a first-class, self-resizable widget. **This is a real fix, and it REVERSES the old "drop the resizer" idea** (do NOT drop the resizer): today the inspector only renders correctly INSIDE a `WindowWdgt`. `spawnInspector` (`Widget.coffee:2855`) always wraps it in a 560×410 window, commented "built to live inside a window … opening it 'naked' leaves its panes badly cramped" (`:2853-2854`); and the `@resizer = new HandleMorph @` (`InspectorWdgt.coffee:306-311`) is parked under a TODO calling it "not needed because the inspector is always created inside a window … leaving it … because the tests are delicate". The owner wants the InspectorWdgt to ALSO render correctly, be functional, AND be resizable ON ITS OWN (no window chrome). So:
      - KEEP and wire up the `@resizer` — it is NOT dead code; it is exactly what a naked inspector needs to be resizable.
      - FIX the naked layout/paint so the panes are sized + the background painted correctly without a window (find WHY naked = "cramped panes" — likely the inspector's `doLayout`/background assumes the window supplies its extent + chrome; `InspectorWdgt extends Widget`, `colloquialName` at `:59`). The windowed `spawnInspector` path STAYS; this ADDS a supported, tested naked path (it does not remove the window option).
      - ADD a SystemTest for the naked inspector (the owner asked for this explicitly): `world.add new InspectorWdgt target` with NO window, screenshot it (renders correctly, not cramped), resize it via its `@resizer` (`@dragResizeMoveHandleTo_InputEvents` — select the handle by type), and edit a property to prove it's functional. This also finally gives the long-"delicate" resizer real coverage, retiring the TODO.
      - Small RELATED cleanup that can ride along: unify the two `save` methods (`InspectorWdgt.coffee:541` `@target.injectProperty …` vs `ClassInspectorWdgt.coffee:36-41` `@target.evaluateString "@"+propertyName+" = "+txt`).
    (This grew from "trivial dead-code removal" into a real render-fix + new test; the owner may promote it ahead of #1 if the naked-inspector defect matters more than the content-text gap.)

 #3 — TextWdgt text SHADOWS: Arc 4 dropped the old TextMorph's shadowOffset/shadowColor; NO current site needs them, so do this ONLY if a concrete caller appears. Not an arc yet.

 ====================================================================
 THE RENAMING / MIGRATION PHASE (LAST) — the `*Morph` -> `*Wdgt` batches, AFTER the above. Do NOT start until the owner says the non-rename work is done.
 ====================================================================
 The migration is WANTED (end state all-`*Wdgt`); these are the batches, in rough order. Each is its own arc; the recurring load-bearing decision is the menus-strip-`Wdgt` recapture cost. (Findings recorded here so the future session needn't re-derive them when this phase begins.)

 BATCH 4 — complete the button family Arc 5 began (the most-set-up rename batch):
   - IN-FAMILY `*Morph` leftovers (extend ButtonWdgt or a descendant — reconfirm each `extends`): `MenuItemMorph extends LabelButtonWdgt` (32 src refs; 27 test files — the EXPENSIVE one); `SimpleRectangularButtonMorph extends ButtonWdgt` (3) + `CodeInjectingSimpleRectangularButtonMorph extends SimpleRectangularButtonMorph` (8); `CloseIconButtonMorph` (5), `CollapseIconButtonMorph` (3), `UncollapseIconButtonMorph` (2) — all `extends ButtonWdgt` (siblings of the already-Wdgt EditIconButtonWdgt / InternalIconButtonWdgt / ExternalIconButtonWdgt).
   - PROBABLY a SEPARATE lineage (confirm before including): `ToggleButtonMorph` (11) `extends SwitchButtonMorph` (6); `SliderButtonMorph` (13, the slider thumb); `RadioButtonsHolderMorph`; `UpperRightTriangleIconicButton`.
   - The icon/rect renames are almost certainly pixel-neutral IDENTIFIER swaps (a `\b`-bounded sweep of extends/new/instanceof/findTopWidgetByClassNameOrClass + the filename; no manifest) — VERIFY none is photographed in a hierarchy menu (the menus-strip-`Wdgt` rule makes a drawn colloquial name shift). `MenuItemMorph -> MenuItemWdgt` is the one that MOVES PIXELS: `"a MenuItemMorph ➜"` nav strings become `"a MenuItem ➜"`, menu/hierarchy tests that PHOTOGRAPH that label re-baseline, and `instanceof MenuItemMorph` sites (GlassBoxBottomWdgt, HorizontalMenuPanelWdgt, MenuMorph) + ~27 test files reference the identifier — the Arc-4 menu-churn pattern. Scope the batch to EXCLUDE MenuItemMorph (cheap icon/rect only) or INCLUDE it (accept the recapture) — explicit owner decision UP FRONT (the streamed-yeti / triggermorph "may menu pixels change?" template).

 BATCH 5 — ListMorph -> ListWdgt: a clean rename — `class ListMorph extends ScrollPanelWdgt`, NO subclasses, sole real construction site `InspectorWdgt.coffee:239` (`new ListMorph`) + a couple of standalone test fixtures (MACRO-PATTERNS.md:711,740). Same menus-strip caveat if photographed. The lowest-risk batch.

 BATCH 6 — RectangleMorph -> RectangleWdgt (surfaced as a candidate in the inspector arc; confirm touch-list).

 BATCH 7 — the long tail of remaining `*Morph` classes -> all-`*Wdgt`, family by family, until the codebase is all-`*Wdgt`.

 YOUR TASK: produce a PLAN (do not implement yet) for the NEXT arc — **ROADMAP #2, the naked (chrome-less) InspectorWdgt fix** (detailed just above) — applying the playbook. Structure it in PHASES with continuous verification (per the owner-workflow memory note: run straight through, ONE end-of-arc review; commit/push only after that review). The renaming phase (BATCHES 4-7) stays documented below for when the technical items are cleared — do NOT start it until the owner says the non-rename work is done.

 In the plan, specifically:
 - Use Explore/Plan agents to confirm the touch-list (reconfirm, don't re-derive): WHY the naked InspectorWdgt renders cramped/unpainted (its `doLayout`/background likely assumes the window supplies the extent+chrome — `InspectorWdgt.coffee`, `colloquialName` ~:59), the parked `@resizer = new HandleMorph @` (`InspectorWdgt.coffee:306-311`, the "not needed … delicate tests" TODO to retire), the always-windowed `spawnInspector` wrap (`Widget.coffee:2855`, which STAYS), and the two `save` paths to unify (`InspectorWdgt.coffee:541` `injectProperty` vs `ClassInspectorWdgt.coffee:36-41` `evaluateString`).
 - PRESENTATION/BEHAVIOUR CHECK (playbook §2): `world.add new InspectorWdgt target` with NO window and LOOK at it — confirm exactly WHAT is cramped/unpainted, and decide the naked-layout fix WITH the owner (the windowed `spawnInspector` path is unchanged; this ADDS a supported naked path).
 - Enumerate the affected/new macro SystemTests: the existing WINDOWED-inspector tests must stay byte-identical (that path is untouched — verify, zero recapture); ADD a naked-inspector test (renders correctly + resize via `@resizer` with `@dragResizeMoveHandleTo_InputEvents` + edit a property to prove it's functional), which also finally covers the long-"delicate" resizer.
 - Call out the playbook-§4 gotchas (the capture `--clean` deletes-both-densities trap; run-macro-test-headless not rebuilding; a green capture ≠ a correct test — eyeball; hover/determinism after a resize drag — park the pointer at a neutral spot before the shot).
 - Make the plan SELF-CONTAINED for a future no-context session (Orientation + Glossary like the prior plans), list the source touch-list and the test list per phase, and give verification steps: build_and_smoke, per-test capture at dpr 1 AND dpr 2 (only for the ones that actually move), build_and_test = 162/162, a WebKit cross-engine run, and a --homepage build+boot if any touched code ships there. Add a "Potential follow-ups" section: text-shadows (#3) and THE RENAMING PHASE (BATCHES 4-7) above. Note that the legacy-DELETION phase is complete, the content-text reflow (#1) is DONE, the naked-inspector (#2) is THIS arc, and the incremental `*Morph`->`*Wdgt` MIGRATION toward an all-`*Wdgt` codebase is deliberately LAST.
