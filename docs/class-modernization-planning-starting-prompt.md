You are picking up a series of "bring a class to latest" modernizations in the Fizzygum CoffeeScript GUI framework (workspace: /Users/davidedellacasa/code/Fizzygum-all/, with sibling git repos Fizzygum/ = source, Fizzygum-tests/ = the 165 macro SystemTests). You have NO prior context.

START BY READING (process, conventions, and the full record of the closed arcs):
 1. The PLAYBOOK — Fizzygum/docs/class-modernization-playbook.md — read it in full first. Its §7 records every arc that has CLOSED (the worked examples + lessons), and §4 is the recurring mechanics/gotchas you must reuse.
 2. The macro-test subsystem docs — Fizzygum/src/macros/CLAUDE.md and Fizzygum/src/macros/MACRO-PATTERNS.md — plus the /author-macro-test skill in Fizzygum-tests. Read Fizzygum-tests/DETERMINISM.md before touching any input/layout/render path.
 3. The project auto-memory notes (they summarize each closed arc's key findings so you don't re-derive them) and the prior approved plans in /Users/davidedellacasa/.claude/plans/ — in particular triggermorph-deletion-menuitem-fold-in.md + batch4-button-family-rename-completion.md, whose owner-decision-UP-FRONT handling of "may menu pixels change?" is the TEMPLATE for the menu-pixel decision every rename batch faces.

WHAT IS ALREADY DONE (committed + pushed to master; do NOT redo — full per-arc record lives in playbook §7 + the auto-memory notes):
 - The "delete a legacy/deprecated class" phase is COMPLETE. No `*Morph2`/`*Morph3` modern-duplicate pairs and no live deprecated-class-with-a-living-successor remain (Inspector, String/Text, the Morph3 fork, TriggerMorph — all deleted/renamed). The button family was modernized to `Widget -> ButtonWdgt -> { SimpleButtonWdgt, LabelButtonWdgt -> { MenuItemWdgt, MagnetWdgt } }` (the `*Morph` leaf names were renamed to `*Wdgt` in BATCH 4).
 - The non-rename TECHNICAL-CLEANUP phase is COMPLETE: #1 bare-`TextWdgt` content reflow / FIT_BOX_TO_TEXT; #2 naked, first-class, self-resizable `InspectorWdgt`; #3 TextWdgt text shadows — CLOSED-SUBSUMED (the per-glyph baked shadow was deliberately NOT built; the ONE unified widget drop-shadow already shadows text, owner principle "no widget bakes its own shadow into its back-buffer"; see auto-memory `fizzygum-unified-shadow-mechanism`).
 - Suite is at 165/165 (Chrome + WebKit, dpr 1 + 2); `--homepage` builds + boots.

WHAT REMAINS: the WANTED `*Morph` -> `*Wdgt` RENAMING / MIGRATION phase — the end state is all-`*Wdgt` (Fizzygum/CLAUDE.md + playbook §7). Done DELIBERATELY and INCREMENTALLY, a coherent family at a time, NOT in one sweep. **This phase is UNDERWAY: BATCH 5 (ListMorph→ListWdgt), BATCH 4 (button-family completion — 6 classes incl. MenuItemMorph→MenuItemWdgt), AND BATCH 6 (RectangleMorph→RectangleWdgt + HighlighterMorph→HighlighterWdgt) are all DONE (2026-06-16, recorded in playbook §7). BATCH 7 (the long tail) is UNDERWAY — its FIRST family, the **Icon family** (`IconMorph`→`IconWdgt` + 16 `*Morph` leaf icons; all 93 `extends IconMorph` re-pointed, incl. 77 already-`*Wdgt` children), is DONE 2026-06-16 (playbook §7; plan `~/.claude/plans/batch7-icon-family-rename.md`). The rest of the long tail (`BoxMorph`, `CircleBox`/`Slider`, Color palette, Switch/Toggle, `HandleMorph`, …, `WorldMorph` last) remains.**
 - **Owner ordering decision (settled): the renaming is LAST, and is the remaining work — now IN PROGRESS (BATCH 5 done).** The question is never "should we?" (yes — eventually all of it) but "which batch NEXT, and is its menus-strip-`Wdgt` recapture cost accepted?". Batches are largely independent, so the owner may take them out of the rough order below — BATCH 5 was done before BATCH 4 (both now complete).
 - **The recurring load-bearing decision per batch:** a rename is usually a pixel-neutral `\b`-bounded identifier swap (extends / new / instanceof / findTopWidgetByClassNameOrClass + the filename; there is no manifest) — UNLESS the class's colloquial name is DRAWN in a hierarchy/menu nav label, because menus strip `Wdgt` (so `"a FooMorph ➜"` becomes `"a Foo ➜"`), which MOVES pixels and forces recapture of every test photographing that label. Verify per class; settle the recapture cost with the owner UP FRONT (the triggermorph "may menu pixels change?" template). **Refined by BATCH 5: "label-shifting" ≠ "recapture" — only a PHOTOGRAPHED label re-baselines. ListWdgt's menu label DID shift (`"a ListMorph"`→`"a List"`) but no test screenshots that menu, so recapture was ZERO. Don't pre-classify from "is the name drawn?"; rename, run the byte-exact suite, and the RED set IS the recapture set.**
 - **Do NOT start the next batch until the owner explicitly says which one to begin.**

THE BATCHES (rough order; touch-lists recorded so the future session needn't re-derive them — reconfirm with greps):

BATCH 4 — complete the button family: ✅ **DONE (2026-06-16)** — full record in playbook §7 + plan `~/.claude/plans/batch4-button-family-rename-completion.md`. MenuItemMorph WAS included (the recorded "≈27 test files / ≈32 src refs" was a large OVERCOUNT — actual: 9 tests, ~37 src hits incl. 4 `instanceof`, zero string-literals); the separate `*Button*` lineages WERE deferred to BATCH 7; exactly 1 recapture (`macroRightClickClosesDownstreamSubMenus`). The detail lines below are the pre-execution spec, kept for provenance:
   - IN-FAMILY `*Morph` leftovers (reconfirm each `extends`): `MenuItemMorph extends LabelButtonWdgt` (≈32 src refs; ≈27 test files — the EXPENSIVE one); `SimpleRectangularButtonMorph extends ButtonWdgt` (3) + `CodeInjectingSimpleRectangularButtonMorph extends SimpleRectangularButtonMorph` (8); `CloseIconButtonMorph` (5), `CollapseIconButtonMorph` (3), `UncollapseIconButtonMorph` (2) — all `extends ButtonWdgt` (siblings of the already-Wdgt EditIconButtonWdgt / InternalIconButtonWdgt / ExternalIconButtonWdgt).
   - PROBABLY a SEPARATE lineage (confirm before including): `ToggleButtonMorph` (≈11) `extends SwitchButtonMorph` (≈6); `SliderButtonMorph` (≈13, the slider thumb); `RadioButtonsHolderMorph`; `UpperRightTriangleIconicButton`.
   - The icon/rect renames are almost certainly pixel-neutral identifier swaps — VERIFY none is photographed in a hierarchy menu. `MenuItemMorph -> MenuItemWdgt` is the one that MOVES PIXELS: `"a MenuItemMorph ➜"` nav strings become `"a MenuItem ➜"`, menu/hierarchy tests that PHOTOGRAPH that label re-baseline, and `instanceof MenuItemMorph` sites (GlassBoxBottomWdgt, HorizontalMenuPanelWdgt, MenuMorph) + the ≈27 test files reference the identifier. Scope the batch to EXCLUDE MenuItemMorph (cheap icon/rect only) or INCLUDE it (accept the recapture) — explicit owner decision UP FRONT.

BATCH 5 — ListMorph -> ListWdgt: ✅ **DONE (2026-06-16)** — exactly as scoped: a leaf class (no subclasses, no legacy split), sole construction site `InspectorWdgt.coffee` (`new ListWdgt`); 6 source refs + the filename, zero string-literals; `MenuMorph`/`MenuItemMorph` are composed-in (not subclasses), out of scope. The menus-strip label DID shift (`"a ListMorph"`→`"a List"`) but is never PHOTOGRAPHED (the one attach-menu test shoots only before/after-attach/after-wheel), so **ZERO recapture**: 165/165 (Chrome dpr 1+2, WebKit), `--homepage` boots. Full record + lessons: playbook §7.

BATCH 6 — RectangleMorph -> RectangleWdgt: ✅ **DONE (2026-06-16)** — renamed the base + its `*Morph` subclass `HighlighterMorph`→`HighlighterWdgt` (zero test refs, pixel-neutral); re-pointed the already-`*Wdgt` `VideoControlsPaneWdgt`'s `extends`. The FIRST batch whose renamed class is genuinely PHOTOGRAPHED (the rect is the go-to FIXTURE + test SUBJECT): the byte-exact suite went RED on **8** tests — neither the coarse menu agent's "8 photograph the label" nor the precise per-screenshot "2 open-in-shot" prediction; the RED set IS the recapture set, a 3rd proof. The 8 exercise all THREE label regimes (menu strip → `a Rectangle`; PromptMorph title strip → `Rectangle alpha value:`; Object-Inspector hierarchy DIAGRAM uses the REAL name → `RectangleWdgt`), all benign label shifts (eyeballed). Also surfaced the plural-boundary FILE-SELECTION gotcha (`rg -l '\bRectangleMorph\b'` skips a `RectangleMorphs`-only file). 165/165 (Chrome dpr 1+2, WebKit), `--homepage` boots, 37 visualisations regenerated. Plan `~/.claude/plans/batch6-rectanglemorph-rename.md`; full record + lessons in playbook §7.

BATCH 7 — the long tail of remaining `*Morph` classes -> all-`*Wdgt`, family by family, until the codebase is all-`*Wdgt`. ✅ **First family DONE (2026-06-16): the Icon family** — `IconMorph`→`IconWdgt` + 16 `*Morph` leaf icons → `*Wdgt`, all 93 `extends IconMorph` re-pointed (77 of them already-`*Wdgt` children long-orphaned on the un-renamed base); ZERO recapture; plan `~/.claude/plans/batch7-icon-family-rename.md`, record in playbook §7. **KEY LESSON: size a `*Morph` BASE's family by `rg 'extends <Base>'`, NOT the surface `class \w+Morph` grep** (here 16 `*Morph`-named leaves but 93 subclasses). Remaining candidates: `BoxMorph`(+`ClippingBox`/`MouseSensor`/`Pointer`), `CircleBoxMorph`/`SliderMorph`/`SliderButtonMorph`, the Switch/Toggle/Radio button lineages deferred from BATCH 4, `HandleMorph`, `ColorPickerMorph`/`ColorPaletteMorph`, `MenuMorph`/`PromptMorph`, … up to `WorldMorph` last.

YOUR TASK (only once the owner says to begin THIS batch): the next arc is **BATCH 7 — the long tail of remaining `*Morph` classes → all-`*Wdgt`, a coherent family at a time — its FIRST family (the Icon family) is already DONE 2026-06-16, so pick the NEXT family** (BATCH 4 + BATCH 5 + BATCH 6 + the BATCH-7 Icon family are the completed precedents — model their playbook §7 entries + the plans `~/.claude/plans/batch7-icon-family-rename.md` / `batch6-rectanglemorph-rename.md` / `batch4-button-family-rename-completion.md`). FIRST pick the next coherent family WITH THE OWNER (grep the live set with `rg -l 'class \w+Morph ' Fizzygum/src`; candidates incl. `BoxMorph`/`CircleBoxMorph`, the `SliderMorph`/`SliderButtonMorph`/`Switch`/`Toggle` button lineages deferred from BATCH 4, `HandleMorph`, `ColorPickerMorph`/`ColorPaletteMorph`, `MenuMorph`, … up to `WorldMorph` itself). Then produce a PLAN (do not implement yet), applying the playbook, structured in PHASES with continuous verification (per the owner-workflow memory note: run straight through, ONE end-of-arc review; commit/push only after that review). Specifically:
 - Use Explore/Plan agents to BUILD the chosen family's touch-list from greps: its `extends` + ALL subclasses (recurse), the **plural-aware** `\b`-bounded sweep of `extends`/`new`/`instanceof`/`findTopWidgetByClassNameOrClass`/string-literals + filename per class, and which (if any) of the family are PHOTOGRAPHED (a common base may surface in a hierarchy/context menu, a prompt title, OR the inspector hierarchy diagram — check subclasses too). NB the BATCH-6 lessons: `rg -l '\bFooMorph\b'` MISSES a file whose only hit is the plural `FooMorphs` (no `\b` before `s`) — sweep AND verify with `(s?)` + a `rg -o 'FooMorph\w*' | sort -u` catch-all; and `visualisation.html` is tracked+generated, so regenerate every touched test dir.
 - Settle any load-bearing menu-pixel / scope decision with the owner UP FRONT: is the chosen class's — or an in-scope subclass's — colloquial name DRAWN where a test photographs it (menu/context nav, prompt title, OR inspector diagram — the recapture trigger)? which subclasses are in-scope vs deferred? **But don't over-invest in predicting the recapture COUNT — BATCH 6 proved (a 3rd time) the byte-exact suite RED set is the only oracle; settle that the KIND of recapture is acceptable, then let the suite enumerate it.**
 - Call out the playbook-§4 gotchas (the capture `--clean` deletes-both-densities trap; run-macro-test-headless not rebuilding; a green capture ≠ a correct test — eyeball; hover/determinism after any drag — park the pointer at a neutral spot before the shot). Read Fizzygum-tests/DETERMINISM.md first.
 - Make the plan SELF-CONTAINED for a future no-context session (Orientation + Glossary like the prior plans), list the source touch-list and the test list per phase, and give verification steps: build_and_smoke, per-test capture at dpr 1 AND dpr 2 (only for the ones that actually move), build_and_test = 165/165 (current count), a WebKit cross-engine run, and a `--homepage` build+boot if any touched code ships there.
