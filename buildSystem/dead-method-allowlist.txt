# Dead-method allowlist for buildSystem/check-dead-methods.js
# Methods DEFINED in src but referenced nowhere (src + tests + harness).
# Seeded as a BASELINE to triage/delete later; the lint FAILS on any NEW dead method not here.
# Remove a name once you delete the method (or it gains a real caller). One name per line; # = comment.

addChildFirst
addToScope
allChildrenTopToBottom
allWdgtsAtPointer
amountToTranslateWithin
asArray
asArray_xywh
bottomCenterTight
bringUpTestMenu_InputEvents
brokenFor
childrenTopToBottom
cornersTight
darker
divideBy
drawCachedTexture
extentTight
firstSiblingsSuchThat
fullImageHashCode
fullImageNoShadow
fullMoveWithin
getBody
getName
getWorldMatrix
heightTight
howManySiblingsBeforeMeSuchThat
identifyBlockEnd
identifyViaTextLabel
inspectKeyEvent
isPickedUp
isSelectedListItem
isTouching
leftCenterTight
lighter
menuAtPointer
onlyContainingIntegers
positionTight
processCopy
putTicksNextToDoOnceBlocksThatHaveBeenRun
removeComments
resetTrackingOfDoOnceOccurrences
rightCenterTight
rotateZ
runLastWorkingProgram
runProgram
setTextDescription
showMoveHandle
slideBackTo
syntheticEventsMouseMoveWhileDragging_InputEvents
toggleShowBlanks
topCenterTight
userSetStart
visualisedText
widthTight

# DELIBERATELY RETAINED (not triage-to-delete): the BATCH settle tier. 0 callers as of the
# single-over-cores work -- the drag/drop gesture (ActivePointerWdgt.drop, 61f527f0) and
# sizeToTextAndDisableFitting both moved to a single settle over non-settling cores, removing the last
# batch call-sites. Kept as an available performance primitive (ONE O(1) flush over a multi-add bundle
# vs N self-settles) that may be wanted again; its flag + guard stay wired. See the method's comment.
_settleLayoutsAfterBatch

# §4.1 PURE-MEASURE KEYSTONE (proper-layouts campaign, Stage A -- 2026-06-28). The side-effect-free
# TextWdgt.preferredExtentForWidth(availW) is the production pure measure that the vertical-stack /
# window / scroll arranges will CONSUME (shedding their mutate-then-read-back) in Stages B-E. Landed
# ALONE first (no consumer by design), proven byte-exact suite-wide by the Stage-0 probe (4022
# measure-vs-commit differentials, 0 mismatches). Remove this entry once Stage B wires the first
# container consumer. See docs/proper-layouts-4.1-pure-measure-campaign-plan.md.
preferredExtentForWidth
