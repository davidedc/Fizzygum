# Dead-method allowlist for buildSystem/check-dead-methods.js
# Methods DEFINED in src but referenced nowhere (src + tests + harness).
# The lint FAILS on any NEW dead method not here. Remove a name once you delete the method (or it gains a real
# caller). One name per line; # = comment. Settle-tier wrapper/core symmetry pairs (a public <name> whose
# _<name>NoSettle core is live, or vice-versa) are AUTO-exempt via liveSettleTwin -- they need no entry here.
#
# 2026-06-30 sanitization (settle-tier follow-up, Topic 5): the baseline was a triage-to-delete seed; 34
# genuinely-dead methods were DELETED from src (the dead half of the *Tight accessor family incl. its 4-corner
# cascade, abandoned geometry/tree utilities, and excluded-from-homepage dead debug code). The entries below
# are the DELIBERATELY-RETAINED remainder, grouped by why they stay.

# --- Kept as intentional API: coherent families / clean value-class accessors (a member is just currently
#     unused; deleting it would leave a lopsided surface). ---
asArray                 # Point [x,y] / Rectangle [l,t,r,b] conversion API (the _xywh variant was deleted)
divideBy                # Point arithmetic, parallel to the live multiplyBy
rotateZ                 # Point3D, completes the rotateX / rotateY / rotateZ family
colRowFor               # SheetModel address algebra, the inverse of the live addressFor (colToLetters/lettersToCol/addressFor/colRowFor); the view's old value-paint loop used it, widgetised away in Phase 8 — kept as the symmetric public API (hit-test/scroll sub-phases resolve addresses back to col/row)
getBody                 # Macro immutable value-class accessor
toggleShowBlanks        # StringWdgt menu-toggle action, same shape as toggleWeight / toggleItalic / ...
bringUpTestMenu_InputEvents                        # MacroToolkit L1 synthetic-input primitive (test-authoring family)
syntheticEventsMouseMoveWhileDragging_InputEvents  # MacroToolkit L1 synthetic-input primitive (test-authoring family)

# --- LCL / fizzytiles experimental subsystem: LCLProgramRunner is not yet instantiated anywhere (in-progress
#     live-coding feature, excluded from the homepage build). Retained pending that subsystem's completion or a
#     deliberate decision to remove it wholesale. ---
addToScope
resetTrackingOfDoOnceOccurrences
putTicksNextToDoOnceBlocksThatHaveBeenRun
runProgram
runLastWorkingProgram
getWorldMatrix
identifyBlockEnd

# --- Dispatched ONLY by computed name (invisible to the static reference scanner): the reactive-connection
#     dispatch resolves the connector by "_#{@action}Connector" (ControllerMixin._fireConnection), so the literal
#     method name never appears as a token. See connection-cascade-settle-fix-plan.md. ---
_setFontSizeConnector    # reactive connector for setFontSize; reached only via @target["_setFontSizeConnector"] computed dispatch
_takeSliderValueConnector # reactive connector for takeSliderValue (PromptWdgt prompt slider); reached only via @target["_takeSliderValueConnector"] computed dispatch
