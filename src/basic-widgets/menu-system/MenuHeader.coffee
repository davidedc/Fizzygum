# EMPTY. A pop-up's title header is its frame's TITLE BAR (FrameBarWdgt, laid out from the
# frame's transient bar spec: a rounded header box with the title centred in it, and a tap on
# it pins the frame). Nothing derives from this class and nothing instantiates it.

class MenuHeader extends BoxWdgt
