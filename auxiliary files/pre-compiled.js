// pre-compiled can be generated by appending
//     ?generatePreCompiled
// to a page. A pre-compiled.zip file will
// then download, which can be put in the
// "right" place in the builds by running
//     sh inject_pre-compiled_into_build.sh

window.preCompiled = false;
